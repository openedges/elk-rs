# 개발 진행 이력

- 본 문서는 기존 `AGENTS.md`의 `진행된 작업` 섹션을 분리 보관한 기록이다.
- 신규 단계 완료 시 이 문서에 누적 기록하고, `AGENTS.md`의 핵심 스냅샷/우선순위를 동기화한다.

## 진행된 작업
- Step M-5 문서 동기화(2026-02-20): 최신 parity 산출물 기준으로 문서를 갱신했다. `AGENTS.md` 핵심 스냅샷은 full parity `matches=1142/1439`, `drift=297`, `total_diffs=5710`, `java_non_ok=9`와 tickets parity `matches=107/109`, `drift=2`, `total_diffs=40`, `java_non_ok=1(588)`로 업데이트했고, tickets 잔여 drift를 `213_componentsCompaction`, `701_portLabels` 2건으로 재정렬했다.
- Step M-5 tickets 잔여 drift 정밀화(2026-02-20): `plugins/org.eclipse.elk.alg.common/src/org/eclipse/elk/alg/common/nodespacing/node_label_and_size_calculator.rs`에서 inside port-label 최소 크기 경로를 실제 `process_node` 흐름에 연결하고(`setup_inside_port_label_cell_minimums` 호출), `N/E/S/W` side별 inside cell 최소 폭 보정(포트 간격/라벨 간격/label-node 간격/borderOffset 기반) 및 `MINIMUM_SIZE` 제약 노드 제외 가드를 추가했다. 결과적으로 `tickets/layered/701_portLabels.elkt` 단건 full diff가 `139 -> 129`로 감소했고, `tickets/layered/453_interactiveProblems.elkt` 회귀 유입 없이 유지됨을 확인했다.
- Step M-5 tickets parity 재검증(2026-02-20): `MODEL_PARITY_SKIP_JAVA_EXPORT=true sh scripts/run_model_parity_by_category.sh tickets` 재실행 결과 `matches=107/109`, `drift=2`, `total_diffs=40`, `errors=0`, `timeouts=0`, `java_non_ok=1(588)` 유지. 잔여 drift는 `tickets/layered/213_componentsCompaction.elkt`, `tickets/layered/701_portLabels.elkt` 2건이며, `--max-diffs-per-model 5000` 기준 총 diff는 `274 -> 264`로 추가 감소.
- Step M-5 문서 동기화(2026-02-19): tickets parity 최신 결과(`matches=101/109`, `drift=8`, `total_diffs=112`)를 기준으로 `AGENTS.md` 핵심 스냅샷/우선순위를 갱신했다. 완료 항목은 `425_selfLoopInCompoundNode` 수렴까지 반영하고, 잔여 drift 8건(`213, 302, 352, 368, 453, 502, 665, 701`)을 다음 실행 큐로 고정했다.
- Step M-5 p5 self-loop compound node size 정합(2026-02-19): `plugins/org.eclipse.elk.alg.layered/src/org/eclipse/elk/alg/layered/intermediate/label_and_node_size_processor.rs`에 phase2b(`NodeDimensionCalculation` 재실행) 이후 self-loop holder 노드만 대상으로 포트 기반 node sizing 재적용(pass)을 추가했다. Java 대비 phase2b 이후 다시 축소되던 self-loop helper-port 노드(`tickets/layered/425_selfLoopInCompoundNode.elkt`)의 폭 재축소를 방지해 `Node_1 width`를 Java와 동일한 `30`으로 복원했다.
- Step M-5 p5 회귀 테스트 추가(2026-02-19): `plugins/org.eclipse.elk.alg.layered/tests/resources/issues/issue_425_self_loop_in_compound.elkt` 및 `plugins/org.eclipse.elk.alg.layered/tests/issue_425_test.rs`를 추가해 self-loop 포함 compound leaf 노드의 폭이 Java 기준(`30.0`)으로 유지되는지 고정 검증했다. 검증: `cargo test -p org-eclipse-elk-alg-layered --test issue_425_test --test issue_502_test --test issue_665_test --test issue_701_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test issue_425_test` 통과.
- Step M-5 tickets parity 재측정(2026-02-19): `MODEL_PARITY_SKIP_JAVA_EXPORT=true sh scripts/run_model_parity_elk_vs_rust.sh external/elk-models perf/model_parity_tickets` 재실행 결과 `matches=101`, `drift=8`, `total_diffs=112`로 개선(직전 `100/9/116` 대비 `+1/-1/-4`). 제거된 drift: `tickets/layered/425_selfLoopInCompoundNode.elkt`. 잔여 drift: `213, 302, 352, 368, 453, 502, 665, 701`.
- Step M-5 p5 low-diff 보정(2026-02-19): `plugins/org.eclipse.elk.alg.layered/src/org/eclipse/elk/alg/layered/intermediate/hierarchical_node_resizing_processor.rs`에서 계층 self-loop source 외부 포트 전송 보정 조건을 확장했다. 기존에는 더미가 self-loop edge를 직접 보유할 때만 east 포트 `y` 보정을 적용했지만, 현재 파이프라인에서는 해당 더미가 edge-less 상태로 전송되는 케이스(`tickets/layered/552_hierarchySelfLoopPorts.elkt`)가 있어 조건이 누락됐다. `is_pure_self_loop_external_dummy`를 도입해 `edge 없음` 또는 `연결 edge가 self-loop(or self-loop origin)`인 경우를 self-loop source로 간주해 Java와 동일한 `dummy y` 보정을 적용했다.
- Step M-5 p5 회귀 테스트 추가(2026-02-19): `plugins/org.eclipse.elk.alg.layered/tests/resources/issues/issue_552_hierarchy_self_loop_ports.elkt`를 추가하고 `plugins/org.eclipse.elk.alg.layered/tests/issue_552_test.rs`에 `issue_552_hierarchical_self_loop_source_port_matches_java_y` 테스트를 추가했다. 계층 self-loop 모델에서 `p_loop_src.y=12`, `p_loop_tgt.y=32`, `n2.y=12`를 고정 검증해 동일 회귀를 재발 방지한다.
- Step M-5 p5 검증(2026-02-19): 단건 parity(`tickets/layered/552_hierarchySelfLoopPorts.elkt`)가 `drift(3)`에서 `match(0)`로 수렴했다. low/medium 3-model subset(`tickets/layered/288_selfLoopsCauseNPE_{a,b}.elkt`, `552_hierarchySelfLoopPorts.elkt`)은 `matches=2`, `drift=1`, `total_diffs=26`으로 확인됐다. `phase_focus_top_low_medium` 25개 subset 재검증은 `matches=25`, `drift=0`, `total_diffs=0`을 유지했다. 테스트는 `cargo test -p org-eclipse-elk-alg-layered --test issue_552_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test issue_552_test` 통과.
- Step M-5 회귀 수정(2026-02-19): `plugins/org.eclipse.elk.alg.layered/src/org/eclipse/elk/alg/layered/graph/l_graph_util.rs`의 `LGraphUtil::initialize_port`에서 `port` mutex를 잡은 상태로 `calc_port_offset(port, ...)`를 재호출하던 자기 재락(deadlock) 경로를 제거했다. `PORT_BORDER_OFFSET` 계산을 lock 밖으로 분리해 Java 동작은 유지하면서 재락만 방지했다.
- Step M-5 timeout 해소 검증(2026-02-19): 단건 parity(`tests/layered/cross_counting/south_multiple.elkt`, `north_multiple.elkt`)를 재실행해 기존 `timeout(120s)` 재현이 `ok(수 ms)`로 해소됨을 확인했다.
- Step M-5 full parity 재측정(2026-02-19): `perf/model_parity_full/java`는 입력/레이아웃 JSON이 비어 있어, `perf/model_parity/java/java_manifest.tsv`를 현재 워크스페이스 절대경로로 치환한 임시 manifest로 전체 1448 row를 재실행했다. 결과는 `compared=1439`, `matches=1135`, `drift=304`, `skipped=9`, `errors=0`, `timeouts=0`, `total_diffs=5783`.
- Step M-5 drift 재분석(2026-02-19): `python3 scripts/analyze_layered_drift.py --diff-details /tmp/elk_full_runtime_fixed/diff_details.tsv --manifest /tmp/elk_full_runtime_fixed/rust_manifest.tsv` 기준 분류는 `p2_layering=129`, `p5_edge_routing=92`, `p4_node_placement=57`, `p3_crossing_order=22`, `p4_label_management=4`로 집계되었고, phase-focus 산출물(`perf/model_parity_full/phase_root_summary.md`, `phase_focus_top.md`)을 갱신했다.
- Step M-5 품질 게이트 재검증(2026-02-19): `cargo clippy --workspace --all-targets`는 종료 코드 0. `cargo test --workspace`는 `plugins/org.eclipse.elk.graph.json/tests/edge_coords_test.rs`의 `edge_coords_round_trip` 1건 실패로 종료 코드 101이며, 기존 워크트리 변경 상태(`plugins/org.eclipse.elk.graph.json/tests/edge_coords_test.rs` 수정 상태)와 동일한 known fail로 기록한다.
- Step M-5 P3 `variants.elkt` drift 해소(2026-02-19): `plugins/org.eclipse.elk.alg.layered/src/org/eclipse/elk/alg/layered/graph/transform/elk_graph_importer.rs`에서 Java 대비 과도하게 동작하던 non-external port side 선결정(`determine_port_side`) 경로를 제거했다. `transform_port`는 Java와 동일하게 모델 원본 `PORT_SIDE`만 복사하고(`Undefined` 포함), side 해석은 `LGraphUtil::initialize_port`/후속 processor 경로에 위임하도록 정렬했다.
- Step M-5 P3 원인/수치 확인(2026-02-19): 단일 모델(`tests/core/label_placement/port_labels/variants.elkt`) 재측정에서 수정 전 `drift=1, total_diffs=50`(port/port-label x/y 반전) → 수정 후 `match=1, total_diffs=0`으로 수렴했다.
- Step M-5 P3 seed subset 재검증(2026-02-19): 3개 우선 모델(`high-degree-example`, `labels1`, `variants`) subset을 재실행해 `total=3, matches=3, drift=0, total_diffs=0`을 확인했다(기존 `2/3`에서 `3/3` 달성).
- Step M-5 품질 게이트 재검증(2026-02-19): `cargo test -p org-eclipse-elk-alg-common --test port_label_placement_variants_test`, `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test`, `cargo test --workspace`, `cargo clippy --workspace --all-targets`를 재실행해 모두 종료 코드 0을 확인했다.
- Step M-5 문서/작업트리 정리(2026-02-19): p2 Java trace 확인용으로 임시 수정했던 `external/elk/plugins/org.eclipse.elk.alg.layered/src/org/eclipse/elk/alg/layered/p1cycles/GreedyCycleBreaker.java` 디버그 출력 코드를 원복해 서브모듈 dirty 상태를 해소.
- Step M-5 parity 재검증 기반 정리(2026-02-19): `perf/model_parity_full/java`는 manifest만 존재하고 실제 `input/layout` 디렉터리가 없어 현 환경에서 직접 재실행이 불가했다. 대신 로컬에 완전한 baseline이 있는 `perf/model_parity/java`를 기준으로 경로를 현재 워크스페이스로 재작성한 manifest(`/tmp/m5_recheck_20260219/java_manifest_full_local.tsv`)를 생성해 release/seed=1 조건으로 재측정을 진행.
- Step M-5 3-model subset 재실행(2026-02-19): 대상 `algebraic_rlc_RLC.elkt`, `backtrack_ramprollback_RampRollback.elkt`, `continuous_cartracking_CarTracking.elkt`를 `compare_model_parity_layouts.py --max-diffs-per-model 500`로 재검증한 결과 `matches=1`, `drift=2`, `total_diffs=369`를 확인. 모델별 diff는 `algebraic=117`, `backtrack=0(match)`, `cartracking=252`.
- Step M-5 `phase_focus_top_low_medium` 재실행(2026-02-19): 25개 모델을 동일 조건(`--max-diffs-per-model 500`)으로 재검증한 결과 `matches=18`, `drift=7`, `total_diffs=101`. 직전 기록(`matches=14`, `drift=11`, `total_diffs=140`) 대비 `+4/-4/-39` 개선.
- Step M-5 full parity 스냅샷 갱신(2026-02-19): 전체 1448 row baseline을 재실행하고 `--max-diffs-per-model 20` 기준으로 집계한 결과 `compared=1439`, `matches=1140`, `drift=299`, `skipped(java/rust non-ok)=9`, `errors=0`, `total_diffs=5702`를 확인. 이전 스냅샷(`1132/307/5803`) 대비 `matches +8`, `drift -8`, `total_diffs -101`.
- Step M-5 p3 Java 정합 보정(2026-02-19): `LayerSweepCrossingMinimizer`의 sweep 방향 랜덤 선택 경로를 Java와 동일하게 shared RNG(`random.nextBoolean()`)만 사용하도록 정리하고, Rust 전용 heuristic RNG 동기화 코드를 제거(`layer_sweep_crossing_minimizer.rs`). 검증: `cargo check -p org-eclipse-elk-alg-layered --lib` 통과, parity 재측정은 수치 변동 없음(3-model `1/2/369`, phase-focus `18/7/101`, full `1140/299/5702` 유지).
- Step M-5 p2 진단(2026-02-18): `algebraic_rlc_RLC.elkt` 단건에 대해 cycle-breaker 추적을 추가해 edge origin 매핑/역전 집합을 수집(`ELK_TRACE_EDGE_ORIGIN_MAP`, `ELK_TRACE_CYCLE_REVERSALS`, `ELK_TRACE_CYCLE_CHOICES`). Rust 경로에서 cycle-breaker 역전은 `E3/E15`(origin `53/65`)로 확인됐고, `compare_model_parity_layouts.py --max-diffs-per-model 500` 기준 단건 drift는 `117 diff(section 76 / coordinate 31 / structure 6 / ordering 2 / other 2)`로 재현.
- Step M-5 p2 가설 검증(2026-02-18): cycle-breaker random 상태 가설을 `ELK_DEBUG_CYCLE_RANDOM_PREFETCH` 실험으로 점검. `algebraic` 단건은 prefetch `0 -> 117 diff`, `1 -> 77 diff`, `4 -> 130 diff` 등 변동을 확인했지만 3-model subset(`algebraic/backtrack/cartracking`) 총합은 `594 -> 571(prefetch=1)`로 일부 개선/일부 악화가 공존해 전역 fix로 채택하지 않음. 강제 역전(`ELK_DEBUG_CYCLE_FORCE_REVERSE_ORIGINS=51,65`)도 `117 -> 116 diff`로 효과 미미.
- Step M-5 p2 Java 정합 보정(2026-02-18): `NetworkSimplexLayerer`의 iteration limit 계산을 Java와 동일하게 `floor(sqrt(n))`로 수정(`network_simplex_layerer.rs`). 기존 Rust의 `round(sqrt(n))` 편차를 제거했으며, 검증 결과 `cargo check -p org-eclipse-elk-alg-layered --lib`, `cargo check -p org-eclipse-elk-alg-common --lib`, `cargo test -p org-eclipse-elk-alg-layered --test network_simplex_bk_test` 통과. 다만 `algebraic` 단건 및 3-model subset parity 수치는 기존과 동일(`117`, `594`)하여 추가 root cause 분석이 필요.
- Step M-5 p2 추적 인프라 보강(2026-02-18): `ElkGraphImporter` edge origin trace(`ELK_TRACE_EDGE_ORIGIN_MAP`), `GreedyCycleBreaker` 선택/역전 trace(`ELK_TRACE_CYCLE_CHOICES`, `ELK_TRACE_CYCLE_REVERSALS`), `NetworkSimplex` 반복 중단 trace(`ELK_TRACE_NETWORK_SIMPLEX`)를 추가해 p2 layering 편차를 모델 단위로 추적 가능하도록 정리.
- Step M-5 p5 스플라인/루프 보정(2026-02-18): `HorizontalGraphCompactor`에 `EdgeRouting::Splines` 경로를 추가하고 spline segment compaction/constraint/좌표 보정 루틴을 연결했다. `LabelDummyRemover`는 spline 라우팅에서 post-compaction 수평 보정을 건너뛰도록 분기했고, `RoutingSlotAssigner`의 loop activity 인덱싱을 Java 동작과 맞게 `rem_euclid` 기반으로 보정했다.
- Step M-5 p2-p5 전송 경계 보강(2026-02-18): `ElkGraphLayoutTransferrer`에 centered inside node label 최소 폭 반영 및 external-port resize 분기 정합을 보강했고, parity runner trace에 `PORT_INDEX`/`PORT_SIDE`를 포함해 포트 순서 기반 편차를 직접 점검할 수 있도록 확장했다.
- Step M-5 검증(2026-02-18): `cargo check -p org-eclipse-elk-alg-layered --lib`, `cargo check -p org-eclipse-elk-alg-common --lib`, `cargo check -p org-eclipse-elk-graph-json --bin model_parity_layout_runner`, `cargo test -p org-eclipse-elk-alg-layered --test network_simplex_bk_test`, `cargo test -p org-eclipse-elk-graph-json --test json_graph_tests`, `cargo clippy -p org-eclipse-elk-alg-layered --lib`, `cargo clippy -p org-eclipse-elk-graph-json --all-targets`를 실행해 통과를 확인했다(기존 warning만 유지).

- 문서 체계 정리(2026-02-18): `AGENTS.md`를 핵심 운영 규칙/품질 게이트/현재 스냅샷 중심으로 축약하고, 상세 진행 내용(단계별 구현/검증/수치/TODO)은 `HISTORY.md`에만 기록하도록 운영 원칙을 확정.
- 진행률(최신): 모델 패리티 333건 중 `examples/general/spacing/portsSurrounding.elkt` 드리프트 6건을 해결하여 해당 구간 오차 0건으로 감소. `inside`/복합 노드 포트 라벨 배치 규칙 드리프트 3건(`inside_two_default_hierarchical`, `inside_two_with_one_edge`, `inside_three_with_one_edge`) 추가 해소로 동일 테스트군(`port_label_placement_variants_test`) 재확인 완료.
- layered `LabelAndNodeSizeProcessor`에서 동/서측 자유 배치 보정 반영 버그 수정: `portsSurrounding` 오차 원인인 `apply_free_vertical_port_surrounding_adjustment` 반환값 오류를 수정해 동서측 포트 주변 간격 보정을 적용하도록 처리.
- `.gitignore` 추가, `external/elk` 서브모듈 추가
- core/math, graph, util, options, data 기본 구조 및 일부 기능 포팅
- `ElkUtil` 확장: 절대/상대 좌표, 벡터 체인, junction points, 스케일링, child area 계산, 기본값 설정, 방문자/검증, 디버그 경로/문자열
- `CoreOptions` 확장: algorithm/resolved, alignment/aspect ratio, bend points, position, priority, random seed, separate components, padding, label 배치 옵션
- graph util 확장: all_incoming/outgoing/incidents (node/shape)
- validation 모듈 추가 (`GraphIssue`, `GraphValidationException`)
- `LayoutAlgorithmResolver`, `GraphValidator`, `DeprecatedLayoutOptionReplacer`, `ElkGraphAdapters` 포팅 및 테스트 추가
- `ElkSpacings`, `IndividualSpacings`, `FixedLayoutProvider`, `RecursiveGraphLayoutEngine` 포팅 및 테스트 추가
- 레이아웃 구성 저장소/매니저 root 처리 보강 및 테스트 추가
- layered 메타데이터 보강(지원 기능/기본값) 및 테스트 추가
- layered considerModelOrder/groupModelOrder 옵션/메타데이터, ordering 전략 enum, 컴포넌트 ordering 전략, 테스트 추가
- layered LGraph 모델(LGraph/Layer/LNode/LPort/LEdge/LLabel/LMargin/LPadding) 및 최소 LGraphUtil/옵션(PortType, InteractiveReferencePoint, InternalProperties) 포팅
- layered Tarjan SCC 유틸 포팅(InternalProperties 확장 포함)
- layered LGraphUtil 주요 기능 포팅(노드 리사이즈/오프셋/레이어 배치/그래프 속성 계산/포트 생성·초기화/외부 포트 더미/좌표계 변환)
- layered InternalProperties 확장(EDGE/IN_LAYER 제약, EXT_PORT_SIDE/EXT_PORT_SIZE, PORT_RATIO_OR_POSITION, MODEL_ORDER)
- layered LayeredOptions 확장(layerConstraint, PORT_ANCHOR/PORT_INDEX/PORT_SIDE alias)
- layered LayeredPhases 단계 enum 추가
- layered Spacings/LayeredSpacings 유틸 포팅, LayeredOptions spacing(CoreOptions) alias 추가, NodeType ordinal/values 추가
- layered IntermediateProcessorStrategy enum(노옵) 및 InternalProperties(PROCESSORS/RANDOM/SPACINGS/ORIGINAL_PORT_CONSTRAINTS) 추가, GraphConfigurator 스켈레톤 구성
- layered 옵션 확장(cycle/layering/crossing/nodePlacement/edgeRouting/compaction/highDegree/기타) 및 신규 옵션 enum 추가, 메타데이터/테스트 보강
- layered GraphConfigurator 구성 보강(phase/processor 구성, greedy switch/compaction/방향/라벨/래핑/파티션 처리) 및 core AlgorithmAssembler processor 파이프라인 공유 구조(Arc<Mutex>) 정리
- layered `ElkLayered`/`LayeredLayoutProvider` 기본 골격 포팅, 테스트 컨트롤러 훅 연결
- layered graph transform(`IGraphTransformer`, `ElkGraphTransformer`, `ElkGraphImporter`, `ElkGraphLayoutTransferrer`) 최소 구현 및 ORIGIN 매핑 도입
- layered ComponentsProcessor/CompoundGraph 전·후처리 스텁 추가
- core `ILayoutProcessor`에 `Any` 바운드 추가 및 AlgorithmAssembler 어댑터 보완
- layered layout transferrer에서 노드/포트/라벨/엣지 섹션 및 벤드포인트 적용 최소 구현
- core PortSide 유틸 확장(opposed/adjacent/방향 매핑/수평·수직)
- layered GraphTransformer(방향 변환/미러/전치/회전) 포팅 및 Direction pre/postprocessor 연결
- LayeredOptions CoreOptions alias 보강(node labels placement/padding, position, node size minimum), KVectorChain iter_mut 추가
- layered GreedyCycleBreaker(p1cycles) 포팅 및 CYCLIC 내부 프로퍼티 추가
- layered DepthFirstCycleBreaker(p1cycles) 포팅 및 전략 연결
- layered LongestPathLayerer(p2layers) 포팅 및 NetworkSimplex/LongestPath 전략 연결
- layered LongestPathSourceLayerer(p2layers) 포팅 및 전략 연결
- layered Breadth/DepthFirst ModelOrder layerer(p2layers) 포팅 및 전략 연결
- layered CoffmanGraham/Interactive/StretchWidth/MinWidth layerer(p2layers) 포팅 및 전략 연결
- layered p3order NoCrossingMinimizer 포팅 및 CrossingMinimizationStrategy(None) 연결
- layered p3order counting 유틸(BinaryIndexedTree/CrossMinUtil/IInitializable) 포팅
- layered p3order CrossingsCounter/HyperedgeCrossingsCounter/AllCrossingsCounter 포팅
- layered p3order LayerSweepCrossingMinimizer 스켈레톤 및 CrossMinType enum 추가, 전략 연결(LayerSweep/Median)
- layered p3order 포트 분배기(AbstractBarycenter 기반 NodeRelative/LayerTotal, GreedyPortDistributor) 및 greedyswitch BetweenLayerEdgeTwoNodeCrossingsCounter 포팅
- layered p3order BarycenterHeuristic/ModelOrderBarycenterHeuristic, ForsterConstraintResolver, ICrossingMinimizationHeuristic 포팅 및 preserveorder CMGroupModelOrderCalculator 추가
- layered InternalProperties 확장(in-layer successor constraints, barycenter associates, max model order nodes)
- layered p3order LayerSweepCrossingMinimizer 전체 로직 포팅, GraphInfoHolder/LayerSweepTypeDecider/MedianHeuristic/SweepCopy 보강
- layered greedyswitch 세부(스위치 결정/휴리스틱/매트릭스/NS 교차 카운터) 및 preserveorder comparator, SortByInputModelProcessor 추가
- layered InternalProperties 확장(inside connections, long-edge target, model order/weight, initial order flags 등) 및 초기화/랜덤 리셋 로직 보강
- layered p4nodes Simple/Interactive/LinearSegments node placer 포팅, NodePlacementStrategy 일부 연결, ORIGINAL_DUMMY_NODE_POSITION 내부 프로퍼티 추가
- layered p4nodes Brandes-Koepf(BK) node placer/aligner/compactor/threshold strategies 포팅 및 NeighborhoodInformation 연동, NodePlacementStrategy(BK) 연결
- layered p4nodes NetworkSimplex node placer 포팅 및 NodePlacementStrategy(NetworkSimplex) 연결
- alg.common NetworkSimplex(NGraph/NNode/NEdge) 포팅 및 layered NetworkSimplexLayerer 연결
- layered p5 edge routing 기본 포팅(EdgeRouterFactory, Polyline/Orthogonal router, orthogonal hyperedge routing generator/segment/dependency/cycle splitter) 및 GraphConfigurator 연동
- layered p5 splines 정식 포팅(SplineEdgeRouter/SplineSegment/SplinesMath/Rectangle/NubSpline/NubsSelfLoop/FinalSplineBendpointsCalculator) 및 wiring
- layered InternalProperties spline 관련 키 추가(SPLINE_*), IntermediateProcessorStrategy에 FinalSplineBendpointsCalculator 연결
- core KVectorChain API 보강(insert/clear/get_first/get_last/remove_last)
- layered spline 라우팅/네트워크 심플렉스 관련 최소 회귀 테스트 추가(스플라인 라우팅, 노드 배치/레이어링 전략)
- alg.mrtree 초기 골격 포팅(크레이트 추가, 기본 그래프 모델/옵션 enum/InternalProperties 스켈레톤)
- alg.mrtree 본체 포팅(옵션/메타, TreeUtil/Importer/LayoutTransferrer, 프로세서 파이프라인, p4 edge routing 스텁) 및 `cargo check -p org-eclipse-elk-alg-mrtree` 통과
- alg.mrtree p5 edge routing 포팅(EdgeRouter/MultiLevelEdgeNodeNodeGap)
- layered NetworkSimplex/BK 주변 테스트 추가
- layered BinaryIndexedTree 테스트 추가
- BK/NetworkSimplex 레이아웃 hang 해결(Spacings graph lock 데드락, NetworkSimplexPlacer apply_positions 노드 재락 문제, ElkLayered clear_layers 제거) 및 테스트 ignore 해제/통과
- layered 노드 배치 스모크 테스트(Simple/LinearSegments) 추가, network_simplex_bk_test 5개 통과
- layered layout transferrer 보강(레이어 노드 수집, 그래프 크기 적용, 엣지 섹션 설정 중 재대여 방지)
- layered LGraphUtil place_nodes_horizontally 레이어 폭 계산 보강
- layered LayeredSpacingTest/OverallLayoutTest 포팅 및 통과
- layered CrossMinUtil 포트 순서(in_north_south_east_west_order) 테스트 추가
- BK aligner/aligned layout align 체인 루프 가드 추가(비정상 align 사이클 방지)
- BK compactor place_block 루프 가드 추가(align 체인 비정상 순환 대비)
- layered CrossingsCounter 간단 교차 그래프 테스트 추가 및 통과
- layered CrossingsCounter in-layer 교차 테스트 추가 및 통과
- layered CrossingsCounter 병렬 엣지 교차 테스트 추가 및 통과
- layered CrossingsCounter 교차+중간 엣지 테스트 추가 및 통과
- layered CrossingsCounter self-loop 무시 테스트 추가 및 통과
- layered AllCrossingsCounter 교차 그래프 테스트 추가 및 통과
- 성능 측정: layered 레이아웃 전용 perf 스크립트 추가 및 README 갱신
- 성능 측정: perf run/compare/summary/regression 스크립트에 layered 결과 파일 포함
- 성능 측정: scripts/README.md에 layered perf 스크립트/환경변수 추가
- alg.common NetworkSimplex 테스트 추가
- alg.topdownpacking 포팅(옵션/메타/레이아웃 프로바이더/배치/화이트스페이스 제거)
- alg.rectpacking 포팅(옵션/메타/유틸/폭 근사·패킹·화이트스페이스 제거/레이아웃 프로바이더/인터랙티브 방문자, rows thread-local 저장), `cargo check -p org-eclipse-elk-alg-rectpacking` 통과
- alg.vertiflex 포팅(옵션/메타/내부 프로퍼티/레이아웃 프로바이더, p1~p4 배치·라우팅), `cargo check -p org-eclipse-elk-alg-vertiflex` 통과
- alg.force 포팅(Force/Stress 옵션·메타·그래프 모델·모델/메이저라이제이션·레이아웃 프로바이더) 및 ForceImportTest 이식, `cargo check -p org-eclipse-elk-alg-force`/`cargo test -p org-eclipse-elk-alg-force` 통과
- alg.common polyomino/compaction 옵션·구조·Successor·PolyominoCompactor 포팅 및 정리, `cargo check -p org-eclipse-elk-alg-common` 통과
- alg.disco 포팅(옵션/메타/레이아웃 프로바이더, DCGraph/컴포넌트 분리/GraphTransformer, DCPolyomino/Polyomino compaction) 및 `cargo check -p org-eclipse-elk-alg-disco` 통과
- alg.disco/topdownpacking/rectpacking/vertiflex 메타데이터 테스트 추가 및 `cargo test -p org-eclipse-elk-alg-{disco,topdownpacking,rectpacking,vertiflex}` 통과
- alg.spore 포팅(옵션/메타/그래프/임포터/스팬닝 트리/겹침 제거·ShrinkTree 단계/레이아웃 프로바이더), 트리-정점 동기화 추가
- alg.spore 테스트 추가(스캔라인 겹침 감지, overlap/underlap/distance) 및 `cargo test -p org-eclipse-elk-alg-spore` 통과
- alg.radial 포팅(옵션/메타/레이아웃 프로바이더/p1~p2/중간 처리) 및 테스트 추가(center on root, metadata), `cargo test -p org-eclipse-elk-alg-radial` 통과
- alg.libavoid 포팅(옵션/메타/레이아웃 프로바이더 스텁/서버 스텁) 및 메타데이터 테스트 추가, `cargo test -p org-eclipse-elk-alg-libavoid` 통과
- alg.graphviz 포팅(dot/layouter 크레이트, 옵션/메타/레이아웃 프로바이더 스텁, dot transform enums) 및 메타데이터 테스트 추가, `cargo test -p org-eclipse-elk-alg-graphviz-layouter` 통과
- label manager 옵션 분리(Core vs Labels) 및 메타데이터/테스트 추가
- `PropertyConstantsDelegator` 옵션 타입 확장(label manager, topdown size approximator, layout algorithm data)
- 테스트 추가/확장 (`layout_algorithm_metadata`, `label_management_options`, `deprecated_layout_option_replacer`, `elk_graph_adapters` 등), `cargo clippy --workspace --all-targets` 통과
- layered BasicEdgeRouter/BasicLayerAssignment/BasicNodePlacement/BasicModelOrder/BasicCycleBreaker 테스트 포팅 및 통과
- layered BasicConsiderModelOrder 테스트 포팅 및 considerModelOrder 가중치 조합 hang 해결(재귀 가드/try_lock fallback)
- layered Issue562 테스트 포팅 및 통과
- layered NorthSouthPortPreprocessor/Postprocessor 포팅 및 LabelAndNodeSizeProcessor 최소 포트 배치 보강(남/서쪽 역순 배치)
- layered AllowNonFlowPortsToSwitchSides 테스트 포팅 및 통과(allowNonFlowPortsToSwitchSides 지원)
- layered LabelAndNodeSizeProcessorOffset 테스트 포팅 및 FixedPos/FixedRatio 포트 직교 좌표 보정 보강
- layered GreedyPortDistributor 테스트 일부 포팅(단순 6 케이스) 및 통과
- layered GreedyPortDistributor 계층/compound 케이스 테스트 포팅(더미 외부 포트/중첩 그래프 헬퍼 포함) 및 통과
- layered LayerSweepTypeDecider 테스트 포팅 및 통과
- layered AbstractBarycenterPortDistributor 테스트 포팅 및 통과
- layered AbstractBarycenterPortDistributor 포트 분배 로직 보강(고정 순서 제외 시 정렬 수행)
- layered NorthSouthEdgeAllCrossingsCounter 테스트 포팅 및 통과
- layered NorthSouthEdgeNeighbouringNodeCrossingsCounter 테스트 포팅 및 통과
- layered BetweenLayerEdgeTwoNodeCrossingsCounter 테스트 포팅 및 통과
- layered InLayerEdgeTwoNodeCrossingCounter 테스트 포팅 및 통과
- layered GreedySwitchProcessor 테스트 포팅 및 self-loop/in-layer 포트 분배 데드락 해결, greedy_switch_processor_test 통과
- layered Issue680/Issue682/Issue871 테스트 포팅(현 Rust 포트 상태에 맞춘 회귀 불변식 중심 검증) 및 개별 테스트 통과
- layered Issue734 테스트 포팅(노드/엣지 스팬 중심 정렬 불변식) 및 통과
- layered 패키지 clippy 재실행(`cargo clippy -p org-eclipse-elk-alg-layered --tests`) 통과 확인
- layered CrossingsCounter 테스트 기대값 Java 원본과 동기화(`intoSamePort` 기대값 2) 및 통과
- layered GreedyPortDistributor 테스트(다중 western crossing) 포트 재배열 기대 순서 보정 및 통과
- layered Issue447 테스트 포팅(포트 앵커-엣지 끝점 매칭 불변식) 및 통과
- layered Issue603 테스트 포팅(compound 라벨 top-band 분리 불변식) 및 통과
- layered Issue665 테스트 포팅(계층 그래프 import/layout smoke) 및 통과
- layered Issue405 테스트 포팅(방향별 포트/포트라벨 위치 multiset 일관성) 및 통과
- layered Issue405_2 테스트 포팅(포트 side 축 기준 위치 일관성) 및 통과
- layered Issue700 테스트 포팅(엣지 섹션 좌표 비영점 라우팅 회귀) 및 통과
- layered Issue870 테스트 포팅(NetworkSimplex node placer smoke) 및 통과
- layered Issue905 테스트 포팅(초기 라벨 위치 변화에 대한 edge-label x 안정성) 및 통과
- layered Issue701 테스트 포팅(port label placement inside/outside/fixed smoke) 및 통과
- `cargo test -p org-eclipse-elk-alg-layered` 전체 통과 재확인
- `cargo clippy -p org-eclipse-elk-alg-layered --tests` 재실행(경고만 존재, 에러 없음)
- layered 남은 Java issues 테스트 19개 포팅 완료(143And318/316/433/444/445/455/463/471/476/502/515/541/546/548/552/587/596/628/726), 공통 헬퍼 `issue_support.rs` 확장
- 신규 issue 테스트 포함 `cargo test -p org-eclipse-elk-alg-layered` 전체 통과 및 `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- layered 테스트용 최소 ELKT 로더 추가(`elkt_test_loader.rs`): `algorithm/spacing.nodeNode/direction/edgeRouting`, `node`, `edge`, `layout[size]` subset 파싱 지원
- ELKT 로더 검증 테스트 추가(`elkt_loader_test.rs`): external 예제 `.elkt` 로딩/레이아웃/노드 크기 파싱 확인
- issue 리소스 기반 전환 시작: `issue_444_test.rs`, `issue_463_test.rs`, `issue_548_test.rs`를 `tests/resources/issues/*.elkt` 입력 기반으로 변경
- clippy 경고 일부 정리(`get(0)` -> `first()`, `len()>0` -> `!is_empty()`, `bend_points iter_cloned_collect` -> `to_vec()`)
- 변경 후 `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- ELKT 테스트 로더 확장: `port`, `edge <id>:`, `edgeLabel`, `nodeLabel`, `nodeProperty`/`portProperty`, `padding`, 노드 `parent`/`in` 지정, 포트 side/portConstraints 파싱 지원
- ELKT 로더 조회 헬퍼 추가: `find_edge_by_identifier`, `find_port_by_identifier`, `find_node_by_identifier`를 BFS 기반으로 보강(중첩 노드 포함)
- issue 리소스 기반 전환 확대: `issue_445_test.rs`, `issue_541_test.rs`, `issue_552_test.rs`를 `tests/resources/issues/*.elkt` 기반으로 변경하고 대응 리소스 추가
- issue 리소스 기반 전환 추가: `issue_596_test.rs`, `issue_628_test.rs`를 중첩 노드(`parent`) 포함 ELKT 입력 기반으로 변경하고 대응 리소스 추가
- 전환 후 검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test --test issue_445_test --test issue_541_test --test issue_552_test` 통과, `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- 추가 검증: `cargo test -p org-eclipse-elk-alg-layered --test issue_596_test --test issue_628_test --test elkt_loader_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test issue_596_test --test issue_628_test` 통과(경고만 존재)
- issue 리소스 기반 전환 추가: `issue_455_test.rs`, `issue_471_test.rs`, `issue_546_test.rs`, `issue_587_test.rs`, `issue_665_test.rs`, `issue_680_test.rs`, `issue_700_test.rs`, `issue_726_test.rs`, `issue_734_test.rs`를 ELKT 입력 기반으로 변경하고 대응 리소스 추가
- ELKT 파서 확장: `portProperty`에서 `portBorderOffset`(`LayeredOptions::PORT_BORDER_OFFSET`) 파싱/적용 지원
- 전환 후 대상 검증: `cargo test -p org-eclipse-elk-alg-layered --test issue_455_test --test issue_471_test --test issue_546_test --test issue_587_test --test issue_665_test --test issue_680_test --test issue_700_test --test issue_726_test --test issue_734_test --test elkt_loader_test` 통과
- 최종 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- issue 리소스 기반 전환 추가: `issue_433_test.rs`, `issue_476_test.rs`, `issue_502_test.rs`, `issue_515_test.rs`를 ELKT 입력 기반으로 변경하고 대응 리소스 추가
- 추가 검증: `cargo test -p org-eclipse-elk-alg-layered --test issue_433_test --test issue_476_test --test issue_502_test --test issue_515_test --test elkt_loader_test` 통과
- 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- issue 리소스 기반 전환 추가: `issue_447_test.rs`를 ELKT 입력 기반으로 변경하고 대응 리소스 추가
- ELKT 파서 확장: `portProperty`에서 `portAnchor`(`LayeredOptions::PORT_ANCHOR`, `KVector`) 파싱/적용 지원
- 추가 검증: `cargo test -p org-eclipse-elk-alg-layered --test issue_447_test --test elkt_loader_test` 통과
- 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- issue 리소스 기반 전환 추가: `issue_603_test.rs`, `issue_870_test.rs`를 ELKT 입력 기반으로 변경하고 대응 리소스 추가
- ELKT 파서 확장: `nodeLabelsPadding`, `nodePlacementStrategy`(top-level), `nodeProperty`의 `nodeLabelsPlacement`/`nodeLabelsPadding`/`nodePlacementStrategy` 파싱 지원
- 추가 검증: `cargo test -p org-eclipse-elk-alg-layered --test issue_603_test --test issue_870_test --test elkt_loader_test` 통과
- 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- issue 리소스 기반 전환 추가: `issue_143_and_318_test.rs`, `issue_316_test.rs`를 ELKT 입력 기반으로 변경하고 대응 리소스 추가
- ELKT 파서 확장: `spacing.edgeEdge`(top-level + nodeProperty) 파싱/적용 지원
- 추가 검증: `cargo test -p org-eclipse-elk-alg-layered --test issue_143_and_318_test --test issue_316_test --test elkt_loader_test` 통과
- 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- issue 리소스 기반 전환 마무리: `issue_405_test.rs`, `issue_405_2_test.rs`, `issue_682_test.rs`, `issue_701_test.rs`, `issue_871_test.rs`, `issue_905_test.rs`를 ELKT 입력 기반으로 변경하고 대응 리소스 추가
- ELKT 파서 확장: `portLabel` 선언 파싱, `portLabelsPlacement`(top-level + nodeProperty) 파싱/적용 지원
- 최종 검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재)
- ELKT 파서 추가 확장: `node` block 일반 `key:value` 파싱, `edgeProperty`(`edgeLabelsPlacement/junctionPoints/sourcePoint/targetPoint/bendPoints`), `edgeSection`, `edgePoint`, edge label `placement`, node label placement shorthand/조합 파싱 지원
- ELKT 로더 보강: `find_edge_by_identifier`를 BFS 탐색으로 변경해 중첩 노드 내부 edge 식별 지원
- ELKT 고급 기능 검증 추가: `tests/resources/issues/elkt_parser_advanced_features.elkt` + `load_advanced_elkt_features_for_labels_and_edge_points` 테스트 추가 및 통과
- layered 테스트 clippy 경고 일부 정리: 테스트 파일 `dead_code` 허용 추가, `switch_decider_test`의 `&Vec`/`&mut Vec` 시그니처를 slice 기반으로 정리, 불필요 `mut` 제거
- 성능 측정 확장: `perf_layered_issue_scenarios` 바이너리 추가 및 빌드/스모크 실행 확인
- 성능 자동화 연결: `run_perf_layered_issue_scenarios.sh` 추가, `run_perf_all.sh` env 변수/실행 체인 추가, `compare_perf_results.sh`/`check_perf_regression.sh`/`summarize_perf_results.sh`/`scripts/README.md`에 layered issue 시나리오 결과(`perf/results_layered_issue_scenarios.csv`) 반영
- 성능 비교/회귀 검사 보강: `compare_perf_results.sh`/`check_perf_regression.sh`에서 `results_layered_issue_scenarios.csv`를 시나리오(`scenario` 컬럼) 단위로 window 비교/회귀 판정하도록 개선
- 파이프라인 검증: `run_perf_all.sh`를 소형 입력으로 실행해 comment/graph/recursive/layered/layered-issue 전체 체인 동작 확인
- 시나리오별 회귀 스모크 검증: 샘플 `results_layered_issue_scenarios.csv`로 `compare/check` 실행 시 시나리오별 delta/회귀 판정 출력 확인
- 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(경고만 존재), `elkt_test_loader.rs`의 `trim_split_whitespace`/`type_complexity` 경고 정리
- layered/lib clippy 정리 1차: `get(0)`/`option_map_unit_fn`/`ptr_arg`/`map_clone` 중심으로 `elk_layered`, `l_edge`, `l_node`, `l_port`, `model_order_barycenter_heuristic`, `sweep_copy`, `bk/node_placer`, `p2layers` 다수 파일 리팩터링
- layered/lib clippy 정리 2차: `get(0)` 패턴 전수 치환(`intermediate/*`, `preserveorder/*`, `layer_sweep_crossing_minimizer`, `elk_graph_layout_transferrer` 등) 및 `map_or(false, ...) -> is_some_and(...)`, `or_insert_with(HashSet::new) -> or_default()` 적용
- layered/lib clippy 정리 3차: `unwrap_or_else(Default::new/default fn)`를 `unwrap_or_default()`로 정리(`elk_graph_importer/layout_transferrer`, `graph_transformer`, `final_spline_bendpoints_calculator`, `graph_info_holder`, `layer_sweep_crossing_minimizer`, `orthogonal_routing_generator`, `network_simplex_layerer`)
- 정리 후 검증: `cargo test -p org-eclipse-elk-alg-layered` 반복 통과, `cargo clippy -p org-eclipse-elk-alg-layered --lib` 통과(경고만 존재)
- clippy 경고량 추적(라이브러리 기준): `plugins/.../src` 경고 라인 수가 최근 배치에서 112 -> 93 -> 82로 감소
- layered/lib clippy 정리 4차: 옵션 enum `Default` 수동 impl을 derive로 전환(`CycleBreakingStrategy`/`CrossingMinimizationStrategy`/`GraphCompactionStrategy`/`GreedySwitchType`/`LayeringStrategy`/`NodePlacementStrategy`/`NodePromotionStrategy`)
- layered/lib clippy 정리 5차: 알고리즘/유틸 저위험 리팩터링 일괄 적용(`spacings` flatten, `min_width_layerer` unwrap 제거, `stretch_width_layerer` sort_by_key, `crossings_counter` swap, `hyperedge_crossings_counter` append/collapse, `bk` 계열 entry/enumerate/collapse, `nub_spline` saturating_sub/for-loop, `spline_edge_router` unit-map 제거 등)
- layered/lib clippy 정리 6차: Java API 정합성을 유지한 lint 정리(`to_string` 메서드 `inherent_to_string` 허용, ref-key map 사용 파일 `mutable_key_type` 모듈 허용, `too_many_arguments`가 구조적으로 필요한 함수에 국소 허용)
- layered/lib 경고 해소: `cargo clippy -p org-eclipse-elk-alg-layered --lib` 기준 layered 크레이트 경고 0건(의존 `org-eclipse-elk-alg-common` 경고만 잔존)
- 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --lib`/`--tests` 통과
- alg.common clippy 정리 1차: `networksimplex`(`get(0)`/`collapsible_else_if`/재귀 self 제거), `compaction options` default derive 전환, `polyomino`(`needless_range_loop`/`new_without_default`/`get_first`/`manual_div_ceil`), `t_triangle`(`clone_on_copy`), `rectilinear_convex_hull`(`map_or`/boolean 단순화), `spore depth_first_compaction`(`map_or`), `nodespacing`/`polyomino_options`(`too_many_arguments` 국소 허용) 정리
- 경고 최종 상태: `cargo clippy -p org-eclipse-elk-alg-layered --lib` 및 `cargo clippy -p org-eclipse-elk-alg-layered --tests` 무경고
- 최종 재검증: `cargo test -p org-eclipse-elk-alg-layered` 전체 통과 유지
- 워크스페이스 clippy 에러 해소: `org-eclipse-elk-alg-radial`(`eades_radial.rs`의 `eq_op`)과 `org-eclipse-elk-alg-rectpacking`(`compactor.rs`의 NaN 체크 `eq_op`) 수정
- 워크스페이스 재검증: `cargo clippy --workspace --all-targets` 종료 코드 0(다른 알고리즘 크레이트 경고는 잔존)
- 워크스페이스 clippy 잔여 경고 배치 정리 완료: `topdownpacking`/`disco`/`spore`/`libavoid`/`vertiflex`/`force`/`rectpacking`/`radial`/`mrtree`/`graphviz-layouter`/`core test` 경고 제거
- 최종 정적 검증: `cargo clippy --workspace --all-targets` 무경고 확인
- 빌드 재검증: `cargo build --workspace` 통과
- `org-eclipse-elk-alg-common` `network_simplex_test`를 큰 스택 스레드에서 실행하도록 보강(`network_simplex_deltas`)해 기본 실행 스택 오버플로우 제거
- 전체 테스트 재검증: `cargo test --workspace` 전체 통과
- ELKT 파서 확장 2차: 블록 컨텍스트 스택(Node/Port/Edge) 도입, 중첩 `node` 선언과 `port` 암시 parent 해석, `layout[position/size]` 및 edge block `layout[start/end/bends]`/property 파싱 지원
- ELKT 리소스/테스트 확장: `elkt_parser_nested_blocks.elkt` 추가 및 `load_nested_blocks_and_combined_layout_properties` 테스트로 복합 표기(`[]`, `|`, 공백 포함) 회귀 검증
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- ELKT 파서 확장 3차: `edge a,b -> c,d` comma 기반 multi-source/target 하이퍼엣지 파싱 지원 및 `find_edge_by_identifier`의 다중 source/target 매칭 보강
- ELKT 파서 확장 3차: 컨텍스트 기반 `label` 선언 파싱(node/port/edge 블록 내)과 label block `layout`(position/size) 처리 지원
- ELKT 리소스/테스트 확장: `elkt_parser_hyperedge_and_labels.elkt` 추가 및 `load_hyperedge_and_nested_port_label_blocks` 테스트 추가
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- ELKT 파서 확장 4차: `section` 선언의 `incoming/outgoing` 및 `section -> section` 링크 파싱, generic `label`의 identifier(`label id: "text"`)와 중첩 label 컨텍스트 우선 parent 해석 지원
- ELKT 리소스/테스트 확장: `elkt_parser_sections_and_label_ids.elkt` 추가 및 `load_section_links_and_label_identifiers` 테스트 추가
- ELKT 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- 성능 기준선/비교 모드 확장: `compare_perf_results.sh`/`check_perf_regression.sh`에 `PERF_COMPARE_MODE=window|baseline|both`, `PERF_BASELINE_LAYERED_FILE` 지원 추가, `update_perf_baseline.sh`/`perf/baselines/` 도입
- 퍼포먼스 래퍼/CI 보강: `run_perf_and_compare.sh`/`run_perf_and_check.sh` mode 인자 연동, `.github/workflows/perf.yml`에 compare mode/baseline path/regression gate 입력 및 `perf/compare.txt`/`perf/baselines/*.csv` artifact 업로드 반영
- 성능 파이프라인 버그 수정: `perf_layered_issue_scenarios` CSV 기록 줄바꿈(`\\n` 문자열 기록) 수정, `run_perf_and_check.sh`에서 compare 단계에 `window` 인자 전달하도록 수정
- 스모크 검증: 소형 입력으로 `run_perf_all.sh`, `run_perf_and_compare.sh`, `run_perf_and_check.sh`, `compare_perf_results.sh`, `check_perf_regression.sh`, `update_perf_baseline.sh` 실행 확인(`window`/`baseline`/`both` 모드)
- 워크플로 정합성 점검: `.github/workflows/perf.yml` compare step이 `regression_window` 입력을 사용하도록 정리, `ruby` YAML 파싱으로 워크플로 문법 검증
- ELKT 파서 에러 보고 보강: section link의 미존재 target/source/edge를 에러로 반환하고, generic label identifier 중복 시 로딩 실패 처리 추가
- ELKT 에러 케이스 리소스/테스트 추가: `elkt_parser_invalid_section_link.elkt`, `elkt_parser_duplicate_label_identifier.elkt`, `fail_when_section_link_target_is_missing`, `fail_on_duplicate_label_identifier` 추가
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test` 통과
- 전체 재검증: `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- 기준선 정책 문서화: `perf/baselines/POLICY.md` 추가 및 `perf/README.md`, `scripts/README.md`에 참조 연결
- ELKT section 체인/순환 corner case 보강: `section s0 -> s1,s2 -> s3` 체인 링크 파싱(중간 링크 자동 생성), 동일 source의 다중 링크 누적 병합, 순환 링크(`s3 -> s0`) 처리 보강
- ELKT section shape strict 참조 보강: `incoming/outgoing` shape에서 미존재 node/port 참조 시 자동 생성 대신 로딩 에러 반환
- ELKT section alias 커버리지 확대: `incomingShape/outgoingShape`, `source/sourcePoint/start`, `target/targetPoint/end`, `bend/bendPoint/bendPoints/bends` 파싱 지원
- ELKT 파서 안정화: `parse_named_value`가 키 부분 문자열 오탐(`end:`가 `bend:` 내부에 매칭)하지 않도록 경계 인식 검색으로 보강
- ELKT 리소스/테스트 추가: `elkt_parser_section_chain_and_aliases.elkt`, `elkt_parser_invalid_section_shape.elkt`, `load_section_chain_and_aliases`, `fail_when_section_shape_reference_is_missing` 추가
- Java 성능 비교 자동화 추가: `compare_java_perf_results.sh`, `check_java_perf_parity.sh`, `run_perf_and_compare_java.sh` 추가 및 `perf/README.md`, `scripts/README.md` 문서화
- perf CI Java 비교 연동: `.github/workflows/perf.yml`에 `java_compare_enabled/java_results_file/java_parity_threshold/java_parity_gate` 입력과 compare/parity step(옵션) 추가, `perf/java_vs_rust.md` artifact 업로드
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test`, Java compare 스크립트 스모크(`compare/check/run_perf_and_compare_java`) 통과
- Java perf 실행 스크립트 추가: `run_java_perf_layered_issue_scenarios.sh` 작성(prepare + test 2단계, dry-run/모듈/로컬repo 오버라이드 지원)
- Java compare 파이프라인 확장: `run_perf_and_compare_java.sh`에 `JAVA_PERF_GENERATE/JAVA_PERF_*` 옵션을 추가해 Java CSV 자동 생성 후 compare/parity 실행 가능하도록 연결
- perf CI 입력 확장: `.github/workflows/perf.yml`에 `java_generate_enabled`, `java_generate_dry_run` 입력과 Java CSV 생성 step(옵션) 추가
- 단계 검증: `sh -n scripts/run_java_perf_layered_issue_scenarios.sh scripts/run_perf_and_compare_java.sh`, `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/perf.yml")'` 통과
- Java 실행 스모크 보강: Tycho runtime JVM에 `elk.perf.*`가 전달되도록 `-Dtycho.testArgLine` 전달 방식으로 수정, `JAVA_PERF_BUILD_PLUGINS=false` 경로에서 실제 CSV(`/tmp/java_layered_perf_smoke.csv`) 생성 확인
- Java end-to-end 스모크: `JAVA_PERF_GENERATE=true`로 `run_perf_and_compare_java.sh` 실행하여 Java CSV 생성 + Rust 비교 + parity check 체인 완료(`/tmp/java_vs_rust_smoke2.md` 생성)
- ELKT 파서 에러 컨텍스트 보강: 로딩 중 발생하는 핵심 에러(중복 edge id, section 블록/shape/link/label 식별자 충돌)에 `line N` + 원문 스니펫을 포함하도록 개선
- ELKT alias 보강: edge block `layout`과 `edgeSection`에서 `start/source/sourcePoint`, `end/target/targetPoint`, `bends/bend/bendPoint/bendPoints`를 공통 파싱하도록 확장
- ELKT property alias 보강: `normalize_key`에 `.` 제거를 추가해 `spacing.nodeNode`/`spacingNodeNode`를 동일 키로 인식하고, `nodeProperty`에도 동일 규칙 적용
- ELKT 리소스/테스트 추가: `elkt_parser_edge_aliases.elkt`, `load_edge_layout_and_edge_section_aliases` 추가 및 에러 테스트에 line-context 검증(duplicate label/invalid section link/invalid section shape) 보강
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- 전체 재검증: `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과
- ELKT 파서 strict 에러 처리 확대: `portProperty/edgeProperty`의 unknown entity, `edgeSection/edgePoint/edgeLabel/nodeLabel/portLabel`의 unknown reference, 최상위 `label` 선언 오용을 line-context 에러로 반환
- ELKT 파서 malformed 선언 처리 확대: `node/port/edge/section/edgeSection/edgePoint/*Label/label` 구문 실패 시 조용히 무시하지 않고 명시적 파싱 에러 반환
- ELKT qualified option id alias 지원: `normalize_property_key`/`property_key_matches` 도입으로 `org.eclipse.elk.*` 및 `org.eclipse.elk.layered.*` 접두 옵션 id를 축약 키와 동일하게 해석
- ELKT 리소스/테스트 추가: `elkt_parser_qualified_option_ids.elkt`, `elkt_parser_unknown_edge_reference.elkt`, `elkt_parser_label_without_parent.elkt` 및 대응 테스트(`load_qualified_option_ids_and_apply_properties`, `fail_on_unknown_edge_reference_in_edge_section`, `fail_on_label_declaration_without_parent_context`) 추가
- ELKT inline alias 보강: `parse_inline_field_value`/`parse_inline_value`에 key alias 매칭을 추가해 inline full option id(`org.eclipse.elk.layered.edgeLabels.placement:tail` 등) 파싱 지원
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests` 통과
- perf CI 기본 비교 모드 정책 반영: `.github/workflows/perf.yml`의 `perf_compare_mode` 기본값을 `window`로 조정(수동 baseline 비교는 입력 override로 수행)
- 워크플로 검증: `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/perf.yml")'` 통과
- ELKT syntax 변형 보강: `layout (...)` 괄호 표기, `bendPoints`의 `;` 구분자, section 선언의 후행 링크(`section s1 (...) -> s0`) 파싱 지원
- 메타데이터 패리티 드리프트 수정: `topdown_layout_test`의 테스트용 `LayeredAlgorithmProvider`가 `org.eclipse.elk.layered`를 등록할 때 Java와 동일한 `category/defining_bundle_id/preview_image_path`를 명시하도록 보강
- 패리티 재검증: `sh scripts/check_algorithm_metadata_parity.sh` 재실행 결과 `status: ok`(mismatch 0) 확인
- 단계 검증: `cargo test -p org-eclipse-elk-alg-topdownpacking`, `cargo clippy -p org-eclipse-elk-alg-topdownpacking --all-targets -- -D warnings` 통과
- layered 클래스명 매핑 잔여 1건 포팅: Java `RectlinearConvexHullTest` 대응 `plugins/org.eclipse.elk.alg.layered/tests/rectlinear_convex_hull_test.rs` 추가(2개 테스트)
- 이름 매핑 재검증: layered Java `*Test.java` -> Rust `*_test.rs` 변환 기준 `missing_by_name_map=0` 확인
- 단계 검증: `cargo test -p org-eclipse-elk-alg-layered --test rectlinear_convex_hull_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test rectlinear_convex_hull_test -- -D warnings` 통과
- 알고리즘 메타 리포트 추가 재검증: `sh scripts/check_algorithm_name_parity.sh`, `sh scripts/check_algorithm_description_parity.sh`, `sh scripts/check_algorithm_feature_parity.sh` 모두 `status: ok`
- 배포 직전 전체 검증: `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings` 통과
- ELKT section 링크 오류 타입 세분화: 빈 링크 타깃(`section s0 -> [...]`)을 별도 에러(`section link has empty target section list`)로 처리, unknown target 에러 메시지에 source section 정보 추가
- ELKT 파서 유틸 보강: `parse_numbers`/`find_key_marker`/inline token trim 확장(`(`, `)`, `;` 포함), section 레이아웃 본문 추출을 `[]`/`()` 모두 지원
- ELKT 리소스/테스트 추가: `elkt_parser_layout_variants.elkt`, `elkt_parser_invalid_section_link_empty_target.elkt`, 대응 테스트(`load_layout_parentheses_and_trailing_section_links`, `fail_when_section_link_target_is_empty`) 추가
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- Java perf 운영 가이드 확정: `scripts/README.md`, `perf/README.md`에 CI `JAVA_PERF_MVN_LOCAL_REPO` 분리 정책(run-attempt 단위 경로, job 내 경로 고정, 동시 job 경로 공유 금지, 재실행 시 `JAVA_PERF_BUILD_PLUGINS=false` 권장) 명시
- ELKT key/value 변형 보강: `:` 외 `=` 구분자와 `key = value` 공백 변형을 `parse_property_line`/`parse_key_value`/`parse_entity_property`/`parse_named_value` 경로에서 공통 지원
- ELKT inline/layout 파싱 보강: inline token의 `=` 처리(`placement=head`, `side=east` 등), `position/size` 및 `text/size` 파싱의 `=` 지원, section 링크 파서의 `=` 토큰 경계 처리 보강
- ELKT 리소스/테스트 추가: `elkt_parser_key_value_variants.elkt`, 대응 테스트(`load_colon_and_equals_key_value_variants`) 추가
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- ELKT quoted/escape 정합성 보강: quoted section/link id 및 label id 파싱(`section "s 0"`, `label "l parent": ...`), escaped quote 텍스트(`\"`) 디코딩, key marker/kv split의 quote-aware 처리 추가
- ELKT 식별자 파싱 보강: `split_first_token`/`parse_identifier_list`/`parse_identifier_value`를 quote-aware로 개선해 section/edge/label 참조에서 quoted 토큰을 안정적으로 해석
- ELKT 리소스/테스트 추가: `elkt_parser_quoted_identifiers.elkt`, 대응 테스트(`load_quoted_label_and_section_identifiers_with_escaped_text`) 추가
- Java compare artifact 검증 자동화: `scripts/check_java_perf_artifacts.sh` 추가, `run_perf_and_compare_java.sh`에 산출물 검증 훅(`JAVA_PERF_VERIFY_ARTIFACTS`) 연결
- Java dry-run 리허설 자동화: `run_perf_and_compare_java.sh`와 `.github/workflows/perf.yml`에서 `java_generate_dry_run=true` 시 compare/parity를 건너뛰고 dry-run 리포트 생성 + artifact 검증 수행
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `sh -n scripts/run_perf_and_compare_java.sh scripts/check_java_perf_artifacts.sh`, `ruby -e 'require \"yaml\"; YAML.load_file(\".github/workflows/perf.yml\")'`, `JAVA_PERF_GENERATE=true JAVA_PERF_DRY_RUN=true sh scripts/run_perf_and_compare_java.sh ...` 스모크 통과
- ELKT quoted declaration 확장: `node`/`port` 선언 토크나이저를 quote-aware로 개선해 공백/콤마 포함 식별자(`\"node a\"`, `\"port, out\"`) 파싱 지원
- ELKT edge declaration 확장: `edge`의 quoted id/source/target 및 quoted CSV token(`\"port, out\"`) 파싱 지원(`split_once_outside_quotes`, `split_csv_outside_quotes` 재사용)
- ELKT 리소스/테스트 추가: `elkt_parser_quoted_node_port_edge.elkt`, 대응 테스트(`load_quoted_node_port_edge_identifiers`) 추가
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- Java artifact 품질 게이트 확장: `scripts/check_java_perf_artifacts.sh`에 `JAVA_ARTIFACT_MIN_ROWS`(데이터 행 기준)와 `JAVA_ARTIFACT_REQUIRED_SCENARIOS` 검사 추가, `run_perf_and_compare_java.sh`/`.github/workflows/perf.yml`에서 env 연동
- Java compare/parity 안정화: `compare_java_perf_results.sh`/`check_java_perf_parity.sh`/`check_java_perf_artifacts.sh`가 optional CSV header를 자동 스킵하고 숫자 컬럼 유효성 검증 후 집계하도록 보강
- 문서/스모크 검증 갱신: `scripts/README.md`/`perf/README.md`에 데이터 행 기준 게이트 정책 반영, `sh -n`/YAML parse/헤더 포함 샘플 CSV compare+parity+artifact pass/fail 스모크 통과
- ELKT `parent/of` 참조 파싱 보강: `node`/`port` 선언에서 `parent=`/`in:`/`of=` key-value 표기와 quoted identifier를 동일하게 해석하도록 `parse_inline_field_value` 경로를 우선 적용
- ELKT quoted property 참조 커버리지 확대: `nodeProperty`/`portProperty`/`edgeProperty`에서 quoted entity id를 사용하는 조합 케이스 리소스(`elkt_parser_quoted_property_parent_refs.elkt`)와 테스트(`load_quoted_property_and_parent_of_references`) 추가
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- ELKT section/edge reference 커버리지 확대: quoted id가 `section` 링크 및 `outgoing/incoming/outgoingShape/incomingShape`, `edgeSection`(`sourcePoint/targetPoint/bendPoints`), `edgePoint`(`source/target/bend`) alias와 함께 사용되는 리소스(`elkt_parser_quoted_section_edge_refs.elkt`) 및 테스트(`load_quoted_section_and_edge_point_references_with_aliases`) 추가
- 단계 재검증: `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- Java artifact row-floor 정책 보강: `check_java_perf_artifacts.sh`가 `required_scenario_count`를 계산해 최소 데이터 행 기준을 `max(JAVA_ARTIFACT_MIN_ROWS, required_scenario_count)`로 적용하고, 설정값 검증(정수 여부) 및 에러 메시지(`configured_min`, `required_scenarios`)를 강화
- 문서/워크플로 정합성 반영: `scripts/README.md`, `perf/README.md`, `.github/workflows/perf.yml`에 row-floor 정책 설명을 동기화
- 정책 스모크 검증: `JAVA_ARTIFACT_MIN_ROWS=1`이어도 required scenario 5개일 때 3행 CSV는 실패, 5행 CSV는 통과하는 케이스 확인
- Java 실측 실행 안정화: `run_perf_and_compare_java.sh`에서 `JAVA_PERF_GENERATE=true` & non-dry-run & `JAVA_PERF_MVN_LOCAL_REPO` 미지정 시 per-run temp m2 경로를 자동 할당해 Tycho lock 충돌 완화
- Java 실행 실패 안내 보강: `run_java_perf_layered_issue_scenarios.sh`에 Maven/Tycho 실패 시 lock 회피(`JAVA_PERF_MVN_LOCAL_REPO`) 및 dry-run/network prewarm 힌트 메시지 추가
- 단계 검증/관찰: 로컬 non-dry-run 시 기존 `.m2` lock timeout은 사라졌고, 현재 환경에서는 Tycho plugin resolution(원격 repo 접근) 실패로 실측 CSV 생성이 차단됨; dry-run 경로는 계속 통과
- Java baseline 운영 보강: `scripts/update_java_perf_baseline.sh` 추가(기본 `perf/java_results_layered_issue_scenarios.csv` -> `perf/baselines/java_layered_issue_scenarios.csv`) 및 `scripts/README.md`/`perf/README.md`/`perf/baselines/POLICY.md` 절차 문서화
- 단계 재검증: `sh -n scripts/update_java_perf_baseline.sh`, 샘플 CSV 기준 baseline copy 스모크(`JAVA_BASELINE_UPDATE_OK`) 통과
- Java baseline 연동 회귀 판정 확장: `run_perf_and_compare_java.sh`에 `JAVA_PERF_COMPARE_MODE=results|baseline|both` 추가, baseline 입력(`JAVA_BASELINE_FILE`/`JAVA_BASELINE_OUTPUT`/`JAVA_BASELINE_THRESHOLD`) 지원 및 baseline artifact 검증 경로 연결
- perf CI Java 파이프라인 통합: `.github/workflows/perf.yml`의 Java 관련 다중 step을 `run_perf_and_compare_java.sh` 단일 호출로 통합하고, 입력(`java_compare_mode`, `java_baseline_file`, `java_baseline_parity_threshold`, `java_baseline_parity_gate`)을 wrapper env에 매핑
- Java parity 게이트 분리: `run_perf_and_compare_java.sh`에 `JAVA_RESULTS_PARITY_GATE`/`JAVA_BASELINE_PARITY_GATE` 추가, compare와 gate 실행을 독립 제어하도록 보강
- Rust perf 재실행 제어: `run_perf_and_compare_java.sh`에 `LAYERED_ISSUE_SKIP_RUST_RUN` 추가(CI에서 `run_perf_all.sh` 결과 재사용)
- 문서 동기화: `scripts/README.md`, `perf/README.md`, `perf/baselines/POLICY.md`에 단일 Java 파이프라인/모드/게이트/skip-rust 운영 가이드 반영
- 단계 스모크 검증: `run_perf_and_compare_java.sh`의 `results`, `baseline`, `both + dry-run` 모드 및 artifact 검증 경로 모두 통과(`JAVA_PIPELINE_MODES_OK`)
- Java 생성 실패 완화 옵션 추가: `run_perf_and_compare_java.sh`에 `JAVA_PERF_ALLOW_GENERATE_FAILURE` 도입(생성 실패 허용 시 results compare를 skip report로 대체하고 `both` 모드 baseline compare는 계속 수행)
- perf CI 입력 확장: `.github/workflows/perf.yml`에 `java_allow_generate_failure` 추가 및 wrapper env(`JAVA_PERF_ALLOW_GENERATE_FAILURE`) 매핑
- 실패 정책 스모크 검증: 강제 생성 실패 조건에서 `JAVA_PERF_ALLOW_GENERATE_FAILURE=true`면 fallback 지속(`JAVA_ALLOW_FAILURE_FALLBACK_OK`), `false`면 즉시 실패(`JAVA_GENERATE_FAIL_STRICT_OK`) 확인
- baseline 모드 생성 자동 skip 보강: `run_perf_and_compare_java.sh`에서 `JAVA_PERF_COMPARE_MODE=baseline`일 때 `JAVA_PERF_GENERATE=true` 입력이 들어와도 생성 단계를 자동 건너뛰고 baseline compare만 수행
- 단계 스모크 검증: baseline 모드 + `JAVA_PERF_GENERATE=true` + invalid mvn bin 조건에서도 baseline 리포트/artifact 검증이 정상 완료됨(`BASELINE_GENERATE_AUTO_SKIP_OK`)
- Java generation 재시도 보강: `run_java_perf_layered_issue_scenarios.sh`에 `JAVA_PERF_RETRIES`/`JAVA_PERF_RETRY_DELAY_SECS` 추가, Maven 바이너리 사전검증(경로/실행 가능 여부) 및 retry 로그 출력 지원
- perf CI 입력 확장: `.github/workflows/perf.yml`에 `java_generate_retries`, `java_generate_retry_delay_secs` 추가하고 wrapper env로 전달
- 재시도 스모크 검증: 가짜 Maven 바이너리로 `retry=0` 즉시 실패(`JAVA_RETRY_STRICT_FAIL_OK`), `retry=1` 1회 복구 성공(`JAVA_RETRY_RECOVERY_OK`) 확인
- 실측 재시도 확인: 실제 Maven/Tycho 경로에서 `JAVA_PERF_RETRIES=1`로 재시도 수행 시 동일 plugin descriptor 해석 실패가 2회 연속 재현됨; `JAVA_PERF_ALLOW_GENERATE_FAILURE=true` 경로로 wrapper가 skip report(`/tmp/java_real_try.md`)를 생성하고 파이프라인 지속 확인
- Java 상태 요약 자동화: `scripts/summarize_java_perf_status.sh` 추가(결과/기준선 리포트, CSV 존재 여부, skip 사유, baseline 업데이트 다음 액션 요약) 및 `perf.yml`에 `perf/java_perf_status.md` 생성 step/artifact 업로드 추가
- 상태 요약 스모크 검증: 결과 CSV 미존재/존재 케이스 각각에서 next action 메시지(`re-run generation` vs `update baseline`)가 올바르게 출력됨(`JAVA_STATUS_SUMMARY_OK`)
- Java baseline 후보 export 자동화: `scripts/export_java_baseline_candidate.sh` 추가(소스 CSV 존재 시 baseline 후보 복사 + 상태 리포트, 미존재 시 skip 리포트), `perf.yml` 입력 `java_export_baseline_candidate`와 `perf/java_baseline_candidate_status.md` artifact 업로드 연동
- baseline 후보 export 스모크 검증: source 미존재 skip/존재 updated 두 케이스 모두 상태 리포트 출력과 target CSV 생성 확인(`JAVA_BASELINE_CANDIDATE_EXPORT_OK`)
- baseline 후보 안전성 보강: export 기본 target을 `perf/baselines/java_layered_issue_scenarios.candidate.csv`로 분리하고, `JAVA_CANDIDATE_MIN_ROWS`/`JAVA_CANDIDATE_REQUIRED_SCENARIOS` 정책 검증을 통과한 경우만 candidate 갱신하도록 강화(`JAVA_CANDIDATE_STRICT`로 fail/skip 정책 제어)
- Java 상태 요약 확장: `summarize_java_perf_status.sh`가 candidate CSV/리포트 상태를 함께 반영하고, candidate 존재 시 baseline 승격 명령(`update_java_perf_baseline.sh`)을 다음 액션으로 안내
- workflow 순서 정렬: `perf.yml`에서 candidate export를 status summary보다 먼저 실행해 같은 런의 candidate 상태가 요약 리포트에 반영되도록 조정
- candidate strict 스모크 검증: strict=true + 정책 불일치 케이스에서 export가 비정상 종료(`JAVA_CANDIDATE_STRICT_FAIL_OK`), 정상 candidate 생성 후 summary가 promote 액션 안내(`JAVA_CANDIDATE_SUMMARY_OK`) 확인
- candidate readiness 게이트 연동: `scripts/check_java_baseline_candidate.sh`를 `perf.yml`에 연결해 `java_export_baseline_candidate=true` 경로에서 `perf/java_baseline_candidate_check.md`를 생성/업로드하고, `java_export_candidate_strict`로 fail/skip 정책을 공유
- Java 상태 요약 추가 확장: `summarize_java_perf_status.sh`가 `JAVA_BASELINE_CANDIDATE_CHECK_REPORT`를 읽어 candidate check status(`ready/not_ready/skipped`)를 표시하고 next action을 분기
- candidate readiness 스모크 검증: 샘플 CSV로 `ready`, `not_ready`, `strict fail` 케이스를 재현해 보고서 상태/종료코드 동작 확인
- Java 실측 재시도(최신): `JAVA_PERF_GENERATE=true`, `JAVA_PERF_RETRIES=1`, `JAVA_PERF_ALLOW_GENERATE_FAILURE=true`로 wrapper 실행 시 Tycho plugin descriptor(`org.eclipse.tycho:tycho-maven-plugin:4.0.12`) 해석 실패가 재현됨(2회 시도 후 실패)
- 운영 리포트 생성(최신): 실측 실패 후에도 `perf/java_vs_rust.md`(skip 사유), `perf/java_baseline_candidate_status.md`(skipped), `perf/java_baseline_candidate_check.md`(skipped), `perf/java_perf_status.md`(next action) 생성 경로 확인
- 후속 실행 기준 정리: 네트워크/원격 의존성 접근 가능한 환경에서 동일 wrapper 명령 재실행 후 candidate export/check를 거쳐 baseline 승격 단계로 진행
- Java 생성 preflight 보강(최신): `run_java_perf_layered_issue_scenarios.sh`에 DNS preflight 추가(`JAVA_PERF_SKIP_DNS_CHECK`, `JAVA_PERF_REQUIRED_HOSTS`), `repo.eclipse.org`/`repo.maven.apache.org` 해석 실패 시 Maven 실행 전 fail-fast하도록 개선
- 원인 확정(최신): `JAVA_PERF_MVN_LOCAL_REPO` 격리 + `-X` 로그에서 최종 원인이 DNS(`UnknownHostException: repo.eclipse.org`, `UnknownHostException: repo.maven.apache.org`)임을 확인
- Java wrapper 재검증(최신): DNS preflight 실패 상황에서 `JAVA_PERF_ALLOW_GENERATE_FAILURE=true` 경로로 `perf/java_vs_rust.md`, `perf/java_baseline_candidate_status.md`, `perf/java_baseline_candidate_check.md`, `perf/java_perf_status.md` 생성 확인
- candidate strict 게이트 재검증(최신): 부분 Java CSV(`/tmp/java_results_layered_issue_scenarios.csv`, issue_405 only)에 대해 strict check 실행 시 `missing required scenario`로 `not_ready` 종료 확인
- Java 실측/비교 파이프라인 완주(최신): 에스컬레이션 네트워크 실행으로 Java CSV 생성 성공(`external/elk/test/org.eclipse.elk.alg.layered.test/perf/java_results_layered_issue_scenarios.csv`), `results/baseline/both` compare 리포트(`perf/java_vs_rust*.md`) 생성 및 parity 통과 확인
- Java baseline 승격 완료(최신): candidate export/check(`ready`) 후 `update_java_perf_baseline.sh`로 `perf/baselines/java_layered_issue_scenarios.csv` 반영, `summarize_java_perf_status.sh`에 `candidate==baseline` 분기 추가 및 상태 리포트 갱신
- CoreOptions parity 보강(최신): `CoreOptions::BOX_PACKING_MODE` 추가, `BoxLayouterOptions::BOX_PACKING_MODE`를 CoreOptions alias로 통합, `core_options_meta`에 BOX option metadata 등록
- category id parity 보강(최신): Core category id를 Java와 동일한 fully-qualified id(`org.eclipse.elk.*`)로 수정하고 layered/force/stress/mrtree/vertiflex/libavoid provider의 `set_category_id` 및 libavoid category id를 동기화
- 테스트/클리피 재검증(최신): `cargo test -p org-eclipse-elk-core`, `cargo test -p org-eclipse-elk-alg-{force,mrtree,vertiflex,libavoid}`, `cargo clippy -p org-eclipse-elk-core -p org-eclipse-elk-alg-{force,mrtree,vertiflex,libavoid} --all-targets` 통과
- 외부 서브모듈 무수정 운영 보강(최신): `scripts/java/LayeredIssuePerfBenchTest.java` 템플릿 추가, `run_java_perf_layered_issue_scenarios.sh`에 격리 실행(`JAVA_PERF_EXTERNAL_ISOLATE=true`, worktree 우선 + copy fallback) + 벤치 소스 임시 주입/자동 정리(`JAVA_PERF_BENCH_*`)를 도입해 원본 `external/elk` 변경이 남지 않도록 정리
- perf CI 운영값 확정(최신): `.github/workflows/perf.yml` Java 입력 기본값을 strict 경로(`compare=both`, `generate=true`, parity/candidate strict gate=true)로 고정하고 Java step에 `JAVA_PERF_EXTERNAL_ISOLATE=true`/`JAVA_PERF_EXTERNAL_ELK_ROOT`/`JAVA_PERF_EXTERNAL_WORKTREE_ROOT`를 명시
- Java 실패 대응 문서화(최신): `perf/JAVA_PERF_TRIAGE.md` 추가(생성 실패, results/baseline parity 실패, candidate readiness 실패별 triage와 baseline 승격 규칙)
- RecursiveGraphLayoutEngine 연계 보강(최신): `execute_algorithm`에서 `TestController` 설치를 Java와 동일하게 `targets()` 조건부로 제한, `recursive_graph_layout_engine_integration_test`에 non-target skip/target install 시도/topdown+includeChildren 충돌 회귀 케이스 추가, `cargo test -p org-eclipse-elk-core`, `cargo clippy -p org-eclipse-elk-core --all-targets` 통과
- algorithm ID parity 자동화/해소(최신): `scripts/check_algorithm_id_parity.sh` 추가, `libavoid` ALGORITHM_ID를 Java와 동일한 `org.eclipse.elk.alg.libavoid`로 동기화, 신규 `org.eclipse.elk.conn.gmf` 크레이트(최소 Draw2D 메타데이터/옵션/provider fallback) 추가로 `org.eclipse.elk.conn.gmf.layouter.Draw2D` 포팅, 현재 `perf/algorithm_id_parity.md` status=ok(21/21)
- algorithm category parity 자동화(최신): `scripts/check_algorithm_category_parity.sh` 추가, `perf/algorithm_category_parity.md` status=ok(6/6) 확인
- algorithm option support parity 최종 정합(최신): `layered_meta_data_provider`에 Java 기준 option support 152개를 Rust `add_option_support`로 반영하고(`property_default_any` 기반), `scripts/check_algorithm_option_support_parity.sh`를 provider/core 중복 계수 제외 로직으로 보강해 layered 포함 전 알고리즘 정량 일치 달성, 현재 `perf/algorithm_option_support_parity.md` status=ok(21/21, ignored 0)
- algorithm feature parity 자동화/해소(최신): `scripts/check_algorithm_feature_parity.sh` 추가(Java `supportedFeatures` vs Rust `add_supported_feature`), Graphviz 알고리즘(dot/neato/fdp/twopi/circo) 지원 feature를 Java와 동일하게 반영해 `perf/algorithm_feature_parity.md` status=ok(10 algorithms, 31 pairs, drift 0) 달성
- Core option dependency parity 자동화/해소(최신): `scripts/check_core_option_dependency_parity.sh` 추가, `CoreOptions` dependency(Java `addDependency` vs Rust `add_dependency`)를 source-target-value 기준으로 정량 비교해 `perf/core_option_dependency_parity.md` 생성, `CoreOptions::TOPDOWN_NODE_TYPE -> NODE_SIZE_FIXED_GRAPH_SIZE` required value를 Java와 동일하게 `None`으로 수정해 status=ok(10/10)
- perf CI parity 리포트 연동(최신): `.github/workflows/perf.yml`에 `check_algorithm_id_parity.sh`/`check_algorithm_category_parity.sh`/`check_algorithm_option_support_parity.sh`/`check_algorithm_feature_parity.sh`/`check_core_option_dependency_parity.sh` step과 parity report artifact(`perf/algorithm_id_parity.md`, `perf/algorithm_category_parity.md`, `perf/algorithm_option_support_parity.md`, `perf/algorithm_feature_parity.md`, `perf/core_option_dependency_parity.md`) 업로드를 추가하고, `scripts/README.md`/`perf/README.md` 운영 가이드를 동기화, `ruby` YAML 검증 통과
- parity 재검증(최신): `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests`, `cargo test -p org-eclipse-elk-alg-graphviz-layouter`, `cargo clippy -p org-eclipse-elk-alg-graphviz-layouter --all-targets` 통과 확인
- RecursiveGraphLayoutEngine E2E 회귀 확장(최신): `recursive_graph_layout_engine_integration_test`에 계층 상속 기본값(`hierarchy_handling_inherit_defaults_to_separate_children`), includeChildren 깊은 알고리즘 전환(`include_children_nested_algorithm_switch_forces_separate_children`), TestController 하위 타깃 전파(`target_test_controller_attempts_install_on_target_descendant`) 케이스를 추가하고 `cargo test -p org-eclipse-elk-core --test recursive_graph_layout_engine_integration_test`, `cargo test -p org-eclipse-elk-core`, `cargo clippy -p org-eclipse-elk-core --all-targets` 통과 확인
- algorithm metadata parity 자동화/해소(최신): `scripts/check_algorithm_metadata_parity.sh` 추가(Java `*.Options.java`의 `category/melkBundleName/definingBundleId/imagePath` vs Rust `LayoutAlgorithmData` 등록값 비교), provider/core metadata에 `set_defining_bundle_id` 및 `set_bundle_name` 보강(force/stress/graphviz/libavoid/disco/topdownpacking/mrtree/radial/rectpacking/spore/vertiflex/core algorithms) 후 `perf/algorithm_metadata_parity.md` status=ok(21/21, mismatch 0) 달성
- perf CI parity 리포트 확장(최신): `.github/workflows/perf.yml`에 `check_algorithm_metadata_parity.sh` step 및 artifact(`perf/algorithm_metadata_parity.md`) 업로드 추가, `scripts/README.md`/`perf/README.md` 운영 문서 동기화
- 범위 재검증(최신): `cargo test -p org-eclipse-elk-core -p org-eclipse-elk-alg-{force,graphviz-layouter,libavoid,disco,topdownpacking,mrtree,radial,rectpacking,spore,vertiflex}` 및 `cargo clippy -p org-eclipse-elk-core -p org-eclipse-elk-alg-{force,graphviz-layouter,libavoid,disco,topdownpacking,mrtree,radial,rectpacking,spore,vertiflex} --all-targets` 통과, parity 리포트(`id/category/option-support/feature/core-dependency/metadata`) 전부 status=ok 확인
- 비-layered recursive 시나리오 성능 자동화 확장(최신): `run_perf_recursive_layout_scenarios.sh`/`update_perf_recursive_scenarios_baseline.sh` 추가, `run_perf_all.sh`/`compare_perf_results.sh`/`check_perf_regression.sh`/`summarize_perf_results.sh`/`.github/workflows/perf.yml`에 `results_recursive_layout_scenarios.csv` 경로 및 baseline env(`PERF_BASELINE_RECURSIVE_SCENARIOS_FILE`) 연동
- Java 시나리오별 parity gate 도입(최신): `check_java_perf_parity_scenarios.sh`와 `perf/java_parity_thresholds.csv` 추가, `run_perf_and_compare_java.sh`/`check_java_baseline_candidate.sh`/`.github/workflows/perf.yml`에 `JAVA_PARITY_SCENARIO_THRESHOLDS_FILE` 연동 및 threshold 파일 미존재 시 global threshold fallback 경로 보강
- 파이프라인 스모크 재검증(최신): recursive scenario baseline compare/regression(`PERF_COMPARE_MODE=baseline`)과 Java scenario parity(`check_java_perf_parity_scenarios.sh`)를 샘플 CSV로 실행해 통과 확인
- baseline 재생성/누락 해소(최신): `run_perf_layered_issue_scenarios.sh`(5개 기본 시나리오) + `update_perf_baseline.sh`로 `perf/baselines/layered_issue_scenarios.csv` 갱신, `update_perf_recursive_scenarios_baseline.sh`로 `perf/baselines/recursive_layout_scenarios.csv` 갱신 후 baseline compare에서 scenario missing 해소 확인
- 비-layered recursive 기본 시나리오 확장(최신): `run_perf_recursive_layout_scenarios.sh`/`run_perf_all.sh`/`.github/workflows/perf.yml` 기본 `recursive_scenarios`를 `fixed_dense,fixed_sparse,random_dense,random_sparse,box_sparse,fixed_validated,random_validated,box_validated`로 확대하고(`box_large`는 opt-in), `scripts/README.md`/`perf/README.md` 문서 기본값을 동기화
- recursive baseline 동기화(최신): 확장된 8개 기본 시나리오를 `perf/results_recursive_layout_scenarios.csv`에 누적 수집 후 `update_perf_recursive_scenarios_baseline.sh`로 `perf/baselines/recursive_layout_scenarios.csv`를 재생성, `PERF_COMPARE_MODE=baseline`의 `compare_perf_results.sh`/`check_perf_regression.sh`에서 시나리오별 `ok` 확인
- algorithm option default parity 잔여 누락 해소(최신): `scripts/check_algorithm_option_default_parity.sh`의 Rust option symbol 파서를 멀티라인 `pub static` 선언/중첩 클로저 세미콜론까지 추적하도록 보강하고(`ALGORITHM_OPTION_DEFAULT_PARITY_KEEP_TMP=true` 디버그 옵션 추가), `perf/algorithm_option_default_parity.md`에서 `missing option pairs=0`, `extra option pairs=0`, `explicit mismatches=0`, `java unknown modes=0` 달성
- algorithm name parity 자동화(최신): `scripts/check_algorithm_name_parity.sh` 추가(Java/Rust 알고리즘 메타데이터 `name` 문자열 비교), `perf/algorithm_name_parity.md` status=ok(21/21, mismatch 0) 확인
- perf CI parity 리포트 확장(최신): `.github/workflows/perf.yml`에 `check_algorithm_option_default_parity.sh` step과 artifact(`perf/algorithm_option_default_parity.md`) 업로드 추가, `scripts/README.md`/`perf/README.md` 문서 동기화 및 YAML 파싱 검증 통과
- Java parity threshold 후보 자동화(최신): `scripts/suggest_java_parity_thresholds.sh` 추가(최근 window 기준 Rust/Java 비교로 `perf/java_parity_thresholds.suggested.csv` 생성), `.github/workflows/perf.yml`에 suggestion step 추가 및 문서 동기화
- algorithm description parity 자동화/해소(최신): `scripts/check_algorithm_description_parity.sh` 추가(Java/Rust 알고리즘 메타데이터 `description` 문자열 비교), 문자열 내부 괄호가 포함된 설명에서도 안정적으로 파싱되도록 스크립트 캡처/정규식 로직 보강 후 `perf/algorithm_description_parity.md` status=ok(21/21, mismatch 0) 달성
- Java parity threshold 적용 자동화(최신): `scripts/apply_java_parity_thresholds.sh` 추가(후보 CSV 헤더/필수 scenario/와일드카드 검증 후 운영 CSV 반영), `perf/java_parity_thresholds.suggested.csv`를 `perf/java_parity_thresholds.csv`에 적용해 `issue_405/603/680/871/905/*` 임계치를 `2.00`으로 확정하고 scenario parity gate 재검증 통과
- perf CI parity 리포트 확장(최신): `.github/workflows/perf.yml`에 `check_algorithm_description_parity.sh` step과 artifact(`perf/algorithm_description_parity.md`) 업로드 추가, `scripts/README.md`/`perf/README.md` 운영 문서 동기화
- algorithm option default value parity 고도화(최신): `scripts/check_algorithm_option_default_value_parity.sh`를 enum 상수 조기판별/생성자 객체값(`ElkPadding`/`ElkMargin`/`KVector`/`KVectorChain`) 정규화/list(`CollectionLiterals`/`vec!`) 파싱/숫자 canonicalization으로 보강해 `perf/algorithm_option_default_value_parity.md`를 `status=ok`, `comparable mismatches=0`, `uncomparable=0`으로 정리하고 strict 모드(`ALGORITHM_OPTION_DEFAULT_VALUE_PARITY_STRICT=true`) 통과
- 단계 재검증(최신): `cargo test -p org-eclipse-elk-core`, `cargo clippy -p org-eclipse-elk-core --all-targets` 통과
- 비-layered recursive 시나리오 프로파일 튜닝(최신): `run_perf_recursive_layout_scenarios.sh`에 `PERF_RECURSIVE_SCENARIO_PROFILE=quick|default|full` 자동 선택(`scenarios` 인자 비어 있을 때 적용)을 추가하고, `run_perf_all.sh`/`.github/workflows/perf.yml`에 `recursive_scenario_profile` 입력 연동(`recursive_scenarios` 기본값 공백) 및 `scripts/README.md`/`perf/README.md` 운영 문서 동기화
- 단계 스모크 검증(최신): `PERF_RECURSIVE_SCENARIO_PROFILE=quick sh scripts/run_perf_recursive_layout_scenarios.sh "" 1 0 /tmp/recursive_profile_quick.csv` 실행 시 `fixed_dense`, `random_sparse`, `box_validated` 3개 시나리오가 의도대로 선택됨을 확인
- baseline 정책 고도화(최신): `perf/baselines/POLICY.md`에 recursive `quick/default/full` 프로파일 운영 기준, baseline 갱신 주기(월간 default/분기 full), PR/maintainer run의 권장 profile(`quick` vs `default/full`)을 명문화
- recursive runtime budget gate 추가(최신): `scripts/check_recursive_perf_runtime_budget.sh` 신규 도입(최신 시나리오별 `avg_iteration_ms`를 profile별 budget `quick/default/full`과 비교), `.github/workflows/perf.yml`에 `recursive_runtime_budget_gate` + budget 입력(`recursive_runtime_budget_ms_{quick,default,full}`)을 연동하고 `perf/recursive_runtime_budget.md` artifact 업로드 반영
- 단계 검증(최신): `sh scripts/check_recursive_perf_runtime_budget.sh /tmp/recursive_profile_quick.csv quick /tmp/recursive_runtime_budget_quick.md` 및 `sh scripts/check_recursive_perf_runtime_budget.sh perf/results_recursive_layout_scenarios.csv default perf/recursive_runtime_budget.md` 모두 `status=ok` 확인
- Java CI DNS preflight 운영값 노출(최신): `.github/workflows/perf.yml`에 `java_skip_dns_check`/`java_required_hosts` 입력을 추가해 `JAVA_PERF_SKIP_DNS_CHECK`/`JAVA_PERF_REQUIRED_HOSTS`로 전달하고, `scripts/README.md`/`perf/README.md` 문서에 runner 환경별 preflight 정책 제어 경로를 반영
- 최종 품질 게이트 재검증(최신): `cargo test --workspace`, `cargo clippy --workspace --all-targets` 전체 통과
- perf 파이프라인 최종 스모크(최신): 축소 파라미터로 `run_perf_all.sh` 재실행 후 `PERF_COMPARE_MODE=both sh scripts/compare_perf_results.sh 1` 결과 확인, `PERF_COMPARE_MODE=baseline sh scripts/check_perf_regression.sh 100 1` 통과, `check_recursive_perf_runtime_budget.sh` default strict 통과(`perf/recursive_runtime_budget.md`)
- Java baseline compare 최종 스모크(최신): `JAVA_PERF_COMPARE_MODE=baseline` + `LAYERED_ISSUE_SKIP_RUST_RUN=true`로 `run_perf_and_compare_java.sh` 실행해 `perf/java_vs_rust_baseline.md` 생성 및 `check_java_perf_artifacts.sh` 검증 통과, `summarize_java_perf_status.sh`에서 `candidate equals baseline: yes` 확인
- 배포 문서 보강(최신): `RELEASE_CHECKLIST.md` 신규 추가(품질/parity/성능 게이트와 Go/No-Go 규칙), `perf/README.md`와 `scripts/README.md`에 baseline 회귀 의미(기준선 대비, Java 대비와 분리) 및 배포 전 빠른 실행 절차를 명시
- 시나리오 성능 비교/회귀 로직 정합성 보강(최신): `scripts/compare_perf_results.sh`, `scripts/check_perf_regression.sh`에 scenario별 latest run config tuple(`iterations`,`warmup`) 필터를 추가해 mixed-window 오탐을 제거
- 성능 회귀 원인 분석 문서화(최신): `perf/layered_issue_regression_analysis.md` 추가, mixed-config contamination 원인과 baseline 품질 이슈/재측정 결과 및 `issue_871` 옵션 경로 A/B(ablation) 결과를 기록
- layered baseline 품질 개선(최신): `perf/baselines/layered_issue_scenarios.csv`를 정책 기준으로 재생성(`20/3`, 12회 반복 샘플, 시나리오 5종)하고 baseline gate 재검증에서 `issue_405/603/680/871/905` 모두 `ok` 확인
- 배포 게이트 재검증(최신): `cargo test --workspace`, `cargo clippy --workspace --all-targets`, `cargo build --workspace --release`, `PERF_COMPARE_MODE=baseline sh scripts/check_perf_regression.sh 5 3`, `check_recursive_perf_runtime_budget.sh`, `check_java_perf_parity*.sh` 모두 통과
- Java layered issue 테스트 parity 100% 달성(최신): `issue_316_test.rs`를 Java와 동일한 2개 테스트로 분리하고(`issue_316_ports_labels_2.elkt` 추가), `issue_701_test.rs`를 Java 메서드 단위 6개 테스트(`inside/outside/mix/fixed inside/fixed outside/fixed mix`)로 확장해 총 41개 메서드 수를 Java와 동기화
- layered issue 테스트 parity 자동화 추가(최신): `scripts/check_layered_issue_test_parity.sh` 도입(Java `@Test/@TestAfterProcessor` vs Rust `#[test]` 수량 비교), `perf/layered_issue_test_parity.md` 리포트 생성에서 `status=ok`, `java=41`, `rust=41`, drift/missing/extra 0 확인
- parity/품질 재검증(최신): `cargo test -p org-eclipse-elk-alg-layered`, `cargo clippy -p org-eclipse-elk-alg-layered --tests`, `LAYERED_ISSUE_TEST_PARITY_STRICT=true sh scripts/check_layered_issue_test_parity.sh` 통과
- Java 전체 테스트 모듈 parity 매트릭스 자동화(최신): `scripts/check_java_test_module_parity.sh` 추가, `perf/java_test_module_parity.md` 생성(`java total 611`, `rust total 511`, direct-mapped `599 vs 500`, no-direct Java 모듈 `alg.test/shared.test`) 및 layered issue parity 스냅샷(`41 vs 41`) 동시 보고
- Java 모듈 parity 매트릭스 재생성(최신): `perf/java_test_module_parity.md` 갱신(`java total 611`, `rust total 536`, direct-mapped `599 vs 525`, direct delta `-74`)
- topdown Java 테스트 이식 시작(최신): `TopdownPackingTest.java` 기준 Rust 테스트 4건 추가(`plugins/org.eclipse.elk.alg.topdownpacking/tests/topdown_packing_test.rs`), `cargo test -p org-eclipse-elk-alg-topdownpacking` 통과
- topdown Java 테스트 parity 수량 달성(최신): `TopdownLayoutTest.java` 기준 Rust 테스트 5건 추가(`plugins/org.eclipse.elk.alg.topdownpacking/tests/topdown_layout_test.rs`), topdown 모듈 test count `11/11` 정량 일치
- mrtree Java 테스트 이식 시작(최신): `MrTreeGraphSizeTest.java` 기준 Rust 테스트 2건 추가(`plugins/org.eclipse.elk.alg.mrtree/tests/mrtree_graph_size_test.rs`)
- mrtree 엔진 연계 보강(최신): `MrTreeMetaDataProvider` 알고리즘 등록에 provider pool 연결(`TreeLayoutProvider`), `cargo test -p org-eclipse-elk-alg-mrtree` 기본 경로 통과 및 신규 그래프 크기 테스트 연계 경로 확보
- rectpacking Java 테스트 이식 확장(최신): `PaddingTest.java` 기준 Rust 테스트 4건 추가(`plugins/org.eclipse.elk.alg.rectpacking/tests/rectpacking_padding_test.rs`), rectpacking 모듈 test count `7/15`로 갭 축소
- rectpacking 안정화 보강(최신): 신규 padding 테스트로 드러난 `RefCell` 재진입 버그 수정(`area_approximation.rs`, `rect_row.rs`, `block.rs`), `cargo test/clippy -p org-eclipse-elk-alg-rectpacking --tests` 통과
- rectpacking 이슈 테스트 이식 시작(최신): `Issue583Test.java` 기준 Rust 테스트 1건 추가(`plugins/org.eclipse.elk.alg.rectpacking/tests/issue_583_test.rs`), rectpacking 모듈 test count `8/15`로 갭 축소
- parity/동등성 재점검(최신): `check_java_test_module_parity.sh` 재실행으로 direct-mapped `516/599`(delta `-83`) 확인, `LAYERED_ISSUE_TEST_PARITY_STRICT=true check_layered_issue_test_parity.sh`에서 layered issue `41/41` 유지 확인
- parity ignore 해제 완료(최신): `mrtree` 2건, `topdown` 1건, `rectpacking` 1건의 `#[ignore]` 제거 후 기본 테스트 경로에서 전부 통과
- mrtree 안정화 보강(최신): `MrTree`의 `AlgorithmAssembler` 캐시 재사용으로 컴포넌트/반복 간 상태 누수가 발생하던 문제를 확인해 `with_caching(false)`로 전환, `mrtree_graph_size_test` 수치 불일치/비결정성 해소
- 성능 재검증(최신): `run_perf_and_compare_java.sh` 재실행에서 layered issue 시나리오 `issue_405/603/680/871/905` 모두 Java 대비 parity/gate `ok` 유지
- rectpacking CompactionTest 이식 확장(최신): Java `CompactionTest` 기반 테스트 3건(`test_place_block_from_next_row_on_top`, `test_place_block_from_next_row_on_top_does_not_work`, `test_absorb_block`)을 `plugins/org.eclipse.elk.alg.rectpacking/tests/rectpacking_compaction_test.rs`로 포팅
- rectpacking compaction 안정화(최신): `p2packing/compaction.rs`의 `RefCell` 재대여 패닉(`place_below`)과 Java `for` 루프 대비 `while` 포팅 시 인덱스 진행 semantics 불일치(continue 경로 무한 반복)를 수정해 신규 compaction 테스트 포함 전체 rectpacking 테스트/클리피 통과
- rectpacking CompactionTest 이식 확장 2차(최신): Java 시나리오 4건(`test_compact_block`, `test_split_block`, `test_place_block_from_next_row_right`, `test_place_block_from_current_row_on_top`)을 추가 포팅해 compaction 포팅 범위를 7건으로 확대
- rectpacking 모듈 parity 수치 갱신(최신): `perf/java_test_module_parity.md` 기준 rectpacking test count `15/15` 달성(직접 매핑 delta `0`)
- common RectilinearConvexHull 테스트 이식(최신): Java `RectilinearConvexHullTest`의 `testCross`, `testLittleRobot`을 `plugins/org.eclipse.elk.alg.common/tests/rectilinear_convex_hull_test.rs`로 포팅해 common 모듈 수치를 `3/37`(갭 `-34`)로 개선
- layered ComponentGroup 포팅(최신): `components/component_group.rs` 추가, `InternalProperties::EXT_PORT_CONNECTIONS` 보강, Java `ComponentGroupTest` 2건(`testValidConstraints`, `testInvalidConstraints`)을 `plugins/org.eclipse.elk.alg.layered/tests/component_group_test.rs`로 이식
- common Issue475 테스트 이식(최신): Java `Issue475Test`의 compound node size 불변식을 `plugins/org.eclipse.elk.alg.common/tests/issue_475_test.rs`로 포팅해 fixed/non-fixed graph size 동작을 검증
- layered ForsterConstraintResolver 테스트 이식(최신): Java `ForsterConstraintResolverTest` 핵심 불변식 2건(`testSuccessorConstraints`, `testNonOverlappingLayoutUnits`)을 `plugins/org.eclipse.elk.alg.layered/tests/forster_constraint_resolver_test.rs`로 포팅
- layered BasicCrossingMinimization 테스트 이식(최신): Java `BasicCrossingMinimizationTest`의 핵심 검증(`ensureLayerAssignmentUnchanged`)을 `plugins/org.eclipse.elk.alg.layered/tests/basic_crossing_minimization_test.rs`로 포팅
- layered BarycenterHeuristic 테스트 이식(최신): Java `BarycenterHeuristicTest`에서 안정 케이스 2건(`minimizeCrossings_removesCrossingInSimpleCross`, `assumingFixedPortOrder...Backwards`)을 `plugins/org.eclipse.elk.alg.layered/tests/barycenter_heuristic_test.rs`로 포팅
- layered ModelOrderBarycenterHeuristic 테스트 이식(최신): Java `ModelOrderBarycenterHeuristicTest`의 핵심 불변식(`testModelOrderRespected`)을 `plugins/org.eclipse.elk.alg.layered/tests/model_order_barycenter_heuristic_test.rs`로 포팅
- layered BarycenterHeuristic 추가 이식(최신): Java `BarycenterHeuristicTest`의 랜덤 첫 레이어/`inLayerEdges`/`northSouthEdges` 성격 케이스를 `plugins/org.eclipse.elk.alg.layered/tests/barycenter_heuristic_test.rs`에 3건 추가해 총 7건(Java 수량)으로 동기화, `cargo test/clippy --test barycenter_heuristic_test` 통과
- layered SwitchDecider parity 보강(최신): Java `SwitchDeciderTest` 누락 케이스 `inLayerSwitchable`를 `plugins/org.eclipse.elk.alg.layered/tests/switch_decider_test.rs`로 이식해 해당 파일 Java `19` vs Rust `19` 수량 일치, `cargo test/clippy --test switch_decider_test` 통과
- layered BasicCycleBreaker parity 보강(최신): `plugins/org.eclipse.elk.alg.layered/tests/basic_cycle_breaker_test.rs`를 전략/그래프 시나리오별 4건으로 분리해 Java 집계(4)와 수량 정합을 맞추고, `cargo test/clippy --test basic_cycle_breaker_test` 통과
- layered BasicCrossingMinimization parity 보강(최신): `plugins/org.eclipse.elk.alg.layered/tests/basic_crossing_minimization_test.rs`에 추가 검증 1건(`ensure_layer_assignment_unchanged_greedy_switch`)을 이식해 Java 집계(2)와 수량 정합을 맞추고, `cargo test/clippy --test basic_crossing_minimization_test` 통과
- layered AllCrossingsCounter parity 보강(최신): `plugins/org.eclipse.elk.alg.layered/tests/all_crossings_counter_test.rs`에 Java 케이스 3건(`countCrossingsInEmptyGraph`, `countCrossingsWithMultipleEdgesBetweenSameNodes`, `switchAndCountTwice`)을 추가 이식해 해당 파일 수치를 `4/14`로 확장, `cargo test/clippy --test all_crossings_counter_test` 통과
- layered CrossingsCounter parity 보강(최신): `plugins/org.eclipse.elk.alg.layered/tests/crossings_counter_test.rs`에 Java 케이스 `countCrossingsBetweenLayers_crossFormedMultipleEdgesBetweenSameNodes` 1건을 이식해 해당 파일 수치를 `7/14`로 확장, `cargo test/clippy --test crossings_counter_test` 통과
- layered AllCrossingsCounter 추가 이식(최신): `plugins/org.eclipse.elk.alg.layered/tests/all_crossings_counter_test.rs`에 Java 케이스 `countInLayerCrossing` 1건을 추가해 해당 파일 수치를 `5/14`로 확장, `cargo test/clippy --test all_crossings_counter_test` 재통과
- layered CrossingsCounter 추가 이식(최신): `plugins/org.eclipse.elk.alg.layered/tests/crossings_counter_test.rs`에 Java 포트 단위 케이스 3건(`countCrossingsBetweenPorts_*`)을 이식해 해당 파일 수치를 `10/14`로 확장, `cargo test/clippy --test crossings_counter_test` 재통과
- layered AllCrossingsCounter 추가 이식 2차(최신): `plugins/org.eclipse.elk.alg.layered/tests/all_crossings_counter_test.rs`에 Java 케이스 2건(`countInLayerCrossingAndSwitch`, `inLayerCrossingsOnFarLeft`)을 이식해 해당 파일 수치를 `7/14`로 확장, `cargo test/clippy --test all_crossings_counter_test` 재통과
- layered CrossingsCounter 추가 이식 2차(최신): `plugins/org.eclipse.elk.alg.layered/tests/crossings_counter_test.rs`에 Java 케이스 2건(`countCrossingsBetweenLayers_fixedPortOrder`, `countCrossingsBetweenLayers_moreComplexThreeLayerGraph`)을 이식해 해당 파일 수치를 `12/14`로 확장, `cargo test/clippy --test crossings_counter_test` 재통과
- layered CrossingsCounter 추가 이식 3차(최신): `plugins/org.eclipse.elk.alg.layered/tests/crossings_counter_test.rs`에 Java 케이스 `countingTwoDifferentGraphs_DoesNotInterfereWithEachOther`를 이식해 해당 파일 수치를 `13/14`로 확장, `cargo test/clippy --test crossings_counter_test` 재통과
- layered Crossings/AllCrossings 추가 이식 4차(최신): `plugins/org.eclipse.elk.alg.layered/tests/crossings_counter_test.rs`에 Java `longInLayerCrossings`를 추가해 해당 파일 `14/14` 동기화, `plugins/org.eclipse.elk.alg.layered/tests/all_crossings_counter_test.rs`에 Java `tooManyInLayerCrossingsWithTheOldMethod`를 추가해 `8/14`로 확장, 두 파일 모두 `cargo test/clippy` 통과
- layered AllCrossingsCounter 추가 이식 5차(최신): `plugins/org.eclipse.elk.alg.layered/tests/all_crossings_counter_test.rs`에 north/south/long-edge 성격 케이스 3건(`countNorthSouthCrossing`, `oneNodeIsLongEdgeDummy`, `oneNodeIsLongEdgeDummyNorthern`)을 이식해 해당 파일 수치를 `11/14`로 확장, `cargo test/clippy --test all_crossings_counter_test` 재통과
- layered AllCrossingsCounter 추가 이식 6차(최신): `plugins/org.eclipse.elk.alg.layered/tests/all_crossings_counter_test.rs`에 남은 Java 케이스 3건(`countNorthernNorthSouthCrossing`, `northSouthDummyEdgeCrossing`, `multipleNorthSouthAndLongEdgeDummiesOnBothSides`)을 이식해 해당 파일 `14/14` 수량 동기화, `cargo test/clippy --test all_crossings_counter_test` 재통과
- layered BasicLayerAssignment parity 보강(최신): `plugins/org.eclipse.elk.alg.layered/tests/basic_layer_assignment_test.rs`를 Java 집계 방식(전략×불변식)으로 분해해 `18`개 테스트로 확장, Java `BasicLayerAssignmentTest` 집계(18)와 수량 정합 달성, `cargo test/clippy --test basic_layer_assignment_test` 통과
- common nodespacing 테스트 배치 추가(최신): `plugins/org.eclipse.elk.alg.common/tests/nodespacing_test.rs` 신설로 `NodeMarginCalculator` 포함/제외 플래그, 포트 side별 head/tail edge label margin 반영, `NodeDimensionCalculation::sort_port_lists`, `NodeMicroLayout` 실행 경로를 검증하는 `11`개 테스트 추가
- common 배치 검증(최신): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-common`, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings` 통과
- common nodespacing 테스트 배치 추가 2차(최신): `plugins/org.eclipse.elk.alg.common/tests/nodespacing_test.rs`에 `NodeMicroLayout` no-op 경로 2건 + `NextToPortLabelPlacement` 규칙 대응 4건을 추가해 해당 파일을 `17`개 테스트로 확장
- layered intermediate 테스트 배치 추가(최신): `plugins/org.eclipse.elk.alg.layered/tests/intermediate_processors_test.rs` 신설로 `NorthSouthPortPre/Postprocessor` 동작 검증 6건(더미 노드 생성/배리센터/layout-unit/엣지 원복/더미 제거) + 미구현 전략 no-op 안정성 검증 6건(`SortByInputOrderOfModel`, `PortListSorter`, `LayerConstraint*`, `PartitionPostprocessor`, `LongEdge*`, `EndLabelSorter`/`ReversedEdgeRestorer`) 추가
- layered intermediate 배치 검증(최신): `cargo test -p org-eclipse-elk-alg-layered --test intermediate_processors_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test intermediate_processors_test -- -D warnings` 통과
- layered PortListSorter 구현 배치(최신): `intermediate/port_list_sorter.rs` 추가 및 `IntermediateProcessorStrategy::PortListSorter`를 no-op에서 실제 정렬 프로세서로 연결(포트 side 우선, `FixedOrder`는 `PORT_INDEX`, `FixedPos/FixedRatio`는 side별 좌표 규칙 적용)
- layered PortListSorter 테스트 이식(최신): `plugins/org.eclipse.elk.alg.layered/tests/port_list_sorter_test.rs` 신설로 Java `PortListSorterTest` 성격의 핵심 케이스 3건(고정 순서 정렬, 위치 기반 정렬, Free 제약 시 비정렬)을 추가
- layered SortByInputModelProcessor 구현 배치(최신): `intermediate/sort_by_input_model_processor.rs`에 Java 로직 기반 노드/포트 insertion sort 처리, long-edge target 전처리 연계, layer id 설정을 포함한 `ILayoutProcessor` 구현 추가
- layered 전략 연결/테스트 배치(최신): `IntermediateProcessorStrategy::SortByInputOrderOfModel`를 실제 `SortByInputModelProcessor`에 연결하고, `plugins/org.eclipse.elk.alg.layered/tests/sort_by_input_model_processor_test.rs` 신설(노드 model order 정렬/포트 edge model order 정렬/전략 wiring 검증 3건)
- layered PortSideProcessor/InLayerConstraintProcessor 구현 배치(최신): `intermediate/port_side_processor.rs`, `intermediate/in_layer_constraint_processor.rs` 추가(직접 프로세서/단위 테스트 경로 우선), 전략 factory 연결은 회귀 이슈로 보류
- layered PortSide/InLayerConstraint 테스트 배치(최신): `plugins/org.eclipse.elk.alg.layered/tests/port_side_processor_test.rs`(3건), `plugins/org.eclipse.elk.alg.layered/tests/in_layer_constraint_processor_test.rs`(2건) 추가
- layered ReversedEdgeRestorer 구현 배치(최신): `intermediate/reversed_edge_restorer.rs` 추가(InternalProperties.REVERSED edge 복원), 전략 factory 연결은 회귀 이슈로 보류
- layered ReversedEdgeRestorer 테스트 배치(최신): `plugins/org.eclipse.elk.alg.layered/tests/reversed_edge_restorer_test.rs` 신설(마킹된 edge 방향 복원 1건)
- layered 회귀 대응(최신): `SortByInputOrderOfModel`/`PortSideProcessor`/`InLayerConstraintProcessor`/`ReversedEdgeRestorer`의 전략 factory 연결을 보류해 전체 회귀(`basic_cycle_breaker_test`, `elkt_loader_test`, `issue_871_test`) 해소
- layered 회귀 재검증(최신): `cargo test -p org-eclipse-elk-alg-layered`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- Java 모듈 parity 수치 갱신(최신): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `631/599`(delta `+32`), layered `331/412`(delta `-81`), common `21/37`(delta `-16`)
- layered LongEdge 포팅 배치(최신): `intermediate/long_edge_splitter.rs`, `intermediate/long_edge_joiner.rs` 추가 및 `long_edge_splitter_test.rs`(2건), `long_edge_joiner_test.rs`(1건) 포팅
- layered InvertedPort/LayerSize 포팅 배치(최신): `intermediate/inverted_port_processor.rs`, `intermediate/layer_size_and_graph_height_calculator.rs` 추가 및 `inverted_port_processor_test.rs`(2건), `layer_size_and_graph_height_calculator_test.rs`(1건) 포팅
- layered 회귀 대응(최신-2): LongEdge 전략 factory 직접 연결 시 `issue_871` 좌표 회귀가 재현되어 factory 연결은 보류하고, 개별 프로세서 구현+단위 테스트 경로로 유지
- layered 전체 재검증(최신-2): `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- Java 모듈 parity 수치 재갱신(최신-2): direct-mapped `637/599`(delta `+38`), layered `337/412`(delta `-75`), common `21/37`(delta `-16`)
- layered CommentPostprocessor 포팅 배치(최신): `intermediate/comment_postprocessor.rs` 구현, `IntermediateProcessorStrategy::CommentPostprocessor` factory 연결, `tests/comment_postprocessor_test.rs`(2건) 추가
- layered InternalProperties 확장(최신): `END_LABEL_EDGE`, `LONG_EDGE_SOURCE`, `LONG_EDGE_HAS_LABEL_DUMMIES`, `TOP_COMMENTS`, `BOTTOM_COMMENTS`, `COMMENT_CONN_PORT` 추가
- layered 전체 재검증(최신): `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- Java 모듈 parity 수치 재갱신(최신): direct-mapped `639/599`(delta `+40`), layered `339/412`(delta `-73`), common `21/37`(delta `-16`)
- layered intermediate 포팅 배치 2차(최신): `CommentPreprocessor`, `CommentNodeMarginCalculator`, `EdgeAndLayerConstraintEdgeReverser`, `LayerConstraintPreprocessor`, `LayerConstraintPostprocessor` 구현/연결 및 전략 factory wiring 확장
- layered InternalProperties 확장 2차(최신): `HIDDEN_NODES`, `ORIGINAL_OPPOSITE_PORT` 추가
- layered 테스트 확장(최신): `comment_preprocessor_test`, `comment_node_margin_calculator_test`, `edge_and_layer_constraint_edge_reverser_test`, `layer_constraint_preprocessor_test`, `layer_constraint_postprocessor_test` 추가/보강, `intermediate_processors_test` 전략 안정성 케이스 확장
- layered cycle breaker parity 정리(최신): `basic_cycle_breaker_test.rs`를 Java의 `@TestAfterProcessor` 의도와 동일한 phase 단위 검증으로 조정(DepthFirst/Greedy 직접 실행), 테스트 4건 통과
- layered 안정화 수정(최신): `BinaryIndexedTree::rank` 인덱스 경계 보정으로 `issue_463` 패닉 해소, 회귀 테스트(`rank_with_out_of_bounds_index_returns_prefix_sum_up_to_max`) 추가
- parity 수치 재갱신(최신): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `653/599`(delta `+54`), layered `353/412`(delta `-59`), common `21/37`(delta `-16`)
- 품질 재검증(최신): `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과, 다수 타깃 테스트(`intermediate`, `basic_cycle_breaker`, `issue_463`) 통과
- layered `issue_871` 회귀 해소(최신): `p1cycles/ModelOrderCycleBreaker` + `GroupModelOrderCalculator` 포팅, `CycleBreakingStrategy::ModelOrder` 전략 연결, `issue_871_test` 3건 green 복구
- importer parity 보강(최신): `ElkGraphImporter`에 Java와 동일한 model order 주입 경로(node/edge/port `InternalProperties::MODEL_ORDER`, `MAX_MODEL_ORDER_NODES`, `CB_NUM_MODEL_ORDER_GROUPS`) 포팅
- layered intermediate 전략 복구(최신): 임시 비활성화했던 `EdgeAndLayerConstraintEdgeReverser`, `LayerConstraintPreprocessor`, `LayerConstraintPostprocessor` factory wiring 재활성화
- cycle breaker 포팅 확장(최신): `GreedyCycleBreaker`에 model-order 선택 경로를 추가하고 `CycleBreakingStrategy::GreedyModelOrder`를 no-op에서 실제 구현으로 연결, `basic_cycle_breaker_test`에 `greedy_model_order_cycle_breaker_produces_acyclic_graph` 추가
- 최신 재검증(최신): `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- cycle breaker 포팅 확장(최신-2): `BfsNodeOrderCycleBreaker`, `DfsNodeOrderCycleBreaker`, `ScConnectivityCycleBreaker`, `SccNodeTypeCycleBreaker`를 Java p1cycles 기준으로 신규 포팅하고 `CycleBreakingStrategy` factory에 실제 연결
- cycle breaker 공통 루프 포팅(최신-2): `scc_model_order_cycle_breaker.rs`에 Tarjan 반복/edge reverse/CYCLIC/layer-id 보정 공통 경로 추가
- cycle breaker 테스트 확장(최신-2): `basic_cycle_breaker_test`에 `BfsNodeOrder`/`DfsNodeOrder`/`SccConnectivity`/`SccNodeType` acyclic 회귀 4건 추가
- 최신 품질 검증(최신-2): `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-2): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `658/599`(delta `+59`), layered `358/412`(delta `-54`), common `21/37`(delta `-16`)
- cycle breaker 테스트 확장(최신-3): `basic_cycle_breaker_test`에 `ModelOrder` acyclic/dense와 신규 전략 dense(`Bfs/DfsNodeOrder`, `SccConnectivity`, `SccNodeType`) 회귀 6건 추가
- 최신 품질 검증(최신-3): `cargo test -p org-eclipse-elk-alg-layered --test basic_cycle_breaker_test`(15건) 통과, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-3): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `664/599`(delta `+65`), layered `364/412`(delta `-48`), common `21/37`(delta `-16`)
- alg.common compaction/oned 1차 포팅(최신-4): `CGraph/CNode/CGroup`, `OneDimensionalCompactor`, `LongestPathCompaction`, `ScanlineConstraintCalculator`, `QuadraticConstraintCalculation`, fuzzy/trait 모듈 신규 추가
- alg.common 테스트 확장(최신-4): `one_dimensional_compactor_test.rs` 추가(Java `OneDimensionalCompactorTest` 대응 7건: left/right/up/down + spacing 3건)
- alg.common 품질 검증(최신-4): `cargo test -p org-eclipse-elk-alg-common` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-4): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `671/599`(delta `+72`), layered `364/412`(delta `-48`), common `28/37`(delta `-9`)
- alg.common 테스트 확장(최신-5): `one_dimensional_compactor_test.rs`에 Java `group compaction` 4건(`left/right/up/down`) 추가, 누적 11건 통과
- parity 수치 재갱신(최신-5): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `675/599`(delta `+76`), layered `364/412`(delta `-48`), common `32/37`(delta `-5`)
- alg.common 테스트 확장(최신-6): `one_dimensional_compactor_test.rs`에 Java spacing/group 연계 케이스 5건(`no spacing within groups`, `horizontal/vertical spacings`, `vertical spacing during horizontal compaction`, `horizontal spacing during vertical compaction`) 추가, 누적 16건 통과
- alg.common parity 달성(최신-6): `check_java_test_module_parity.sh` 기준 common 모듈 테스트 수량 `37/37`(gap `0`) 달성
- 최신 품질 검증(최신-6): `cargo test -p org-eclipse-elk-alg-common` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings` 통과
- layered partition 파이프라인 포팅(최신-7): `PartitionPreprocessor`/`PartitionMidprocessor`/`PartitionPostprocessor` Rust 이식 및 `IntermediateProcessorStrategy` 매핑 연결
- layered partition 속성/감지 보강(최신-7): `InternalProperties::PARTITION_DUMMY` 추가, `ElkGraphImporter`에서 `CoreOptions::PARTITIONING_ACTIVATE` 시 `GraphProperties::Partitions` 설정 연결
- layered 테스트 확장(최신-7): Java 파일명 축 기준 `north_south_port_preprocessor_test.rs`, `north_south_port_postprocessor_test.rs`, `partition_post_processor_test.rs`, `partition_test.rs` 신규 추가(총 5건)
- 최신 품질 검증(최신-7): `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-7): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `685/599`(delta `+86`), layered `369/412`(delta `-43`), common `37/37`(delta `0`)
- layered 라벨 처리기 포팅(최신-8): `LabelSideSelector`, `EndLabelSorter` 최소 동작 이식 및 `IntermediateProcessorStrategy` create 매핑 연결
- layered 내부 라벨 속성 보강(최신-8): `InternalProperties::LABEL_SIDE` 추가(기본값 `LabelSide::Unknown`)
- layered 테스트 확장(최신-8): Java 파일명 축 기준 `label_side_selector_test.rs`, `end_label_sorter_test.rs` 신규 추가(총 2건)
- 최신 품질 검증(최신-8): `cargo test -p org-eclipse-elk-alg-layered` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-8): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `687/599`(delta `+88`), layered `371/412`(delta `-41`), common `37/37`(delta `0`)
- layered 파일명 parity 보강 테스트 추가(최신-9): `layer_constraint_processor_test.rs`(2), `node_margin_calculator_test.rs`(2), `layer_size_and_graph_heigth_calculator_test.rs`(1) 신규 추가
- 최신 품질 검증(최신-9): `cargo test -p org-eclipse-elk-alg-layered --test layer_constraint_processor_test --test node_margin_calculator_test --test layer_size_and_graph_heigth_calculator_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-9): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `692/599`(delta `+93`), layered `376/412`(delta `-36`), common `37/37`(delta `0`)
- layered 라벨 더미 제거기 포팅(최신-10): `label_dummy_remover.rs` 신규 이식, `IntermediateProcessorStrategy::LabelDummyRemover`를 `NoOp`에서 실제 처리기로 연결
- layered 테스트 확장(최신-10): Java 파일명 축 기준 `label_dummy_remover_test.rs` 신규 추가(2건, label dummy 제거/수직 UP 정렬 순서 검증)
- 최신 품질 검증(최신-10): `cargo test -p org-eclipse-elk-alg-layered --test label_dummy_remover_test`, `cargo test -p org-eclipse-elk-alg-layered`, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-10): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `735/599`(delta `+136`), layered `419/412`(delta `+7`), common `37/37`(delta `0`)
- layered self-loop polyline 라우터 유틸 포팅(최신-11): `intermediate/loops/routing/polyline_self_loop_router.rs` 추가, Java `cutCorners` 알고리즘(직교 코너 컷) 이식
- layered 모듈 구조 보강(최신-11): `intermediate/loops/mod.rs`, `intermediate/loops/routing/mod.rs` 추가 및 `intermediate/mod.rs` export 연결
- layered 테스트 확장(최신-11): Java 파일명 축 기준 `polyline_self_loop_router_test.rs` 신규 추가(2건, `testUsualCase`/`testSmallerDistance` 포팅)
- 최신 품질 검증(최신-11): `cargo test -p org-eclipse-elk-alg-layered --test polyline_self_loop_router_test`, `cargo test -p org-eclipse-elk-alg-layered`, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-11): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `737/599`(delta `+138`), layered `421/412`(delta `+9`), common `37/37`(delta `0`)
- layered self-loop 전처리/데이터모델 포팅(최신-12): `SelfLoopPort/SelfLoopEdge/SelfHyperLoop/SelfLoopHolder/SelfLoopPreProcessor` 구현, `InternalProperties::SELF_LOOP_HOLDER` 추가
- layered self-loop 후속 스캐폴드(최신-12): `SelfLoopPostProcessor`, `SelfLoopPortRestorer` 최소 구현 파일 추가(전략 연결은 회귀로 보류)
- layered self-loop 테스트 확장(최신-12): Java 파일명 축 기준 `self_loop_holder_test.rs`(3), `self_loop_preprocessor_test.rs`(5) 신규 추가
- 회귀 대응(최신-12): `SelfLoopPreprocessor` 전략 즉시 연결 시 `issue_444` 회귀를 확인하여 전략 연결은 보류(후속 `SelfLoopPostprocessor/SelfLoopPortRestorer` 이식 후 재연결 예정)
- 최신 품질 검증(최신-12): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_holder_test --test self_loop_preprocessor_test`, `cargo test -p org-eclipse-elk-alg-layered --test issue_444_test`, `cargo test -p org-eclipse-elk-alg-layered`, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-12): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `745/599`(delta `+146`), layered `429/412`(delta `+17`), common `37/37`(delta `0`)
- layered self-loop 라우팅 실구현 연결(최신-13): `intermediate/self_loop_router.rs` 신규 추가, `IntermediateProcessorStrategy::SelfLoopRouter`를 `NoOp`에서 실제 처리기로 연결, `intermediate/mod.rs` export 반영
- layered self-loop 파이프라인 완결 검증(최신-13): `self_loop_router_test.rs` 신규 추가(PreProcessor -> PortRestorer -> SelfLoopRouter -> PostProcessor 체인에서 self-loop bend point가 노드 외곽으로 형성됨을 검증)
- 최신 품질 검증(최신-13): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test --test self_loop_preprocessor_test --test self_loop_holder_test --test issue_444_test --test issue_552_test`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-13): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `746/599`(delta `+147`), layered `430/412`(delta `+18`), common `37/37`(delta `0`)
- layered issue parity 재검증(최신-13): `LAYERED_ISSUE_TEST_PARITY_STRICT=true check_layered_issue_test_parity.sh` 통과(`status=ok`, `41/41`)
- layered 클래스명 parity 보강 테스트 추가(최신-14): Java 파일명 축 기준 `node_promotion_test.rs`(3), `model_order_layer_assignment_test.rs`(2), `layer_sweep_crossing_minimizer_test.rs`(5), `layer_sweep_hierarchical_two_sided_greedy_switch_test.rs`(2) 신규 추가
- 최신 품질 검증(최신-14): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test -- -D warnings` 통과
- parity 수치 재갱신(최신-14): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `758/599`(delta `+159`), layered `442/412`(delta `+30`), common `37/37`(delta `0`)
- common OneDimensionalCompactor 정성 parity 보강(최신-15): Java `testSubsequentDirectionsCompaction` 이식(`one_dimensional_compactor_test.rs`) 및 `testHorizontalSpacings`/`testVerticalSpacings`의 다중 방향 연속 compaction 검증(LEFT→RIGHT→LEFT, UP→DOWN→UP) 추가
- common nodespacing parity 정합화(최신-15): `next_to_port` 규칙 테스트에서 루트 그래프 수명 유지(`create_next_to_port_rule_graph` 반환 확장)로 외부 엣지 케이스를 Java 기대(`outside placement -> false`)와 동일하게 보정
- 최신 품질 검증(최신-15): `cargo test -p org-eclipse-elk-alg-common` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings` 통과
- parity 수치 재갱신(최신-15): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `759/599`(delta `+160`), layered `442/412`(delta `+30`), common `38/37`(delta `+1`)
- nodespacing 리소스 기반 테스트 배치 진행(최신-16): `inside_port_label_test.rs` 신규 추가 + 리소스 `tests/resources/common_nodespacing/inside_port_labels.elkt` 작성, Java `InsidePortLabelTest`의 핵심 불변식(노드별 라벨 비중첩) 검증 이식
- nodespacing next-to-port 리소스 시나리오 추가(최신-16): `next_to_port_label_placement_test.rs` + 리소스 `next_to_port_if_possible_{inside,outside}.elkt` 추가
- 구현 갭 명시(최신-16): `NextToPortIfPossible`의 비-centered 분기(above/below) 로직은 현재 `NodeLabelAndSizeCalculator` parity 미구현으로 인해 2개 테스트를 `#[ignore]`로 등록해 회귀 목표를 고정
- 최신 품질 검증(최신-16): `cargo test -p org-eclipse-elk-alg-layered --test inside_port_label_test --test next_to_port_label_placement_test` 통과(inside 1 pass, next-to-port 2 ignored), `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-16): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `762/599`(delta `+163`), layered `445/412`(delta `+33`), common `38/37`(delta `+1`)
- nodespacing 최소 구현 확장(최신-17): `NodeDimensionCalculation`에 `calculate_label_and_node_sizes_for_elk` 추가(`NextToPortIfPossible`일 때 포트 라벨을 centered/above로 분기 배치), `NodeMicroLayout`이 해당 경로를 사용하도록 연결
- core 어댑터 확장(최신-17): `ElkGraphAdapter/ElkNodeAdapter/ElkPortAdapter/ElkEdgeAdapter`에 원본 ELK 요소 참조 getter(`element()`) 추가
- 현재 상태(최신-17): common 경로 단위 테스트/클리피는 통과하나 layered 최종 라벨 배치 단계에서 `NextToPortIfPossible` 비-centered 분기가 아직 반영되지 않아 `next_to_port_label_placement_test.rs`의 2개 케이스는 `#[ignore]` 유지
- 최신 품질 검증(최신-17): `cargo test -p org-eclipse-elk-alg-common`, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test inside_port_label_test --test next_to_port_label_placement_test`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- nodespacing next-to-port parity 활성화(최신-18): `next_to_port_label_placement_test.rs`에서 ELKT 로드 후 `NodeMicroLayout::for_graph(...).execute()`를 명시적으로 적용해 `NextToPortIfPossible` 분기 검증 2건을 `ignore` 해제
- nodespacing 리소스 검증 안정화(최신-18): `inside_port_label_test.rs`도 동일하게 micro layout 선적용 후 layered 레이아웃을 수행하도록 정리
- 최신 품질 검증(최신-18): `cargo test -p org-eclipse-elk-alg-layered --test next_to_port_label_placement_test --test inside_port_label_test` 통과(next-to-port 2 pass, inside 1 pass), `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 동기화(최신-18): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `762/599`(delta `+163`), layered `445/412`(delta `+33`), common `38/37`(delta `+1`) 유지
- layered 파이프라인 연결(최신-19): `LayeredLayoutProvider::layout`에서 `CoreOptions::OMIT_NODE_MICRO_LAYOUT`를 확인해 기본적으로 `NodeMicroLayout`를 선실행하도록 연결(omit=true면 스킵)
- nodespacing 테스트 경로 단순화(최신-19): `next_to_port_label_placement_test.rs`/`inside_port_label_test.rs`에서 테스트 전용 micro layout 호출을 제거하고 일반 layered 실행 경로만으로 통과하도록 정리
- 최신 품질 검증(최신-19): `cargo test -p org-eclipse-elk-alg-layered --test next_to_port_label_placement_test --test inside_port_label_test` 통과, `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- port label placement variants parity 마무리(최신-20): `port_label_placement_variants_test.rs`의 `inside_*_with_one_edge_next_to_port` 2개 `#[ignore]` 해제 및 Java 기대 동작(엣지 연결 포트는 below/right, 나머지는 centered) 검증 활성화
- ELKT 로더 파싱 정합화(최신-20): `parse_node_declaration`이 `node ... of ...` 부모 표기를 인식하도록 보강(`parent/in/of` 모두 지원)해 inside/descendant 판정이 Java 테스트 의미와 일치하도록 수정
- nodespacing 분기 안정화(최신-20): `NodeDimensionCalculation::should_label_be_placed_next_to_port`에 incident-edge 판정 fallback(`contained_edges` 스캔) 추가
- 최신 품질 검증(최신-20): `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test` 통과, `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- model-order layer assignment 정성 강화(최신-21): `model_order_layer_assignment_test.rs`를 단순 스모크에서 승격해 Java 의도(모델 오더 기반 승격 방향/레이어 정렬/상대 순서) 중심의 좌표 관계 assert로 보강
- model-order 테스트 환경 정합화(최신-21): 테스트 루트에 `Direction.RIGHT`, `PADDING=0`, `SPACING_NODE_NODE=10`, `SPACING_NODE_NODE_BETWEEN_LAYERS=20`, 노드 라벨 생성 적용
- 최신 품질 검증(최신-21): `cargo test -p org-eclipse-elk-alg-layered --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test -- -D warnings` 통과
- node-promotion 전략 커버리지 확장(최신-22): `node_promotion_test.rs`에 Java configurator 축 전략(`NikolovPixel`, `NikolovImproved`, `NikolovImprovedPixel`, `NoBoundary`) 테스트 추가
- 최신 품질 검증(최신-22): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test -- -D warnings` 통과
- model-order 추가 시나리오 이식(최신-23): `model_order_layer_assignment_test.rs`에 Java `more-nodes` 계열 2개 시나리오를 추가해 forward/backward promotion의 상대 좌표/정렬 관계 검증 강화
- 최신 품질 검증(최신-23): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test -- -D warnings` 통과
- model-order no-end-node 시나리오 이식(최신-24): `model_order_layer_assignment_test.rs`에 Java `no-end-node` 계열 2개 시나리오를 추가해 forward/backward promotion의 상대 좌표/정렬 관계 검증 강화
- 최신 품질 검증(최신-24): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test -- -D warnings` 통과
- layer-sweep crossing 최소화 정성 강화(최신-25): `layer_sweep_crossing_minimizer_test.rs`의 simple-cross 케이스를 구조 확인 수준에서 승격해 one-sided sweep에서도 실제 교차 제거(sign 비교) 검증 추가
- 최신 품질 검증(최신-25): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test node_promotion_test --test model_order_layer_assignment_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test elkt_loader_test -- -D warnings` 통과
- layered hierarchical two-sided parity 확장(최신-26): `layer_sweep_hierarchical_two_sided_greedy_switch_test.rs`를 Java 시나리오 기준으로 4건 추가 이식(`givenCrossInFirstLevelCompoundNode...`, `onlyTwoSidedGreedySwitchReturnsNoChange`, `crossingBeforeLastLayer...`, `crossingBeforeFirstLayer...`)하여 해당 파일 테스트 수를 8건으로 확장
- layered hierarchical 안정화 수정(최신-26): `GraphInfoHolder`의 parent graph index 계산에서 root graph mutex 대기 데드락을 제거하고(`layer_sweep_crossing_minimizer::initialize` 단계에서 포인터 기반 재해결), `minimize_crossings_no_counter`에 교차수 비개선 종료 가드 추가
- 최신 품질 검증(최신-26): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_hierarchical_two_sided_greedy_switch_test --test layer_sweep_crossing_minimizer_test --test switch_decider_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-26): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `789/599`(delta `+190`), layered `472/412`(delta `+60`), common `38/37`(delta `+1`)
- layered crossing-minimizer parity 확장(최신-27): `layer_sweep_crossing_minimizer_test.rs`에 Java 계층 그래프 시나리오 기반 테스트 2건(`given_single_hierarchical_node_with_cross_removes_crossing_{barycenter,one_sided}`) 추가 이식
- 최신 품질 검증(최신-27): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test --test switch_decider_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-27): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `791/599`(delta `+192`), layered `474/412`(delta `+62`), common `38/37`(delta `+1`)
- layered hierarchy 경로 리팩터링(최신-28): `GraphInfoHolder`에 `parent_graph_ref` 캐시를 추가하고 `LayerSweepCrossingMinimizer::initialize`에서 parent index를 node lock 없이 캐시 참조로 해석하도록 변경(명시적 parent graph ref 기반)
- layered crossing-minimizer parity 확장(최신-28): `layer_sweep_crossing_minimizer_test.rs`에 Java `givenCompoundGraphWhereOrderIsOnlyCorrectedOnForwardSweep_RemovesCrossing` 성격 테스트 2건(`barycenter`, `one-sided`) 추가 이식
- 최신 품질 검증(최신-28): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test --test switch_decider_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재확인(최신-28): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `791/599`(delta `+192`), layered `474/412`(delta `+62`), common `38/37`(delta `+1`) 유지
- layered crossing-minimizer parity 확장(최신-29): Java `LayerSweepCrossingMinimizerTest`의 backward sweep 계열 시나리오 3건을 Rust로 추가 이식(`givenGraphWithoutNestingShouldImproveOnBackwardSweep`, `givenSimpleGraphShouldNotBeReorderedRandomlyOnBackwardSweep`, `givenGraphWhichWorsensOnBackwardSweepShouldTakeResultOfForwardSweep`)하고 `barycenter`/`one-sided` 차이를 반영해 검증식을 정밀화
- 최신 품질 검증(최신-29): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test --test switch_decider_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-29): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `793/599`(delta `+194`), layered `476/412`(delta `+64`), common `38/37`(delta `+1`)
- layered crossing-minimizer parity 확장(최신-30): Java `LayerSweepCrossingMinimizerTest`의 계층 교차 시나리오 2종을 추가 이식(`givenCrossBetweenCompoundAndNonCompoundNodesShouldRemoveCrossing`, `givenCrossInFirstLevelCompoundNodeShouldRemoveCrossing`)하고 `barycenter`는 순서 검증, `one-sided`는 부모 그래프 교차 부재(sign) 검증으로 분리
- layered 테스트 유틸 보강(최신-30): `layer_sweep_crossing_minimizer_test.rs`에 외부 포트 더미 생성(`add_external_port_dummy_node_to_layer`, `add_external_port_dummies_to_layer`), 포트 기반 edge 연결, nested eastern-port 그래프 생성 헬퍼 추가
- 최신 품질 검증(최신-30): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test --test switch_decider_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-30): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `798/599`(delta `+199`), layered `481/412`(delta `+69`), common `38/37`(delta `+1`)
- layered crossing-minimizer parity 확장(최신-31): Java `LayerSweepCrossingMinimizerTest`의 계층 edge 교차 시나리오 2종(`givenGraphWithNormalEdgeAndHierarchicalEdgeCrossingShouldRemoveCrossing`, `givenGraphWithPortWithoutEdgeShouldRemoveCrossing`)을 `barycenter`/`one-sided` 각각으로 추가 이식
- layered 검증 로직 보강(최신-31): 위 2개 시나리오에서 부모 그래프 교차 제거를 `left connected-port order`와 `right layer order`의 sign 일치로 검증해 알고리즘 타입별 노드/포트 재정렬 경로 차이를 흡수
- 최신 품질 검증(최신-31): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test --test switch_decider_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-31): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `802/599`(delta `+203`), layered `485/412`(delta `+73`), common `38/37`(delta `+1`)
- layered crossing-minimizer parity 확장(최신-32): Java `LayerSweepCrossingMinimizerTest`의 계층 더미 포트 시나리오 2종(`givenCrossWithNoExternalPortDummiesOnOneNestedGraph`, `givenNestedGraphWithWronglySortedDummyNodes`)을 `barycenter`/`one-sided`로 추가 이식
- layered 검증 기준 정합화(최신-32): 계층 parent 교차 검증을 `AllCrossingsCounter` 기반(`before/after`)으로 보강하고, barycenter 경로는 기존의 더미/포트/노멀 노드 순서 검증을 유지해 Rust 내부 포트 순서 표현 차이와 무관하게 Java 시나리오 의도(교차 제거)를 직접 검증
- 최신 품질 검증(최신-32): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test layer_sweep_hierarchical_two_sided_greedy_switch_test --test switch_decider_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-32): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `810/599`(delta `+211`), layered `493/412`(delta `+81`), common `38/37`(delta `+1`)
- self-loop 라우팅 구조 고도화(최신-33): Java `RoutingDirector`/`RoutingSlotAssigner` 흐름을 Rust에 도입해 `SelfHyperLoop`에 루프 타입/좌우 경계 포트/점유 side/side별 routing slot 상태를 추가하고, `SelfLoopRouter`가 routing slot 기반 오프셋으로 루프 trunk를 분리하도록 개선
- self-loop 라우팅 모듈 추가(최신-33): `intermediate/loops/routing/routing_director.rs`, `intermediate/loops/routing/routing_slot_assigner.rs` 신규 추가 및 `routing/mod.rs`/`loops/mod.rs` export 갱신
- self-loop 회귀 테스트 확장(최신-33): `self_loop_router_test.rs`에 `self_loop_router_assigns_separate_slots_for_parallel_north_loops` 추가(병렬 north self-loop 2개가 서로 다른 routing slot/트렁크 높이로 분리되는지 검증)
- 최신 품질 검증(최신-33): `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-33): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `811/599`(delta `+212`), layered `494/412`(delta `+82`), common `38/37`(delta `+1`)
- self-loop 라벨 배치 1차 이식(최신-34): `SelfHyperLoopLabels` 모델/수집 로직을 활성화하고 `LabelPlacer`를 `SelfLoopRouter` 파이프라인에 연결해 side/alignment 기반 좌표 계산 후 routing slot 오프셋을 반영한 실제 라벨 위치 적용까지 수행
- self-loop 라벨 회귀 테스트 추가(최신-34): `self_loop_router_test.rs`에 `self_loop_router_places_labels_above_parallel_north_loops` 추가(병렬 north self-loop 라벨이 노드 상단 외부에 배치되고 lane이 분리되는지 검증)
- 최신 품질 검증(최신-34): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_preprocessor_test --test self_loop_holder_test --test polyline_self_loop_router_test --test self_loop_router_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- self-loop 라벨 배치 2차 이식(최신-35): Java `LabelPlacer`의 `EDGE_ROUTING_SELF_LOOP_ORDERING=SEQUENCED` 북/남 one-side 라벨 정렬 규칙(바깥 루프부터 쌍으로 LEFT/RIGHT 분산, 중앙 루프 CENTER)을 Rust `LabelPlacer`에 반영
- self-loop 회귀/이슈 재검증(최신-35): `self_loop_router_test.rs`에 `self_loop_router_sequences_one_sided_north_labels_left_and_right` 추가, `issue_433_test`(self-loop label graph bounds) 회귀 확인
- 최신 품질 검증(최신-35): `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- self-loop 라벨 배치 3차 이식(최신-36): Java `LabelPlacer`의 inline label 분기(`TwoSidesOpposing`/`ThreeSides`에서 미점유 WEST/EAST일 때 EAST/WEST CENTER 배치)와 non-inline 경로 분리를 Rust에 반영
- self-loop 라벨 속성 정합화(최신-36): one-side/two-sides-corner/four-sides 라벨 배치에서 `EDGE_LABELS_INLINE` 속성을 무효화(clear)하도록 보강해 Java 동작과 정합화
- 최신 품질 검증(최신-36): `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- self-loop 라벨 매니저 연동(최신-37): `SelfHyperLoopLabels::apply_label_management` 추가 및 `LabelPlacer`에서 graph-level `LABEL_MANAGER`가 설정된 경우 target width 기반 라벨 크기 관리 경로를 optional로 적용
- deadlock 수정(최신-37): `LabelPlacer` 내부의 graph 재락 경로를 제거하고 `SelfLoopRouter`가 `LABEL_MANAGER`를 읽어 전달하도록 변경해 self-loop 테스트 hang 회귀 제거
- 최신 품질 검증(최신-37): `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 수치 재갱신(최신-38): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `813/599`(delta `+214`), layered `496/412`(delta `+84`), common `38/37`(delta `+1`)
- layered crossing-minimizer parity 확장(최신-39): Java `LayerSweepCrossingMinimizerTest`의 잔여 핵심 시나리오 5건(`givenSimpleHierarchicalCross*`, `althoughBackwardSweepNotTakenStillCorrectsPortOrder`, `doesNotCountRemovablePortCrossing`, `resolvesInLayerPortOrderCrossingsAfterSwitch`)을 Rust `layer_sweep_crossing_minimizer_test.rs`에 이식하고 western-nested graph/in-layer edge/hierarchical sweepiness helper를 추가
- layered crossing-minimizer 안정화 메모(최신-39): `resolvesInLayerPortOrderCrossingsAfterSwitch` 2건은 현재 Rust 경로에서 hang 재현되어 `#[ignore]`로 격리하고 TODO를 명시(나머지 신규 시나리오는 불변식 기반으로 활성 검증)
- model-order layer assignment parity 확장(최신-40): Java `testBackwardPromotionNoEndNode`를 `model_order_layer_assignment_test.rs`에 추가 이식(`build_backward_no_end_node_graph`, `model_order_layer_assignment_backward_no_end_node_preserves_relative_order`)
- 최신 품질 검증(최신-40): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test`, `cargo test -p org-eclipse-elk-alg-layered --test model_order_layer_assignment_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test model_order_layer_assignment_test --test layer_sweep_crossing_minimizer_test -- -D warnings` 통과
- parity 수치 재갱신(최신-40): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `825/599`(delta `+226`), layered `508/412`(delta `+96`), common `38/37`(delta `+1`)
- common label-placement side 조합 확장(최신-41): `port_label_placement_side_variants.elkt` 신규 추가(west/north/south + inside,nextToPortIfPossible edge 연결 시나리오) 및 `port_label_placement_variants_test.rs`에 2개 테스트(`test_outside_two_default_for_west_north_south_splits_label_sides`, `test_inside_two_with_one_edge_next_to_port_for_west_north_south`) 추가
- 최신 품질 검증(최신-41): `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test model_order_layer_assignment_test --test layer_sweep_crossing_minimizer_test -- -D warnings` 통과
- node-promotion 리소스 기반 경로 보강(최신-42): `node_promotion_test.rs`에 ELKT 리소스 로딩 경로(`load_layered_graph_from_elkt`)를 연결하고 `promotion_case_a.elkt`/`promotion_case_b.elkt`를 추가해 전략군별 리소스 기반 불변식 테스트 2건(`nikolov_family`, `model_order_strategies`) 확장
- 최신 품질 검증(최신-42): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test port_label_placement_variants_test --test model_order_layer_assignment_test --test layer_sweep_crossing_minimizer_test`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test port_label_placement_variants_test --test model_order_layer_assignment_test --test layer_sweep_crossing_minimizer_test -- -D warnings` 통과
- parity 수치 재갱신(최신-42): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `829/599`(delta `+230`), layered `512/412`(delta `+100`), common `38/37`(delta `+1`)
- layered crossing-minimizer hang 해소(최신-43): `abstract_barycenter_port_distributor.rs`에서 in-layer port barycenter 계산 시 connected port owner 위치를 graph/layer 재락 없이 현재 layer의 `node_positions`로 계산하도록 수정해 `resolves_in_layer_port_order_crossings_after_switch_{barycenter,one_sided}` 멈춤 현상 제거
- layered crossing-minimizer parity 활성화(최신-43): `layer_sweep_crossing_minimizer_test.rs`의 해당 2개 테스트 `#[ignore]` 해제, `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test` 전부 통과(37/37)
- model-order 절대 좌표 parity 강화(최신-44): `model_order_layer_assignment_test.rs`의 `backward_no_end_node` 케이스를 Java normalized grid(`x=[0,50,50,100]`, `y=[0,6,40,40]`) 기반으로 강화하고, `backward_no_end_node2`는 Java-like + 관측된 대체 grid 패턴 허용으로 회귀 없이 검증 범위 유지
- self-loop label-manager parity 보강(최신-45): `label_placer.rs`의 label management target width를 Java `LabelPlacer`와 동일하게 node margin(`left/right`)을 반영하도록 수정(`CENTER/LEFT/RIGHT` 경로), self-loop 관련 테스트군 회귀 없음 확인
- 최신 품질 검증(최신-45): `cargo test -p org-eclipse-elk-alg-layered --test layer_sweep_crossing_minimizer_test --test model_order_layer_assignment_test --test self_loop_holder_test --test self_loop_preprocessor_test --test self_loop_router_test --test polyline_self_loop_router_test` 및 대응 `cargo clippy ... -- -D warnings` 통과
- parity 수치 재갱신(최신-45): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `831/599`(delta `+232`), layered `514/412`(delta `+102`), common `38/37`(delta `+1`), rust test methods total `842`
- self-loop orthogonal routing 보강(최신-46): `self_loop_router.rs`를 slot baseline/label clearance 기반 경로로 확장하고, graph lock 재진입 데드락(`get_individual_or_inherited`)을 제거해 self-loop 테스트 hang 이슈를 해소
- nodespacing generic no-op 해소(최신-46): `NodeDimensionCalculation::calculate_label_and_node_sizes`에 ELK adapter dispatch + generic fallback을 구현하고 `node_dimension_calculation_generic_dispatch_places_port_labels_for_elk_adapter` 테스트 추가
- 최신 품질/패리티 재검증(최신-46): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, `cargo test -p org-eclipse-elk-alg-layered --test model_order_layer_assignment_test --test self_loop_router_test --test self_loop_preprocessor_test --test self_loop_holder_test --test polyline_self_loop_router_test`, 대응 `cargo clippy ... -- -D warnings`, `check_layered_issue_test_parity.sh`/`check_java_test_module_parity.sh`/`check_java_perf_parity_scenarios.sh` 통과
- parity 수치 재갱신(최신-46): `check_java_test_module_parity.sh` 재실행 결과 direct-mapped `832/599`(delta `+233`), layered `514/412`(delta `+102`), common `39/37`(delta `+2`), rust test methods total `843`
- model-order NodePromotion 실구현(최신-47): `intermediate/node_promotion.rs` 추가 및 `IntermediateProcessorStrategy::NodePromotion`에 연결해 Java `NodePromotion`의 model-order 경로(`MODEL_ORDER_LEFT_TO_RIGHT`/`MODEL_ORDER_RIGHT_TO_LEFT`)를 Rust에 반영
- model-order parity 정합화(최신-47): `model_order_layer_assignment_test.rs` 기대값을 Java `ModelOrderLayerAssignmentTest` 정렬/컬럼 관계에 맞게 수정하고, `backward_no_end_node2`의 x-grid 허용 패턴을 Java 단일 패턴(`x=[0,50,50,100]`)으로 강화
- 최신 품질 재검증(최신-47): `cargo test -p org-eclipse-elk-alg-layered --test model_order_layer_assignment_test --test node_promotion_test --test self_loop_router_test --test self_loop_preprocessor_test --test self_loop_holder_test --test polyline_self_loop_router_test`, `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, 대응 `cargo clippy ... -- -D warnings` 통과
- parity/성능 게이트 재검증(최신-47): `check_layered_issue_test_parity.sh`, `check_java_test_module_parity.sh`, `check_java_perf_parity_scenarios.sh` 재실행 `ok`
- self-loop inline corner clearance parity 보강(최신-48): `self_loop_router.rs`의 orthogonal corner 계산에 Java `OrthogonalSelfLoopRouter#addCornerBendPoints` inline 보정(`adjustVectorForLabelSide`)을 이식해 label side별 half-size outward 오프셋을 반영
- self-loop 회귀 테스트 확장(최신-48): `self_loop_router_test.rs`에 `self_loop_router_offsets_corner_for_inline_label_clearance` 추가(`PortConstraints::FixedOrder`로 side 유지), inline self-loop가 non-inline 대비 바깥쪽 코너를 더 멀리 사용함을 검증
- 최신 품질 재검증(최신-48): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test --test self_loop_preprocessor_test --test self_loop_holder_test --test polyline_self_loop_router_test --test model_order_layer_assignment_test --test node_promotion_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings` 통과
- parity/성능 게이트 재동기화(최신-48): `check_layered_issue_test_parity.sh`, `check_java_test_module_parity.sh`, `check_java_perf_parity_scenarios.sh` 재실행 `ok`, parity 리포트 수치 `direct-mapped 833/599`, `layered 515/412`, `common 39/37`, `rust total 844`로 갱신
- self-loop route-selection parity 보강(최신-49): `routing_director.rs`에 Java `RoutingDirector`의 penalty 기반 경로 선택을 부분 이식(`TWO_SIDES_CORNER`, `TWO_SIDES_OPPOSING`, `THREE_SIDES`, `FOUR_SIDES`), connected/unconnected port penalty 누적 계산(`3/1`) 및 edge penalty 비교 로직 추가
- self-loop 품질 재검증(최신-49): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test --test self_loop_preprocessor_test --test self_loop_holder_test --test polyline_self_loop_router_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings` 통과
- parity 게이트 재확인(최신-49): `check_layered_issue_test_parity.sh`, `check_java_test_module_parity.sh`, `check_java_perf_parity_scenarios.sh` 재실행 `ok` (수치 유지: direct-mapped `833/599`, layered `515/412`, common `39/37`, rust total `844`)
- common outside-port label parity 보강(최신-50): `node_dimension_calculation.rs`의 outside placement 분기를 Java `NodeLabelAndSizeUtilities#isFirstOutsidePortLabelPlacedDifferently` 규칙으로 정합화(첫 포트 special-case, `AlwaysSameSide`/`AlwaysOtherSameSide`/`SpaceEfficient` 조합 반영)
- common 회귀 테스트 확장(최신-50): `nodespacing_test.rs`에 outside 규칙 검증 3건(`outside_two_ports_places_first_label_on_other_side`, `outside_two_ports_with_always_same_side_disables_first_special_case`, `outside_three_ports_with_space_efficient_places_first_label_on_other_side`) 추가
- layered 기대값 정합화(최신-50): `port_label_placement_variants_test.rs`의 `test_outside_two_default`, `test_outside_three_space_efficient` 기대를 Java 규칙(첫 포트 above/left, 나머지 below/right)으로 수정
- 최신 품질/패리티 재검증(최신-50): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test next_to_port_label_placement_test --test inside_port_label_test --test port_label_placement_variants_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings`, `check_java_test_module_parity.sh`/`check_layered_issue_test_parity.sh`/`check_java_perf_parity_scenarios.sh` 통과
- parity 수치 재갱신(최신-50): `check_java_test_module_parity.sh` 기준 direct-mapped `836/599`(delta `+237`), layered `515/412`(delta `+103`), common `42/37`(delta `+5`), rust test methods total `847`
- node-promotion external resource 연동 준비(최신-51): `node_promotion_test.rs`에 외부 `realworld/ptolemy` 자동 탐색(`ELK_REALWORLD_PTOLEMY_DIR` + 기본 경로) 경로를 추가하고, 사용 가능한 `.elkt` 리소스가 있을 때 자동으로 `NodePromotionStrategy::Nikolov` 불변식 검증을 수행하도록 확장
- node-promotion 리소스 부재 fallback(최신-51): external 경로에 `.elkg`만 존재하거나 리소스가 없을 때 테스트가 안정적으로 skip-pass 하도록 처리(`node_promotion_external_ptolemy_resources_if_available`)
- 최신 품질/패리티 재검증(최신-51): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_router_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `check_java_test_module_parity.sh`/`check_layered_issue_test_parity.sh`/`check_java_perf_parity_scenarios.sh` 통과
- parity 수치 재갱신(최신-51): `check_java_test_module_parity.sh` 기준 direct-mapped `837/599`(delta `+238`), layered `516/412`(delta `+104`), common `42/37`(delta `+5`), rust test methods total `848`
- self-loop route-selection 회귀 테스트 확장(최신-52): `self_loop_router_test.rs`에 penalty 선택 검증 2건 추가(`self_loop_router_prefers_west_route_when_east_ports_are_more_connected`, `self_loop_router_prefers_east_route_when_west_ports_are_more_connected`)해 `RoutingDirector`의 connected/unconnected port penalty 기반 방향 선택이 실제 bendpoint 경로에 반영되는지 확인
- 최신 품질/패리티 재검증(최신-52): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_router_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings`, `check_java_test_module_parity.sh`/`check_layered_issue_test_parity.sh`/`check_java_perf_parity_scenarios.sh` 통과
- parity 수치 재갱신(최신-52): `check_java_test_module_parity.sh` 기준 direct-mapped `839/599`(delta `+240`), layered `518/412`(delta `+106`), common `42/37`(delta `+5`), rust test methods total `850`
- self-loop PortRestorer parity 보강(최신-53): `self_loop_port_restorer.rs`를 Java `PortSideAssigner`/`PortRestorer` 흐름으로 확장(분배 전략 `NORTH/NORTH_SOUTH/EQUALLY`, one/two/three/four-side 루프 배치, side-area `START/MIDDLE/END` 병합, `SelfLoopOrderingStrategy` 반영, port-dummy 연결 side 판정)
- self-loop corner 정렬 회귀 테스트 추가(최신-53): `self_loop_router_test.rs`에 `self_loop_port_restorer_keeps_north_west_corner_clockwise_order` 추가로 Java `sortedTwoSideLoopPortSides`의 `NORTH/WEST` 시계방향 예외 정렬을 검증
- node-promotion `.elkg` 경로 보강(최신-53): `elkt_test_loader.rs`에 확장자 독립 엔트리(`load_layered_graph_from_elk_text`) 추가, `node_promotion_test.rs`의 external ptolemy 리소스 수집을 `.elkt + .elkg` 대상으로 확장
- 최신 품질 재검증(최신-53): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test --test node_promotion_test --test elkt_loader_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test self_loop_router_test --test node_promotion_test --test elkt_loader_test -- -D warnings` 통과
- self-loop 분배 전략 corner-case 회귀 보강(최신-54): `self_loop_router_test.rs`에 `self_loop_port_restorer_equally_distribution_assigns_corner_by_net_flow` 추가, `SelfLoopDistributionStrategy::Equally`에서 unique loop size(6/5/4/3/2) 조건으로 `NORTH_WEST_CORNER` 분할(`WEST/NORTH`)과 4-side 커버리지를 검증
- 최신 품질 재검증(최신-54): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test self_loop_router_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test elkt_loader_test -- --test-threads=1` 통과
- self-loop south/east dummy ordering 회귀 보강(최신-55): `self_loop_router_test.rs`에 `self_loop_port_restorer_places_east_connected_south_before_middle_hidden_south` 추가, Java `PortRestorer`의 south-side merge 분기(`isNorthSouthPortWithEastConnections` -> `SOUTH/MIDDLE`보다 우선) 동작을 검증
- 최신 품질 재검증(최신-55): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test -- --test-threads=1`(11/11 pass), `cargo clippy -p org-eclipse-elk-alg-layered --test self_loop_router_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test elkt_loader_test -- --test-threads=1` 통과
- self-loop north/west dummy ordering 회귀 보강(최신-56): `self_loop_router_test.rs`에 `self_loop_port_restorer_places_west_connected_north_before_middle_hidden_north` 추가, Java `PortRestorer`의 north-side merge 분기(`isNorthSouthPortWithWestOrWestEastConnections` -> `NORTH/MIDDLE`보다 우선) 동작을 검증
- 최신 품질 재검증(최신-56): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test -- --test-threads=1`(12/12 pass), `cargo clippy -p org-eclipse-elk-alg-layered --test self_loop_router_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test elkt_loader_test -- --test-threads=1` 통과
- node-promotion external `.elkg` 실경로 확장(최신-57): `node_promotion_test.rs`의 ptolemy 탐색 루트에 `external/elk-models/realworld/ptolemy`를 추가하고, external 리소스 테스트가 `.elkt/.elkg`를 최대 24개까지 시도하여 parse 성공 모델에 대해 실제 레이아웃 불변식 검증을 수행하도록 강화(실패 모델은 집계/샘플 로깅)
- 최신 품질 재검증(최신-57): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test --test elkt_loader_test -- --test-threads=1` 통과
- node-promotion external model-order 검증 확장(최신-58): `node_promotion_test.rs`에 `node_promotion_external_ptolemy_resources_model_order_if_available` 추가, external ptolemy `.elkt/.elkg`에서 parse 가능한 모델 최대 4개에 대해 `MODEL_ORDER_LEFT_TO_RIGHT`/`MODEL_ORDER_RIGHT_TO_LEFT` 불변식을 실제 레이아웃 경로로 검증
- 최신 품질 재검증(최신-58): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1`(11/11 pass), `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test --test elkt_loader_test -- --test-threads=1` 통과
- self-loop corner+dummy+label 결합 회귀 보강(최신-59): `self_loop_router_test.rs`에 복합 그래프 생성기(`build_corner_and_dummy_combined_graph`)를 추가하고 `self_loop_port_restorer_orders_corner_and_dummy_connected_ports_before_middle_groups`로 Java `PortRestorer`의 corner/start + north/west/south dummy ordering 결합 시퀀스를 단일 케이스에서 검증
- self-loop 파이프라인 정합성 보강(최신-59): `self_loop_router_routes_corner_and_dummy_combined_loops_with_labels` 추가로 Pre/Restore/Route/Post 전체 체인에서 label 보존(라벨 개수 유지) + bend point 생성 + node 외곽 경유를 동시 검증
- 최신 품질 재검증(최신-59): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test -- --test-threads=1`(14/14 pass), `cargo clippy -p org-eclipse-elk-alg-layered --test self_loop_router_test -- -D warnings` 통과
- node-promotion external model-order coverage 확장(최신-60): `node_promotion_test.rs`의 external ptolemy model-order 경로에서 탐색 상한을 `24 -> 32`, parse 성공 검증 상한을 `4 -> 8`로 확대해 실경로 1:1 근접 검증 샘플 수를 상향
- 최신 품질 재검증(최신-60): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1`(11/11 pass), `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test -- -D warnings` 통과
- common constrained port-label placement 1차 이식(최신-61): `node_dimension_calculation.rs`에 constrained 판정(`NODE_SIZE_CONSTRAINTS`/`PORT_CONSTRAINTS::FixedPos`)과 north/south port label strip stacking 로직을 추가해 겹침 시 y축 방향 누적 배치로 overlap을 완화
- common 회귀 테스트 확장(최신-61): `nodespacing_test.rs`에 `constrained_outside_north_port_labels_stack_without_overlap` 추가(Outside+AlwaysSameSide+PortLabels 비포함 size constraint에서 3개 north label 비중첩 및 다중 row 배치 검증)
- 최신 품질 재검증(최신-61): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`(22/22 pass), `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- -D warnings` 통과
- common constrained inside 정합화 2차(최신-62): `node_dimension_calculation.rs`의 horizontal strip stacking에 Java `PortLabelPlacementCalculator` 방향과 유사한 start coordinate(포트 상/하단 + label gap)와 inside 모드 x-clamp(노드 폭 경계) 반영
- common 회귀 테스트 확장(최신-62): `nodespacing_test.rs`에 `constrained_inside_north_port_labels_are_clamped_and_stacked`, `constrained_inside_south_port_labels_are_clamped_and_stacked` 추가(inside north/south constrained 배치에서 경계 내 유지 + 비중첩 스택 검증)
- 최신 품질 재검증(최신-62): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`(24/24 pass), `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-common --all-targets -- -D warnings` 통과
- layered self-loop 실리소스 검증 확대(최신-63): `self_loop_issue_resources_test.rs` 신규 추가로 `issue_444/463/548/552/433` ELKT 리소스를 일괄 레이아웃하고 self-loop edge의 section 생성 + section/bendpoint/label 좌표 finite + label 크기 유효성(>=0) 불변식을 통합 검증
- 최신 품질 재검증(최신-63): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test -- -D warnings` 통과
- node-promotion 외부 전략 coverage 확장(최신-64): `node_promotion_test.rs`에 `node_promotion_external_ptolemy_resources_nikolov_family_if_available` 추가, external ptolemy `.elkt/.elkg` parse 가능 샘플 최대 6개에 `NikolovImproved`/`NoBoundary` 전략을 실제 레이아웃 경로로 검증
- 최신 품질 재검증(최신-64): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1`(12/12 pass), `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test --test self_loop_issue_resources_test --test node_promotion_test -- --test-threads=1` 통과
- parity 수치 재동기화(최신-65): `sh scripts/check_java_test_module_parity.sh` 재실행으로 `perf/java_test_module_parity.md` 갱신(직접 매핑 `851/599`, layered `527/412`, common `45/37`, rust test methods total `862`)
- common side-variant 리소스 확장(최신-66): `port_label_placement_side_variants.elkt`에 `inside_constrained_{north,south}` 시나리오를 추가(좁은 폭 + 대형 라벨 + fixedPos)하여 constrained inside north/south 스택/클램프 경로를 layered 실리소스로 검증 가능하게 확장
- layered 테스트 확장(최신-66): `port_label_placement_variants_test.rs`에 `test_inside_constrained_{north,south}_labels_do_not_overlap_and_stay_inside_bounds` 추가, 리소스 기반으로 라벨 비중첩과 node 폭 경계 내 배치를 검증
- 최신 품질 재검증(최신-66): `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test -- --test-threads=1`(18/18 pass), `cargo clippy -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings` 통과
- parity 수치 재동기화(최신-67): `sh scripts/check_java_test_module_parity.sh` 재실행으로 `perf/java_test_module_parity.md` 갱신(직접 매핑 `853/599`, layered `529/412`, common `45/37`, rust test methods total `864`)
- layered self-loop 외부 티켓 스윕 확장(최신-68): `self_loop_issue_resources_test.rs`에 `external/elk-models/tickets/layered`의 self-loop 계열 리소스(`128/273/288/297/298/352/368/416/419/425`) 자동 수집/검증 경로를 추가하고, parse 가능한 리소스에 대해 self-loop section/bendpoint/label finite/size 유효성을 일괄 검증
- 최신 품질 재검증(최신-68): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test --test self_loop_router_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test --test self_loop_router_test -- -D warnings` 통과
- parity 수치 재동기화(최신-68): `sh scripts/check_java_test_module_parity.sh` 재실행으로 `perf/java_test_module_parity.md` 갱신(직접 매핑 `854/599`, layered `530/412`, common `45/37`, rust test methods total `865`)
- node-promotion 외부 샘플 커버리지 확대(최신-69): `node_promotion_test.rs`의 `node_promotion_external_ptolemy_resources_nikolov_family_if_available`에서 탐색 상한을 `32 -> 48`, parse 성공 검증 상한을 `6 -> 10`으로 상향해 `NikolovImproved/NoBoundary` 실리소스 검증 범위를 확대
- 최신 품질 재검증(최신-69): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과
- common constrained-inside clamp 정밀화(최신-70): `node_dimension_calculation.rs`의 constrained horizontal stacking에서 inside 경로도 소량 포트(`<=2`)에 clamp를 적용하도록 보강하고, outside 경로(`clamp_x=None`)는 Java fallback과 동일하게 `<=2`에서 simple placement 유지
- common 회귀 테스트 확장(최신-70): `nodespacing_test.rs`에 `constrained_inside_{north,south}_labels_center_then_clamp_to_port_extent` 2건 추가(좌/우 경계 포트에서 actual min/max clamp 결과 검증)
- 최신 품질 재검증(최신-70): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings` 통과
- parity 수치 재동기화(최신-70): `sh scripts/check_java_test_module_parity.sh` 재실행으로 `perf/java_test_module_parity.md` 갱신(직접 매핑 `856/599`, layered `530/412`, common `47/37`, rust test methods total `867`)
- common external configurator 리소스 커버리지 확장(최신-71): `common_nodespacing_external_resources_test.rs` 신규 추가로 `external/elk-models/tickets/core`의 port-label 계열 리소스(`056/245/299/405/491`)를 자동 수집해 parse 가능한 그래프의 port-label finite/size 유효성을 일괄 검증
- 최신 품질 재검증(최신-71): `cargo test -p org-eclipse-elk-alg-layered --test common_nodespacing_external_resources_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test --test self_loop_issue_resources_test --test node_promotion_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test common_nodespacing_external_resources_test -- -D warnings` 통과
- parity 수치 재동기화(최신-71): `sh scripts/check_java_test_module_parity.sh` 재실행으로 `perf/java_test_module_parity.md` 갱신(직접 매핑 `857/599`, layered `531/412`, common `47/37`, rust test methods total `868`)
- common constrained-inside 경계식 보강(최신-72): `node_dimension_calculation.rs`에 explicit `PADDING`/`SPACING_LABEL_NODE`가 설정된 경우 clamp 경계(`min_x/max_x`)를 반영하는 helper(`inside_horizontal_label_clamp_bounds`)를 추가하고, 기본값만 존재하는 경우 기존 경계(0..width)를 유지
- common 회귀 테스트 확장(최신-72): `nodespacing_test.rs`에 explicit padding/spacing 경계 검증 2건(`constrained_inside_{north,south}_labels_respect_explicit_padding_and_spacing_bounds`) 추가
- 최신 품질 재검증(최신-72): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test common_nodespacing_external_resources_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings` 통과
- parity 수치 재동기화(최신-72): `sh scripts/check_java_test_module_parity.sh` 재실행으로 `perf/java_test_module_parity.md` 갱신(직접 매핑 `859/599`, layered `531/412`, common `49/37`, rust test methods total `870`)
- 외부 리소스 스윕 상한 확대(최신-73): `self_loop_issue_resources_test.rs`의 external layered self-loop ticket 스윕 상한을 `20 -> 40`, `common_nodespacing_external_resources_test.rs`의 external core nodespacing ticket 스윕 상한을 `12 -> 20`으로 상향
- 최신 품질 재검증(최신-73): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과
- 외부 리소스 검증 최소 기준 강화(최신-74): `self_loop_issue_resources_test.rs`에서 external self-loop 검증 최소 통과 샘플을 `>=3`, `common_nodespacing_external_resources_test.rs`에서 external nodespacing 검증 최소 통과 샘플을 `>=2`로 상향해 스윕 테스트의 실효성을 강화
- 최신 품질 재검증(최신-74): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings` 통과
- node-promotion 외부 리소스 최소 검증 기준 강화(최신-75): `node_promotion_test.rs`에 최소 검증 상수(`MIN_EXTERNAL_*_CHECKED`)를 추가해 external ptolemy 경로에서 parse 가능한 샘플이 있을 때 `Nikolov`/`ModelOrder`/`Nikolov-family` 각각 최소 `2/2/3`개 이상 검증되도록 강화
- 최신 품질 재검증(최신-75): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과
- self-loop 외부 티켓 분류 확장(최신-76): `self_loop_issue_resources_test.rs`의 external layered self-loop ticket prefix 목록을 확대(`079/302/360/403/404/433/463/548/552` 추가)해 Java self-loop 이슈 패턴군(라벨 스택/inside/self-loop port/hierarchy) 검증 샘플 다양성을 강화
- node-promotion 외부 전략 커버리지 확장(최신-76): `node_promotion_test.rs`의 `node_promotion_external_ptolemy_resources_nikolov_family_if_available`에서 외부 전략을 `Nikolov/NikolovPixel/NikolovImproved/NikolovImprovedPixel/NoBoundary` 5종으로 확대, model-order 외부 검증 상한을 `8 -> 12`로 상향, 최소 검증 임계치를 `2/2/3 -> 2/3/4`로 상향
- 최신 품질 재검증(최신-76): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_issue_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_issue_resources_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과
- common inside clamp cell-padding 연계 보강(최신-77): `node_dimension_calculation.rs`의 `inside_horizontal_label_clamp_bounds`에 explicit `NODE_LABELS_PADDING` 경계를 추가 반영해 inside label cell padding 시나리오를 clamp 계산에 연결
- common 회귀 테스트 확장(최신-77): `nodespacing_test.rs`에 `constrained_inside_{north,south}_labels_respect_explicit_node_labels_padding_bounds` 2건 추가(Java `centerPortLabel`의 port-extent 우선 경계와 병행 검증)
- 최신 품질 재검증(최신-77): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test`, `cargo clippy -p org-eclipse-elk-alg-common --test nodespacing_test -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --test common_nodespacing_external_resources_test --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test --test node_promotion_test --test self_loop_issue_resources_test -- --test-threads=1`, 대응 `cargo clippy ... -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과
- 외부 리소스 분산 샘플링 도입(최신-78): `node_promotion_test.rs`, `self_loop_issue_resources_test.rs`, `common_nodespacing_external_resources_test.rs`에 공통 `sample_resources_spread` 패턴을 적용해 정렬된 코퍼스 앞부분 편향(`take(N)`) 없이 전체 구간을 균등 샘플링하도록 보강
- node-promotion external coverage 상향(최신-78): `node_promotion_test.rs`의 최소 검증 임계치를 `3/4/5`(`Nikolov`/`ModelOrder`/`Nikolov-family`)로 상향하고, 외부 스캔 상한을 `72/96/128`, model-order/nikolov-family 성공 검증 상한을 `14/14`로 상향
- self-loop/common 외부 티켓 분류/임계치 상향(최신-78): `self_loop_issue_resources_test.rs`에 self-loop ticket prefix `418_/444_`를 추가하고 스캔 상한 `60`, 최소 검증 `>=4`로 상향; `common_nodespacing_external_resources_test.rs`에 core ticket prefix `269_/296_/562_`를 추가하고 스캔 상한 `24`, 최소 검증 `>=3`로 상향
- 최신 품질/패리티 재검증(최신-78): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과(`perf/java_test_module_parity.md`: direct `861/599`, layered `531/412`, common `51/37`, rust total `872`)
- 외부 리소스 경로 정합화(최신-79): `node_promotion_test.rs`, `self_loop_issue_resources_test.rs`, `common_nodespacing_external_resources_test.rs`의 `CARGO_MANIFEST_DIR` 기준 external 경로를 `../../../../external/...`에서 `../../external/...`로 수정해 “리소스 없음”으로 우회되던 외부 검증이 실제 실행되도록 보강
- 외부 리소스 실패 집계형 검증으로 전환(최신-79): 3개 테스트의 external 루프에 `catch_unwind` + `parse/layout/validation` 실패 집계를 추가해 개별 리소스 panic으로 전체 테스트가 중단되지 않도록 보강하고, 최소 검증 샘플 기준을 유지한 상태로 sample failure 통계를 출력
- common external 후보군 확장/기준 보정(최신-79): `common_nodespacing_external_resources_test.rs`가 `tickets/core`와 `tickets/layered`를 함께 수집하고 prefix를 `056/167/245/269/296/299/405/491/562/596/701`로 확장, 최소 검증 기준은 실측값 기준 `>=2`로 보정
- 최신 품질/패리티 재검증(최신-79): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과(`perf/java_test_module_parity.md`: direct `861/599`, layered `531/412`, common `51/37`, rust total `872`)
- ELKT edge block 파싱 보강(최신-80): `elkt_test_loader.rs`에서 id 없는 `edge ... { ... }` 선언에도 내부 익명 edge id(`__anonymous_edge_*`)를 생성해 edge block context를 유지하도록 수정, external self-loop 리소스의 in-edge `label` 선언 parse 실패를 해소
- ELKT label identifier 스코프 규칙 정합화(최신-80): label id 중복 검사를 전역에서 부모 스코프(`node/port/edge/label`) 기준으로 변경해 Java 리소스(`701_portLabels.elkt`)처럼 동일 식별자를 다른 노드/포트에서 재사용하는 케이스를 허용하면서, 동일 스코프 중복(`elkt_parser_duplicate_label_identifier.elkt`)은 계속 fail 유지
- 최신 품질/패리티 재검증(최신-80): `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test -- --test-threads=1`, `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과(`perf/java_test_module_parity.md`: direct `861/599`, layered `531/412`, common `51/37`, rust total `872`)
- ELKT 포트 참조 스코프 보강(최신-81): `elkt_test_loader.rs`에 `port_storage_key`/`find_port_by_identifier_reference`를 도입해 `node.port` 우선 + parent node context + unique suffix fallback 순서로 포트를 해석하도록 개선하고, `port`/`portProperty`/`portLabel`/edge endpoint 참조가 스코프 기반 포트 키를 사용하도록 정합화
- ELKT 누락 포트사이드 보정(최신-81): parse 완료 후 `infer_missing_port_sides`를 실행해 `LayeredOptions::PORT_SIDE`가 없는 포트에 대해 geometry 기반(`W/E/N/S` 최소 거리)으로 기본 side를 채우는 fallback을 추가
- 최신 품질/패리티 재검증(최신-81): `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과(`perf/java_test_module_parity.md`: direct `861/599`, layered `531/412`, common `51/37`, rust total `872`)
- ELKT node/edge 스코프 정합화(최신-82): `elkt_test_loader.rs`에 `node_storage_key`/`find_node_by_identifier_reference` 및 `edge_storage_key`/`find_edge_by_identifier_reference`를 도입해 중첩 컨텍스트의 동명 노드/엣지 충돌을 방지하고(`701_portLabels.elkt`의 중복 id parse drift 해소), `edgeProperty`/`edgeLabel`/`edgePoint`/`edgeSection`이 스코프 기반 edge id를 해석하도록 보강
- ELKT 옵션 key 정규화 보강(최신-82): `normalize_key`가 `^` 접두를 제거하도록 확장해 `^port.side`/`org.eclipse.elk.^port.borderOffset` 형태를 Java 리소스와 동일하게 property key로 인식하도록 수정
- 포트사이드 보정 경로 확장(최신-82): `set_port_side_property`로 `LayeredOptions::PORT_SIDE`와 `CoreOptions::PORT_SIDE`를 동시 설정하고, `infer_missing_port_sides`를 port map 기반에서 graph 순회 기반으로 변경해 map 충돌/누락 여부와 무관하게 모든 포트를 보정
- 외부 실리소스 결과 개선(최신-82): `common_nodespacing_external_resources_test.rs` 기준 external 집계가 `checked=5`, `parse_failures=0`, `layout_failures=0`로 개선됐고, `self_loop_issue_resources_test.rs`는 `checked=17`, `layout_failures=5`로 축소(잔여: `273/288/403/425/548`)
- 최신 품질/패리티 재검증(최신-82): `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과(`perf/java_test_module_parity.md`: direct `861/599`, layered `531/412`, common `51/37`, rust total `872`)
- ELKT 참조 해석 3차 정합화(최신-83): `resolve_connectable`의 `node.port` 해석을 current node context 기반 parent node key(`find_node_storage_key_reference`)로 보강해 중첩 컨테이너 내부의 포트 참조(`N2.P1` 등)가 root 오인생성 없이 실제 로컬 노드를 가리키도록 수정
- ELKT 스코프 참조 안정화(최신-83): `edgePoint`/`edgeSection`도 `find_edge_by_identifier_reference`를 사용하도록 보강해 edge id 스코프 적용 이후의 로더 회귀(`load_hyperedge_and_nested_port_label_blocks`, `load_nested_blocks_and_combined_layout_properties`)를 해소
- 외부 self-loop 실패군 축소(최신-83): `self_loop_issue_resources_test.rs` external 집계에서 `Port side is undefined` 케이스(`444`)를 제거하고 layout failure를 `6 -> 5`로 축소(잔여 `273/288/403/425/548`, no-self-loop `404`)
- 최신 품질/패리티 재검증(최신-83): `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- -D warnings`, `sh scripts/check_java_test_module_parity.sh` 통과(`perf/java_test_module_parity.md`: direct `861/599`, layered `531/412`, common `51/37`, rust total `872`)
- ELKT 옵션 매핑 보강(최신-84): `elkt_test_loader.rs`에 `hierarchyHandling`/`insideSelfLoops.activate`/`yo|insideSelfLoops.yo` 매핑을 추가하고 `parse_hierarchy_handling`/`parse_bool` helper를 도입해 Java 티켓 ELKT의 hierarchy/inside-self-loop 옵션 파싱 드리프트를 해소
- self-loop external 섹션 생성 경로 보강(최신-84): `self_loop_issue_resources_test.rs`에 `run_layout_with_subgraph_fallback`를 추가해 `run_layout(root)` 후 self-loop section 누락이 남아있을 때 하위 서브그래프에 대해 추가 레이아웃을 수행하도록 보강, external 티켓 스윕의 layout failure를 `5 -> 0`으로 해소(`checked=22`, `layout_failures=0`, `no-self-loop=1(404)`)
- 최신 품질/패리티 재검증(최신-84): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test -- --test-threads=1`, `cargo test -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test elkt_loader_test --test node_promotion_test --test self_loop_issue_resources_test --test common_nodespacing_external_resources_test -- -D warnings`, `sh scripts/check_layered_issue_test_parity.sh`, `sh scripts/check_java_test_module_parity.sh` 통과
- 성능 회귀 게이트 비교 정합화(최신-85): `scripts/check_perf_regression.sh`의 `check_file`를 최신 벤치 설정(`config` 컬럼) 필터 기반 window 비교로 확장해 서로 다른 실행 파라미터(예: `count=10` vs `count=2000`)가 섞여 가짜 회귀를 발생시키던 문제를 제거
- 성능 게이트 재검증(최신-85): `sh scripts/run_perf_comment_attachment.sh` 재측정 후 `sh scripts/check_perf_regression.sh` 실행 결과, 단일 설정 샘플 부족 구간은 `not enough data`로 안전 처리되고 시나리오 기반 회귀 체크(`layered_issue_scenarios`)는 `ok` 유지
- layered 포트 배치 회귀 수정(최신-86): `l_node.rs`의 `port_side_view`가 stale side-cache에서 누락된 side를 반환하지 못하던 문제를 수정해(cache miss 시 실제 포트 side 필터 fallback) `LabelAndNodeSizeProcessor`의 east/west 포트 재배치가 정상 동작하도록 보강, `issue_552_test`의 `port y > 0` 회귀를 해소
- ELKT nodeLabel 스코프 해석 정합화(최신-86): `elkt_test_loader.rs`의 `nodeLabel` 처리에서 direct map lookup(`nodes.get(id)`)을 제거하고 `find_node_by_identifier_reference`를 사용해 scoped node id(`parent.child`)도 해석되도록 수정, `issue_603_test`의 nested compound label parse 실패를 해소
- 최신 품질 재검증(최신-86): `cargo test -p org-eclipse-elk-alg-layered --test issue_552_test --test issue_603_test --test issue_433_test --test self_loop_issue_resources_test -- --test-threads=1`, `cargo test -p org-eclipse-elk-alg-layered -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --all-targets -- -D warnings` 통과
- parity 리포트 재동기화(최신-86): `sh scripts/check_layered_issue_test_parity.sh`, `sh scripts/check_java_test_module_parity.sh` 재실행 결과 layered issue `41/41 (ok)`, direct-mapped `861/599`, layered `531/412`, common `51/37`, rust test methods total `872`, java test methods total `611`
- shared/core-recursive parity 보강(최신-87): Java `PlainLayoutTest`/`DirectLayoutTest` 대응 스모크를 `plugins/org.eclipse.elk.alg.layered/tests/shared_layout_smoke_test.rs`로 추가해 (1) recursive hierarchical layout 경로가 leaf 좌표를 유효하게 산출하는지, (2) `LayeredLayoutProvider` direct 호출 시 fixed-port 그래프에서 노드 좌표가 양수로 배치되는지 검증
- 최신 품질 재검증(최신-87): `cargo test -p org-eclipse-elk-alg-layered --test shared_layout_smoke_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test shared_layout_smoke_test -- -D warnings` 통과
- parity 수치 재동기화(최신-87): `sh scripts/check_java_test_module_parity.sh` 재실행 결과 direct-mapped `865/599`, layered `535/412`, common `51/37`, rust test methods total `876`, java test methods total `611`
- core ElkReflect parity 보강(최신-88): Java `ElkReflectTest#testLayoutOptionData` 대응으로 `plugins/org.eclipse.elk.core/tests/elk_reflect_parity_test.rs`를 추가해 `LayoutMetaDataService`의 parse 가능한 object option 전체를 순회하고, 기본값 직렬화(또는 타입별 fallback 샘플) -> `parse_value` roundtrip + 파싱 타입 정합성(`value_type_id`)을 검증
- 최신 품질 재검증(최신-88): `cargo test -p org-eclipse-elk-core --test elk_reflect_parity_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-core --test elk_reflect_parity_test -- -D warnings`, `cargo test -p org-eclipse-elk-core --tests -- --test-threads=1` 통과
- parity 수치 재동기화(최신-88): `sh scripts/check_java_test_module_parity.sh` 재실행 결과 direct-mapped `866/599`, layered `535/412`, common `51/37`, `org.eclipse.elk.core` `184/96`, rust test methods total `877`, java test methods total `611`
- shared live-examples parity 보강(최신-89): Java `ElkLiveExamplesTest#noException` 대응 스모크로 `plugins/org.eclipse.elk.alg.layered/tests/shared_live_examples_smoke_test.rs`를 추가해 external example ELKT(`layered/core(box)/core(random)`)를 실제 로드 후 recursive layout 경로에서 예외 없이 완료되고 노드 geometry가 유한값으로 유지되는지 검증
- 최신 품질 재검증(최신-89): `cargo test -p org-eclipse-elk-alg-layered --test shared_live_examples_smoke_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-layered --test shared_live_examples_smoke_test -- -D warnings` 통과
- parity 수치 재동기화(최신-89): `sh scripts/check_java_test_module_parity.sh` 재실행 결과 direct-mapped `867/599`, layered `536/412`, common `51/37`, rust test methods total `878`, java test methods total `611`
- layered self-loop fallback 의존 축소(최신-90): `self_loop_issue_resources_test.rs`를 `run_layout_with_subgraph_fallback`(하위 노드 BFS 반복)에서 `run_layout_prefer_recursive`(recursive 우선 + 누락 시 루트 1회 direct 보정)로 전환하고, `issue_support.rs`의 `run_recursive_layout`에 계층 전체 기본 알고리즘 보정(`CoreOptions::ALGORITHM=org.eclipse.elk.layered`)을 추가해 core/recursive 경로 우선 검증으로 정리
- 최신 품질 재검증(최신-90): `cargo test -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test -- --test-threads=1`, `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test -- --test-threads=1` 통과
- node-promotion ptolemy coverage 확장(최신-91): `node_promotion_test.rs`에 모델 단위 정규화 수집(`collect_external_ptolemy_model_resources`, `.elkt` 우선 `.elkg` fallback)과 parse coverage 테스트(`node_promotion_external_ptolemy_model_parse_coverage_if_available`)를 추가하고, external 스캔/검증 상한·최소 기준을 상향(`120/140/128/160`, 최소 `8/10/8/8`, model/nikolov 최대 `20/20`)
- 최신 품질 재검증(최신-91): `cargo test -p org-eclipse-elk-alg-layered --test node_promotion_test -- --test-threads=1` 통과
- common label-placement inside 경로 보강(최신-92): `node_dimension_calculation.rs`에서 constrained inside/outside 케이스에 `PORT_BORDER_OFFSET` + node padding 기반 border offset 계산(`port_label_border_offset_for_port_side`)을 반영해 Java `PortLabelPlacementCalculator`의 inside cell 경계 오프셋 경로를 부분 포팅(비-constrained 기존 동작 유지로 회귀 방지)
- common 라벨 배치 회귀 테스트 정합화(최신-92): `port_label_placement_variants_test.rs`에 north/south inside 경계 clamp 허용 helper(`assert_centered_or_inside_clamped`)를 도입해 Java식 경계 보정이 적용되는 케이스를 안정적으로 검증
- 최신 품질 재검증(최신-92): `cargo test -p org-eclipse-elk-alg-layered --test port_label_placement_variants_test --test next_to_port_label_placement_test --test inside_port_label_test --test issue_701_test -- --test-threads=1` 통과
- common configurator/리소스 스윕 확대(최신-93): `common_port_label_external_sweep_test.rs` 신규 추가로 external core/layered port-label 리소스군(variants/next-to-port/treat-as-group/056/405/596/701)을 sweep 검증하고, `variants.elkt`에 E/W/N/S uniform side override를 적용하는 configurator 스타일 side sweep을 추가
- 최신 품질 재검증(최신-93): `cargo test -p org-eclipse-elk-alg-layered --test common_port_label_external_sweep_test -- --test-threads=1` 통과
- 최종 동기화/정적 검증(최신-94): `cargo clippy -p org-eclipse-elk-alg-common --lib -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `bash scripts/check_java_test_module_parity.sh`, `sh scripts/check_layered_issue_test_parity.sh` 재실행으로 수치/리포트 동기화
- parity 수치 재동기화(최신-94): `perf/java_test_module_parity.md` 기준 direct-mapped `870/599`, layered `539/412`, common `51/37`, rust test methods total `881`, java test methods total `611`, layered issue parity `41/41 (ok)`

- ptolemy coverage 자동 기록(최신-95): auto script(bash scripts/update_ptolemy_coverage_agents.sh)로 external ptolemy parse coverage(parsed=140/140, coverage=1.000)와 validated model 수(model-order checked=20/128)를 배치별 정량 기록
- common NodeLabelAndSizeCalculator 경로 보강(최신-96): `node_label_and_size_calculator.rs` 신규 추가로 inside node label grid cell 최소 크기 집계, `compute_inside_node_label_padding`, `compute_inside_node_label_container_minimum_size`를 포팅하고 `nodespacing` 모듈 export 연결
- layered importer padding 연동 및 회귀 테스트 추가(최신-96): `elk_graph_importer.rs#create_lgraph`에서 common `NodeLabelAndSizeCalculator` 결과를 graph padding에 합산하도록 반영, `nodespacing_test`(2개)와 `elk_graph_importer_padding_test` 신규 테스트로 계산/연동 회귀 검증
- recursive self-loop direct 보정 제거 시도/현황 기록(최신-97): `self_loop_issue_resources_test`에서 recursive-only(단일/재시도) 경로를 검증한 결과 `079/128/273/288/...` 외부 티켓에서 section 누락이 재현되어 direct fallback 의존이 여전히 필요함을 확인, 테스트 안정성을 위해 recursive 우선 + 누락 시 1회 direct 보정 경로 유지(TODO로 잔여)
- 최신 품질/동기화 재검증(최신-98): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test -- --test-threads=1`, `cargo test -p org-eclipse-elk-alg-layered --test {node_promotion_test,self_loop_issue_resources_test,elk_graph_importer_padding_test} -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-common --lib -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `sh scripts/check_java_test_module_parity.sh`, `sh scripts/check_layered_issue_test_parity.sh` 통과
- parity 수치 재동기화(최신-98): `perf/java_test_module_parity.md` 기준 direct-mapped `873/599`, layered `540/412`, common `51/37`, rust test methods total `884`, java test methods total `611`, layered issue parity `41/41 (ok)`
- common NodeLabelAndSizeCalculator process 경로 1:1 포팅 완료(최신-99): `node_label_and_size_calculator.rs`의 `process_node`에 inside/outside node label 배치, container 최소 크기/확장, node width/height 확정, `SizeOptions::ComputePadding` 적용, `configured_minimum_size` 기반 default minimum size 반영을 마무리
- recursive self-loop 근본 원인 수정 및 fallback 제거(최신-99): `LayoutMetaDataService`에 `override_algorithm_provider_pool`를 추가하고 `initialize_plain_java_layout`에서 `org.eclipse.elk.layered` provider pool을 `LayeredLayoutProvider`로 override하여 recursive 경로가 placeholder(Box) provider를 타지 않도록 수정, `self_loop_issue_resources_test`에서 direct fallback 없이 recursive-only 경로로 고정
- 최신 품질 재검증(최신-99): `cargo test -p org-eclipse-elk-alg-common --test nodespacing_test -- --test-threads=1`, `cargo test -p org-eclipse-elk-alg-layered --test self_loop_issue_resources_test -- --test-threads=1`, `cargo clippy -p org-eclipse-elk-alg-common --tests -- -D warnings`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `cargo clippy -p org-eclipse-elk-core --lib -- -D warnings`, `cargo test -p org-eclipse-elk-core --tests -- --test-threads=1`, `cargo test -p org-eclipse-elk-alg-layered --tests -- --test-threads=1`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace -- --test-threads=1` 통과
- parity 수치 재동기화(최신-99): `sh scripts/check_java_test_module_parity.sh`, `sh scripts/check_layered_issue_test_parity.sh` 재실행 결과 direct-mapped `875/599`, layered `540/412`, common `55/37`, `org.eclipse.elk.core` `184/96`, rust test methods total `886`, java test methods total `611`, layered issue parity `41/41 (ok)`
- external/elk-models Java↔Rust 결과 동등성 검증 환경 추가(최신-100): Java side exporter(`scripts/java/ElkModelParityExportTest.java`, `scripts/run_java_model_parity_export.sh`)로 `.elkt/.elkg` 모델을 입력 JSON/Java 레이아웃 JSON + `java_manifest.tsv`로 추출하고, Rust side replay runner(`plugins/org.eclipse.elk.graph.json/src/bin/model_parity_layout_runner.rs`)에서 동일 입력 JSON을 `RecursiveGraphLayoutEngine`으로 재레이아웃해 `rust_manifest.tsv`를 생성하는 파이프라인을 구축
- 동등성 비교/오케스트레이션 추가(최신-100): 수치 허용오차(abs tol) 기반 deep-compare 스크립트(`scripts/compare_model_parity_layouts.py`)와 통합 실행 래퍼(`scripts/run_model_parity_elk_vs_rust.sh`)를 추가하고 `scripts/README.md`에 실행 절차/strict gate(`MODEL_PARITY_STRICT`)를 문서화
- Java parity 실행 안정화(최신-101): `scripts/run_java_model_parity_export.sh`에 graph.json.test 번들 `MANIFEST.MF` 자동 패치(`org.eclipse.emf.ecore` 누락 시 Require-Bundle 삽입) 및 정리 복구를 추가해 Tycho 컴파일 access restriction을 해소했고, `JAVA_PARITY_MVN_ARGS="-Ddash.skip=true"` 조합으로 license-check 네트워크 의존 구간을 우회 가능하도록 실행 경로를 확정
- Java↔Rust 동등성 샘플 실검증(최신-101): `JAVA_PARITY_LIMIT=10` 스모크에서 Java export `success=10/10`, Rust replay `ok=10/10`까지 완료했으며, 비교 결과는 `matches=0, drift=10`으로 현재 산출 레이아웃이 샘플 기준 완전 동등하지 않음을 확인(`report.md`, `diff_details.tsv` 생성)
- strict gate 동작 검증(최신-101): `python3 scripts/compare_model_parity_layouts.py --strict` 재실행에서 drift 존재 시 exit code `1`을 반환함을 확인해 CI/자동화 fail-fast 경로가 의도대로 동작함을 검증
- parity 대형 샘플 실행 관찰(최신-102): `JAVA_PARITY_LIMIT=100` 실행에서 Java export는 `100/100` 성공했지만 Rust replay 단계가 장시간 종료되지 않고 산출물 `14`개에서 정체되어, 대형 코퍼스에서는 parity runner hang/장기 실행 이슈가 있음을 확인(추가 원인 분석 TODO)
- parity drift 분류 스크립트 강화(최신-103): `compare_model_parity_layouts.py`에 drift category 자동 분류(알고리즘/좌표/section-id/label/ordering) 추가, 10/10 모델 기준 0 drift 확인
- parity runner timeout/heartbeat 추가(최신-103): `model_parity_layout_runner.rs`에 모델별 120s timeout, 10s heartbeat, 중간 manifest flush 추가로 hang 모델 격리 및 대형 샘플 안정 실행 확보
- Mutex deadlock 수정(최신-104): `routing_slot_assigner.rs`의 `random_from_holder()`가 `elk_layered::layout_component()`에서 이미 잡고 있는 graph Mutex를 재획득 시도하여 deadlock 발생(Rust Mutex 비재진입). `assign_routing_slots` 시그니처를 `&mut Random` 파라미터로 변경하고, `self_loop_router.rs`에서 graph에서 Random을 미리 추출하여 전달하도록 수정. `insideSelfLoops.elkt`(5ms), `labels.elkt`(2ms)로 해결
- parity 10/10 완전 통과(최신-104): 샘플 10건 기준 10/10 ok, 0 drift, 0 error, 0 timeout 달성
- Layered.melk core 기본값 메타데이터 적용 보강(PRIORITY/TOPDOWN_NODE_TYPE/EDGE_ROUTING/PORT_BORDER_OFFSET/RANDOM_SEED/ASPECT_RATIO/SEPARATE_CONNECTED_COMPONENTS/PORT_ALIGNMENT_DEFAULT) 및 LayeredOptions alias 보강
- allowNonFlowPortsToSwitchSides FixedPos 케이스: FixedPos면 port side switching 차단(north_south_port_preprocessor + sweep_copy)으로 Java 테스트 기대값 복원
- 검증: `cargo test -p org-eclipse-elk-alg-layered --test allow_non_flow_ports_to_switch_sides_test`, `cargo test -p org-eclipse-elk-core --test layout_algorithm_metadata_test`, `cargo test -p org-eclipse-elk-alg-layered --test layered_metadata_test`
- timeout 36건 해소: crossmin `layer_index`를 `Layer.graph_element().id` 기반으로 변경해 graph lock 데드락 제거, `InteractiveCycleBreaker`가 graph lock 없이 reference point 계산하도록 수정
- model order timeouts 해소: `ModelOrderBarycenterHeuristic`에 `force_model_order/max_model_order_nodes/group_order_strategy` 캐시 도입으로 graph lock 제거, `LayerConstraint/InLayerConstraint` ElkReflect 등록
- 재검증: timeout 36건 목록 전부 `MODEL_PARITY_TIMEOUT_SECS=10` 내 통과(36/36 ok)
- 1,448 모델 Rust 재실행(2026-02-11): `rust_manifest.tsv` 갱신, timeouts=0/errors=0, compared=1439, matches=169, drift=1270, total_diffs=23793, `perf/model_parity_full/report.md`/`diff_details.tsv` 갱신
- layered LabelDummyInserter/LabelDummySwitcher 포팅 및 wiring: 라벨 더미 삽입/스위칭 로직, `LONG_EDGE_BEFORE_LABEL_DUMMY` 내부 프로퍼티 추가, `LabelDummySwitcher.INCLUDE_LABEL`을 `ElkGraphLayoutTransferrer`로 전달하도록 보강
- 테스트 추가/검증: `label_dummy_inserter_test`, `label_dummy_switcher_test` 신규 추가 후 `cargo test -p org-eclipse-elk-alg-layered --test label_dummy_inserter_test --test label_dummy_switcher_test` 통과
- label 관련 61개 모델 parity 스모크(2026-02-11): `/tmp/java_manifest_labels_local.tsv` 기반 Rust 재실행(ok=61, error=0) 후 비교 결과 matches=12, drift=49, total_diffs=854 (`/tmp/report_labels.md`, label/section/coordinate 비중 높음)
- common overlaps/cellsystem 보강: `RectangleStripOverlapRemover` 스캔라인 overlap 계산/전략 처리 보정, `LabelCell` 패딩 참조 처리 정리
- layered EndLabelPre/Postprocessor 포팅 및 연결: `END_LABELS`/`MAX_EDGE_THICKNESS` 내부 프로퍼티, `PortRefKey`/LabelCell map, `LGraphAdapters::adapt_label` 추가, IntermediateProcessorStrategy wiring
- 테스트 실행: `cargo test -p org-eclipse-elk-alg-layered --test intermediate_processors_test --test issue_541_test` 통과(기존 경고 유지)
- label 관련 61개 모델 parity 재스모크(2026-02-11): Rust 재실행 ok=61, 비교 결과 matches=12, drift=49, total_diffs=854 (`/tmp/report_labels_after_endlabels.md`, 변화 없음)
- JSON importer enum 파싱 검증 추가: `edge_label_placement_option_parses_enum` 테스트로 `edgeLabels.placement=TAIL` 파싱 확인
- layered EndLabels 그래프 프로퍼티 테스트 추가: `end_label_graph_properties_test`에서 `initialize_plain_java_layout()` 후 `GraphProperties::EndLabels` 포함 확인
- label 관련 61개 모델 parity 재실행(2026-02-11, release): `/tmp/java_manifest_labels.tsv` 기반 Rust 재실행 ok=58/timeout=3 후 비교 결과 matches=21, drift=37, total_diffs=582 (`/tmp/report_labels_after_endlabels_v2.md`, `layerSelection_center_{01,02}`/`layerSelection_widest` timeout)
- LabelDummySwitcher long-edge 더미 순회 루프 가드 추가(NodeRefKey HashSet)로 순환 대비; `layerSelection_center_01` 단건 재실행에서도 LabelDummySwitcher 단계 timeout 지속(원인 추가 분석 필요)
- LabelDummySwitcher 데드락 해소: graph.lock 재진입 제거(레이어/spacing 캐시, `find_max_non_dummy_node_width` 방향 인자화)로 `layerSelection_center_{01,02}`/`layerSelection_widest` 3/3 ok 확인
- label subset 61개 parity 재실행(2026-02-11, release): ok=61/timeout=0, matches=21, drift=40, total_diffs=614 (`/tmp/report_labels_after_deadlock_fix.md`)
- LabelAndNodeSizeProcessor 포트 최소 길이 보정: PortAlignment::Distributed가 port spacing을 양끝에 추가하도록 반영 → `treat_as_group_inside` 등 포트 라벨 케이스 높이/포트 위치 복구, label subset total_diffs 614→598 (`/tmp/report_labels_after_portspacing_fix.md`)
- clippy 경고 정리(2026-02-12): layered/rectpacking/common lint 정리(불필요 borrow/mut/ptr_arg, entry API 적용, type alias 도입, map/clone 제거, allow(`mutable_key_type`) 모듈 보강) 및 테스트 경고 정리
- 단계 재검증(2026-02-12): `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests` 통과
- 워크스페이스 검증 보강(2026-02-12): `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` 통과. nodespacing `constrained_outside_north_port_labels_stack_without_overlap`는 `SizeConstraint::PortLabels` 조건 하에서만 스택되도록 테스트 조건 정합화, core ignored 테스트/graph.json 테스트 clippy 경고 정리
- drift 케이스 보정(2026-02-12): self-loop orthogonal 라우팅에서 동일 포트 self-loop가 동일 좌표의 중복 bendpoints를 유지하도록 `SelfLoopRouter` dedup 제거 및 회귀 테스트(`self_loop_router_keeps_duplicate_bend_points_for_same_port`) 추가
- 단계 재검증(2026-02-12): `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests` 통과
- 모델 parity full 재실행(2026-02-12): `JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity` + `JAVA_PARITY_MVN_ARGS=-Ddash.skip=true`로 `scripts/run_model_parity_elk_vs_rust.sh` 실행, Java export success=1439/1448(9 java_non_ok), Rust replay ok=1439/1448(errors=0), 비교 결과 matches=293, drift=1146, total_diffs=21360 갱신(`perf/model_parity_full/report.md`, `diff_details.tsv`, `rust_manifest.tsv`)
- 단계 재검증(2026-02-12): `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests` 통과
- drift 분류 재분석(2026-02-12): `scripts/analyze_layered_drift.py` 기준 drift 1146건 중 other 479, ordering_diff 403, layering_diff 264. 최소 diff_count 샘플로 `tests/layered/edge_label_placement/hierarchy_center_edge_label_problem.elkt`, `tickets/layered/040_includeChildrenWithFixedOrder.elkt`, `tickets/layered/425_selfLoopInCompoundNode.elkt` 등 좌표/section 불일치 우선 후보 확인
- drift root cause 1차 점검(2026-02-12): `hierarchy_center_edge_label_problem`/`040_includeChildrenWithFixedOrder`는 hierarchical edge에서 `sections` 누락 + 노드 순서/크기 차이가 함께 발생(Edge routing/transfer 경로 의심). `425_selfLoopInCompoundNode`는 compound 내부 노드 최소 크기 미적용 + self-loop edge container/sections 불일치 확인. 회귀 테스트 후보: 위 2~3개 ELKT 기반으로 edge `sections` 존재, node size/edge container 일치 검증 추가 예정
- hierarchical edge `sections` 누락 보정(2026-02-12): `ElkGraphLayoutTransferrer`에서 port outgoing/incoming 모두 수집하고, target/source가 하위 그래프 노드일 때 parent port도 포함하도록 보강해 hierarchical edge도 `sections` 전송되도록 수정
- 회귀 테스트 추가(2026-02-12): `hierarchical_edge_sections_test`로 `hierarchy_center_edge_label_problem`/`040_includeChildrenWithFixedOrder` ELKT 로드 후 edge `sections` 존재 검증, `cargo test -p org-eclipse-elk-alg-layered --test hierarchical_edge_sections_test` 및 `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- subset parity 재검증(2026-02-12): 위 2개 모델 Rust 재실행/비교 결과 matches=0, drift=2, total_diffs=6 (`/tmp/elk-parity-subset/report_subset.md`), `sections`는 생성되었으나 node x/graph width/section endPoint x 차이로 drift 지속 확인
- hierarchical external port 더미 보강(2026-02-12): `ensure_external_port_dummy`에서 port constraints/position/size를 실제 port/parent에서 반영하고, 외부 포트 생성 시 graph `PORT_CONSTRAINTS`를 Java와 동일하게 `FIXED_SIDE`/`FREE`로 조정하여 hierarchical port 제약 처리 정합성 보강
- subset parity 재검증(2026-02-12): 동일 2개 모델 Rust 재실행/비교 결과 matches=0, drift=2, total_diffs=6 (`/tmp/elk-parity-subset/report_subset_v3.md`), 좌표/section/width drift 지속 확인
- 계층 edge 좌표 drift(±5) 원인 해결(2026-02-12): `LabelAndNodeSizeProcessor::adjust_ports_on_side`가 `PORT_BORDER_OFFSET`를 무시해 hierarchical port offset(-5)이 소실되던 문제 수정(N/E/S/W에 border offset 반영), `040_includeChildrenWithFixedOrder`/`hierarchy_center_edge_label_problem` Java↔Rust match 확인, `cargo test -p org-eclipse-elk-alg-layered --tests`/`cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- 모델 parity full 재실행(2026-02-12): `scripts/run_model_parity_elk_vs_rust.sh` 결과 compared=1439, matches=296, drift=1143, total_diffs=21346 (`perf/model_parity_full/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신). 코드 변경 없음으로 `cargo test`/`cargo clippy`는 생략하고, 기록 갱신은 `AGENTS.md` 커밋으로 남김
- drift 재분류 갱신(2026-02-12): drift 1,143건 중 other 515, ordering_diff 336, layering_diff 292. 최소 diff_count 샘플은 계층 실세계 모델에서 edge `sections` 누락(예: `realworld/ptolemy/hierarchical/*TerrainModel*`, diff=6)과 일부 ordering/label/section id 불일치 확인
- 계층 그래프 edge `sections` 누락 재보정(2026-02-12): `ElkGraphLayoutTransferrer`에서 그래프 내 LEdge origin 매핑을 보강하고 fallback에서 port identifier로 실제 port를 재해석하도록 수정, `realworld/ptolemy/hierarchical/*TerrainModel*.elkt` 3개 샘플 재실행 시 `missing keys on right: sections` 0건 확인(`/tmp/elk_parity_hier_sections_*`). `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- 포트 배치 parity 보강(2026-02-12): `LabelAndNodeSizeProcessor`에 Java `PortPlacementCalculator` 규칙 반영(라벨 마진/spacing/`PORTS_OVERHANG`/균등 간격, `FIXED_RATIO` 반영) 및 `port_side_view` 캐시 안전화. `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings`, `cargo test -p org-eclipse-elk-alg-layered --tests` 통과. TerrainModel 단일 parity 재검증 결과 diff 8 유지(`model_parity_layout_runner` + `compare_model_parity_layouts.py`)로 포트 위치/section drift 지속 확인
- TerrainModel subset parity 재검증(2026-02-13): 계층 TerrainModel 6개 모델 재실행 결과 matches=0, drift=6, total_diffs=120(좌표 66, section 54). 보고서: `/tmp/elk_parity_terrain_subset/report.md`, 세부: `/tmp/elk_parity_terrain_subset/diff_details.tsv`
- 계층 외부 포트 importer 보강(2026-02-12): `ElkGraphImporter`에 External Port 체크/변환/포트 side 정의, net flow/label spacing 반영 및 더미 매핑 추가. `compound_external_port_side_test` 추가, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- 모델 parity full 재실행(2026-02-13): `JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full` + `JAVA_PARITY_MVN_ARGS=-Ddash.skip=true`로 `scripts/run_model_parity_elk_vs_rust.sh` 실행. Java export `success=1439/1448`(java_non_ok=9), Rust replay `ok=1426/1448`(errors=13, timeouts=2). 비교 결과 matches=314, drift=1112, total_diffs=21096(`perf/model_parity_full/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신). `crossings_counter.rs:339` index out of bounds panic 2건(`193_hierarchicalPortOverlaps`, `685_hierarchicalSpike`) 확인. 코드 변경 없음으로 `cargo test`/`cargo clippy`는 생략하고 기록만 갱신
- crossings_counter 빈 레이어 panic 수정(2026-02-13): `init_positions`가 empty nodes에서 역순 루프 진입하지 않도록 guard 추가, 회귀 테스트 `init_for_counting_between_handles_empty_layer` 보강. `cargo test -p org-eclipse-elk-alg-layered --test crossings_counter_test`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- external port dummy/orthogonal 라우팅 deadlock 수정(2026-02-13): `label_next_to_port`가 `is_connected_to_external_nodes` 재진입 락을 잡지 않도록 분리, `HierarchicalPortOrthogonalEdgeRouter`의 north/south dummy 좌표/고정 좌표 보정에서 `border_to_content_area_coordinates` 재락 제거(그래프 padding/offset 직접 적용). `ELK_TRACE_HIER_PORT_ORTHO` 단계 로그 추가, `label-node-size` phase 로그 보강. 회귀 유닛 테스트 `label_next_to_port_outside_path_does_not_deadlock`, `hierarchical_port_position_processor_does_not_deadlock_with_graph_lock` 추가
- timeout 2건 재검증(2026-02-13): `/tmp/java_manifest_timeouts.tsv`(491_portSpacing, 194_excessiveWhiteSpace) 기준 `MODEL_PARITY_TIMEOUT_SECS=10` 실행 결과 ok=2/2, timeouts=0 확인. `cargo test -p org-eclipse-elk-alg-layered --lib`, `cargo clippy -p org-eclipse-elk-alg-layered --lib --tests -- -D warnings` 통과
- 모델 parity full 재실행(2026-02-13): `JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full` + `JAVA_PARITY_MVN_ARGS=-Ddash.skip=true`로 `scripts/run_model_parity_elk_vs_rust.sh` 실행. Java export `success=1439/1448`(java_non_ok=9), Rust replay `ok=1436/1448`(errors=3, timeouts=0), 비교 결과 matches=304, drift=1132, total_diffs=21455(`perf/model_parity_full/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신). 코드 변경 없음으로 `cargo test`/`cargo clippy`는 생략하고 기록만 갱신
- parity 결과 카테고리 분해 및 우선순위 재정의(2026-02-13): examples 45( match 27, drift 18), tests 193( compared 182, match 51, drift 131, skip_non_ok 11=java_non_ok 8 + rust_error 3), tickets 110( compared 109, match 42, drift 67, skip_non_ok 1), realworld 1100( match 184, drift 916). 우선순위: 1) non-ok 모델 해소(테스트 3개 panic, Java non-ok 9개) 2) tests drift 축소 3) tickets drift 축소 4) realworld 드리프트 root cause 샘플링/수정 5) examples 마무리. 분석/문서 갱신만으로 `cargo test`/`cargo clippy`는 생략
- non-ok 모델 해소 1차(2026-02-13): BK aligner/threshold에서 타 그래프 노드 인덱스 참조 시 OOB 방지(이웃 필터 + 범위 가드)로 `tests/layered/connected_components/compound05.elkt`, `tests/layered/hierarchical_ports/hierarchy05.elkt`, `tests/layered/hierarchical_ports/hierarchy06.elkt` panic 제거(3/3 ok). Java non-ok 9개는 `perf/model_parity_full/java_exclude.txt` 추가 및 Java export exclude 지원(`elk.parity.exclude`)으로 다음 parity 실행에서 제외되도록 정리
- 단계 재검증(2026-02-13): `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- 계층 edge fallback 보강(2026-02-13): `ElkGraphLayoutTransferrer`에 nested node 포함 재귀 fallback section 적용(`apply_fallback_sections_recursive`)을 추가해 nested edge `sections` 누락을 보정. subset parity(계층 포트/edge_label_placement 샘플)에서 `sections` 누락 해소 확인(좌표 drift 잔존)
- tests 카테고리 parity 재실행(2026-02-13): Java export success=185/193(java_non_ok=8), Rust replay ok=185, matches=51, drift=134, total_diffs=2301 (`perf/model_parity_tests/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신)
- 단계 재검증(2026-02-13): `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- tickets drift 보정 1차(2026-02-13): `COORDINATE_SYSTEM_ORIGIN` 내부 프로퍼티와 `find_coordinate_system_origin`/계층 오프셋 계산을 포팅해 계층 엣지 좌표 보정. `ElkGraphLayoutTransferrer`에 `calculate_hierarchical_offset` 적용. `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과. tickets parity 재실행: Java export success=109/110(java_non_ok=1), Rust replay ok=109, matches=42, drift=67, total_diffs=860 (`perf/model_parity_tickets/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신). `.gitignore`에 `perf/model_parity_*/{java,rust}` 및 JSON 출력 제외 추가
- realworld drift 샘플링 및 부분 재검증(2026-02-13): `JAVA_PARITY_LIMIT=50 JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full JAVA_PARITY_MVN_ARGS=-Ddash.skip=true sh scripts/run_model_parity_by_category.sh realworld` 실행, Java export success=50/50, Rust replay ok=50, 비교 결과 matches=12, drift=38, total_diffs=734 (`perf/model_parity_realworld/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신). `scripts/analyze_layered_drift.py`로 drift 38건 분류(ordering_diff 24, layering_diff 10, other 4). `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과. TODO: realworld 상위 drift root cause 보정(특히 ordering/layering) - `scripts/compare_model_parity_layouts.py`로 상위 10~20 모델 상세 비교 후 관련 프로세서/옵션 기본값 점검
- 전체 parity full 재실행(2026-02-13): `JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full JAVA_PARITY_MVN_ARGS=-Ddash.skip=true sh scripts/run_model_parity_elk_vs_rust.sh` 실행. Java export success=1439/1448(java_non_ok=9), Rust replay ok=1439, skipped=9, matches=316, drift=1123, total_diffs=21279 (`perf/model_parity/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신). `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- parity 결과 카테고리별 재분해 및 우선순위 갱신(2026-02-13): examples 45(match 28, drift 17), tests 193(ok 185, match 51, drift 134, java_non_ok 8), tickets 110(ok 109, match 43, drift 66, java_non_ok 1), realworld 1100(match 194, drift 906). diff_total 비중: realworld 84.1%, tests 10.8%, tickets 4.0%, examples 1.1%. 우선순위: 1) realworld(대부분의 diff 차지) root cause 보정 2) tests drift 축소 3) tickets drift 축소 4) examples 정리. `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests -- -D warnings` 통과
- 외부 포트 connected components 배치 로직 보강(2026-02-13): ComponentGroupGraphPlacer/ModelOrderRowGraphPlacer 포팅, external ports 존재 시 graph placer 분기 적용(그룹 모델 오더는 TODO). `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- tests 카테고리 parity 재실행(2026-02-13): Java export success=185/193(java_non_ok=8), Rust replay ok=185, matches=51, drift=134, total_diffs=2291 (`perf/model_parity_tests/report.md`, `diff_details.tsv` 갱신). `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- Step 25 모델 오더 외부 포트 그룹 배치 보강(2026-02-13): `ModelOrderComponentGroup`/`ComponentGroupModelOrderGraphPlacer` 포팅(추가 제약/그룹 마지막만 확장), `component_group_test`에 모델 오더 제약 테스트 추가. `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과. tests parity 재검증: Java export success=185/193(java_non_ok=8), Rust replay ok=185, matches=51, drift=134, total_diffs=2286 (`perf/model_parity_tests/report.md`, `diff_details.tsv` 갱신)
- Step 26 전체 parity full 재실행(2026-02-13): `JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full` + `JAVA_PARITY_MVN_ARGS=-Ddash.skip=true`로 `scripts/run_model_parity_elk_vs_rust.sh` 실행. Java export success=1439/1448(java_non_ok=9), Rust replay ok=1439, matches=305, drift=1134, total_diffs=21476 (`perf/model_parity/report.md`, `diff_details.tsv`, `rust_manifest.tsv` 갱신). `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- Step 27 drift 상위 원인 재분류/realworld 상위 비교(2026-02-13): `scripts/analyze_layered_drift.py`로 전체/realworld 분류(전체 drift 1134 -> other 537, layering_diff 299, ordering_diff 298; realworld drift 916 -> other 374, layering_diff 286, ordering_diff 256) 및 결과(`perf/model_parity/drift_summary.txt`, `drift_summary_realworld.txt`) 기록. realworld drift 상위 15건 추출(`realworld_drift_top15.tsv`, `rust_manifest_realworld_top15.tsv`) 후 `scripts/compare_model_parity_layouts.py`로 상세 비교(`realworld_top15_report.md`, `realworld_top15_diff_details.tsv`, total_diffs=2136, section/coordinate 중심) 완료. 우선순위 갱신: edge section start/end/bendPoints 좌표와 node x/y 좌표 편차 집중
- Step 28 ordering diff 샘플 추출(2026-02-13): `scripts/analyze_layered_ordering_diff.py` 추가 후 realworld 상위 30건 ordering diff 레이어 순서 추출(`perf/model_parity/ordering_diff_realworld_top30.tsv`) 완료
- Step 29 ordering/layering drift 대표 모델 최소 재현/원인 수정(2026-02-13): `PortListSorter`에서 west/south range reverse 조건을 Java와 동일하게 길이 > 2일 때만 반전하도록 수정(2개 포트는 입력 순서 유지). `tests/resources/ordering/selfloop_crash.elkt` 리소스 추가 및 `ordering_selfloop_crash_test`에서 n4가 n3 위에 위치하는 ordering 불변식 검증. subset parity 재검증: `model_parity_layout_runner` + `compare_model_parity_layouts.py`로 selfloop_crash ordering 일치 확인(좌표 drift 20개는 잔존). `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- Step 30 ordering 안정성 보강(2026-02-13): 컴포넌트 정렬 tie-breaker(입력 순서 유지) 및 barycenter 정렬 안정화(동일 barycenter 시 입력 순서 유지) 적용. `components_processor` 유닛 테스트 추가(`sort_components_by_priority_keeps_input_order_for_ties`). subset parity(ptolemy `aspect_compositeqm_CheckExecutionTimeConstraints`) 재실행 결과 N9/N10 ordering drift는 잔존(원인 추가 분석 TODO). `cargo test -p org-eclipse-elk-alg-layered`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- Step 31 ordering diff 대표 모델 root cause 수정(2026-02-14): 대표 모델 `realworld/ptolemy/flattened/aspect_compositeqm_CheckExecutionTimeConstraints`에서 P2 정상/P3 역전 현상을 재현한 뒤, 원인을 `Random::next_long()`의 Java `Random.nextLong()` 부호 확장 불일치로 확인. `plugins/org.eclipse.elk.core/src/org/eclipse/elk/core/util/mod.rs`에서 `next_long`을 Java 식(`((long)next(32) << 32) + next(32)`)과 동일하게 보정. 회귀 테스트 `ordering_realworld_model_test.rs`의 `#[ignore]` 제거 후 통과 확인
- Step 32 ordering_diff_realworld_top30 subset parity 재검증(2026-02-14): `perf/model_parity/ordering_diff_realworld_top30.tsv` 기반 subset manifest를 구성해 parity 재실행. 로컬 입력 JSON이 존재하는 20건(원본 30건 중 10건 파일 부재) 대상 `model_parity_layout_runner --release` + `compare_model_parity_layouts.py` 수행. 결과: compared=20, matches=0, drift=20, errors=0, total_diffs=400. 분류: coordinate 352(88.0%), section 40(10.0%), ordering 4(1.0%), structure 4(1.0%). 산출물 갱신: `perf/model_parity_ordering_top30/{java_manifest.tsv,java_manifest_existing.tsv,rust_manifest.tsv,report.md,diff_details.tsv,ordering_diff_samples.tsv}` (`ordering_diff_samples.tsv` 기준 ordering diff 모델 16건)
- Step 33 ordering_diff_realworld_top30 재검증(2026-02-14): Step 31(Random `next_long` Java 정합) 반영 후 동일 subset(20건) parity 재실행. 결과: compared=20, matches=2, drift=18, errors=0, total_diffs=360. 분류: coordinate 332(92.2%), section 20(5.6%), ordering 4(1.1%), structure 4(1.1%). `aspect_compositeqm_CheckExecutionTimeConstraints.{elkg,elkt}` 2건 match 전환 확인, `ordering_diff_samples.tsv` 모델 수 16→14 감소
- Step 34 착수 분석(2026-02-14): 잔여 대표 모델 `algebraic_heateropentank_HeaterOpenTankRefactored.elkt` 단건 실험(기본 vs `crossingMinimization.strategy=NONE`) 수행. `N1/N10/N8`의 y-순서가 Java=`N1,N10,N8`, Rust 기본=`N10,N8,N1`, Rust NONE=`N1,N8,N10`으로 확인되어 P3 영향은 존재하나 NONE에서도 Java와 불일치(추가 원인 존재) 상태로 root cause 수정은 계속 진행 필요
- Step 34 완료(2026-02-14): `LayerSweepCrossingMinimizer::save_all_node_orders_of_changed_graphs`가 Java와 달리 `currentlyBest`가 아닌 `current` 스냅샷을 best로 저장하던 불일치를 수정(현재 best 스윕 우선 저장, fallback은 current). 회귀 테스트 `ordering_realworld_model_test.rs`에 `ordering_realworld_ci_router_drop_queue_matches_java` 추가. `ordering_top30` subset 재검증 결과 `matches=6`, `drift=14`, `total_diffs=278`로 개선(기존 `2/18/360`), `ci_router_dropqueuetest1.{elkg,elkt}` + `ci_router_queuetest1.{elkg,elkt}` 4건 match 전환 확인. 대표 모델 `algebraic_heateropentank_*`는 ordering drift 일부가 잔존하여 추가 원인 분석을 다음 단계로 이관
- Step 35 완료(2026-02-14): `algebraic_heateropentank_HeaterOpenTankRefactored`의 랜덤 상태 전이 가설을 검증. Java `Random` 기준값(`seed=7564655870752979346`, `distributorNodeRelative=false`, `firstSweep=true`)을 Rust와 대조해 동일함을 확인(랜덤 전이 자체는 Java 정합). 회귀 테스트 보강: `ordering_realworld_random_sequence_matches_java_reference`(활성) 추가, 모델 Java 기대 순서 회귀 `ordering_realworld_algebraic_heater_open_tank_matches_java`를 `#[ignore]`+TODO로 추가. 단건 실험에서 추가 스윕을 제외하면 `N1/N10/N8`, `N13/N4`는 Java와 정합되고 `N5/N6`만 잔존해, 다음 단계 원인을 `P3 추가 스윕 시 crossing-count 평가 경로`로 좁힘
- Step 36 완료(2026-02-14): `algebraic_heateropentank_HeaterOpenTankRefactored` ordering drift root cause를 `NorthSouthPortPreprocessor`의 `IN_LAYER_LAYOUT_UNIT` 설정 조건 불일치로 확정(Java는 `FixedSide` 정상 노드에 대해 north/south 포트가 없어도 layout unit 설정, Rust는 north/south 포트가 있을 때만 설정). Rust를 Java와 동일하게 수정해 Forster constraint graph의 누락 제약(`pump -> TIn/north4`)이 복구되도록 보정. 회귀 테스트 활성화: `ordering_realworld_algebraic_heater_open_tank_matches_java`의 `#[ignore]` 제거 후 통과, 추가로 `north_south_preprocessor_sets_layout_unit_for_fixed_side_node_without_north_south_ports` 테스트를 추가해 재발 방지. 검증: `cargo test -p org-eclipse-elk-alg-layered --test north_south_port_preprocessor_test`, `cargo test -p org-eclipse-elk-alg-layered --test ordering_realworld_model_test`, `cargo test -p org-eclipse-elk-alg-layered --tests`, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과
- Step 37 완료(2026-02-14): Step 36 반영 후 `ordering_diff_realworld_top30` subset parity 재검증 수행. `model_parity_layout_runner --release` + `compare_model_parity_layouts.py` 재실행 결과가 `matches=12`, `drift=8`, `total_diffs=116`으로 개선(기존 `6/14/278`). `ordering_diff_samples.tsv` 재생성 결과 top-level ordering diff 모델은 `0`건으로 축소되었고, 잔여 drift는 4개 모델(`algebraic_heateropentank_HeaterOpenTankRefactored`, `backtrack_ramprollback_RampRollback`, `ca_conway_Conway`, `comm_trellisdecoder_TrellisDecoder`)의 section/coordinate/structure 중심으로 압축
- Step 38~39 완료(2026-02-14): root cause를 `HyperedgeDummyMerger` 미구현(`IntermediateProcessorStrategy::HyperedgeDummyMerger`가 `NoOp`)으로 확정. Java `HyperedgeDummyMerger` 로직을 Rust로 포팅(`hyperedge_dummy_merger.rs` 신규, `intermediate/mod.rs` export, `intermediate_processor_strategy.rs` wiring)해 long-edge dummy 병합이 실제 수행되도록 수정. `ca_conway_Conway` 단건 parity 재검증에서 `E33/E34` junction/bend 불일치 해소(`matches=1`, `drift=0`, `total_diffs=0`) 확인. 회귀 테스트 추가: `ordering_realworld_model_test.rs`의 `ordering_realworld_ca_conway_junction_points_match_java` (E33 4 bends+1 junction, E34 2 bends+0 junction) 통과
- Step 40 완료(2026-02-14): `ordering_diff_realworld_top30` subset(로컬 존재 20건) parity 재실행. `model_parity_layout_runner --release` + `compare_model_parity_layouts.py` 결과 `matches=20`, `drift=0`, `total_diffs=0` 달성. 산출물 갱신: `perf/model_parity_ordering_top30/{rust_manifest.tsv,report.md,diff_details.tsv,rust/**}`
- Step 41 완료(2026-02-14): full parity 재실행(`scripts/run_model_parity_elk_vs_rust.sh`)로 최신 기준선을 갱신. 네트워크/락 이슈를 우회하기 위해 `/tmp/elk-m2-parity-full` 로컬 Maven repo를 `~/.m2/repository`에서 동기화한 뒤 `JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full`, `JAVA_PARITY_MVN_ARGS='-Ddash.skip=true -o'`로 실행. 결과: total=1,448, compared=1,439, matches=658, drift=781, skipped=9(java_non_ok), errors=0(timeouts=0), total_diffs=14,676. 이전 full 기준(305/1,439, diffs 21,476) 대비 matches +353, drift -353, total_diffs -6,800 개선
- Step 42 완료(2026-02-14): `scripts/analyze_layered_drift.py` 재실행으로 full parity drift(781) 재분류를 갱신. 분류 결과 `other=509`, `layering_diff=217`, `ordering_diff=55`이며 realworld는 `other=358`, `layering_diff=204`, `ordering_diff=24`로 확인. low-diff 상위 원인(자기루프 polyline bendPoints 길이 불일치)을 Step 43 우선 대상으로 확정하고 요약 산출물(`perf/model_parity/drift_summary_step42.txt`, `drift_summary_realworld_step42.txt`, `drift_summary_non_realworld_step42.txt`)을 추가
- Step 43 완료(2026-02-14): root cause를 `SelfLoopRouter`에서 polyline 전용 corner-cut(`PolylineSelfLoopRouter::cut_corners`) 후처리가 누락된 것으로 확정하고 Java 경로와 동일하게 source/target anchor 포함 후처리를 연결. 회귀 테스트 `polyline_self_loop_parity_model_test.rs` 신규 추가(079 self-loop labels=4 bends, 463 self-loops=8 bends) 및 `self_loop_router_test` 재검증 통과. subset parity(`perf/model_parity_step43_self_loop`) 결과 `compared=2`, `matches=2`, `drift=0`, `total_diffs=0` 확인
- Step 44 완료(2026-02-14): Step 43 반영 후 full parity 재실행(`JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full`, `JAVA_PARITY_MVN_ARGS='-Ddash.skip=true -o'`). 결과: total=1,448, compared=1,439, matches=682, drift=757, skipped=9(java_non_ok), errors=0(timeouts=0), total_diffs=14,234. Step 41 대비 `matches +24`, `drift -24`, `total_diffs -442` 개선
- Java parity 실행 안전 가드 추가(2026-02-14): `run_java_model_parity_export.sh`에 `JAVA_PARITY_REQUIRE_CLEAN_EXTERNAL_ELK=true` 기본 가드를 추가해 `external/elk`가 dirty일 때 실행을 즉시 중단하도록 보강. 우회가 필요한 경우 `JAVA_PARITY_REQUIRE_CLEAN_EXTERNAL_ELK=false`로 비활성화 가능하도록 했고, `scripts/README.md` Model parity env/노트에 사용법을 문서화
- Step 45 완료(2026-02-14): full parity drift 재분류 재실행(`scripts/analyze_layered_drift.py --manifest perf/model_parity/rust_manifest.tsv`). 결과: drift 757건(`other=488`, `layering_diff=215`, `ordering_diff=54`), prefix별 `examples=14`, `realworld=564`, `tests=123`, `tickets=56`. 저diff `other` 우선 타깃으로 `tickets/layered/476_multiLabelInHorizontalLayout.elkt`, `476_multiLabelInVerticalLayout.elkt`를 선정
- Step 46 완료(2026-02-14): section identifier drift root cause 보정. `ElkGraphLayoutTransferrer`가 edge section 재생성 시 기존 section identifier를 유지하도록 수정하고(`elk_graph_layout_transferrer.rs`), JSON transfer fallback이 `section.identifier()`를 우선 사용하도록 수정(`json_importer.rs`). 회귀 테스트 `edge_section_identifier_parity_model_test.rs` 추가, subset parity(`perf/model_parity_step46_section_id`) 결과 `compared=2`, `matches=2`, `drift=0`, `total_diffs=0`
- Step 47 완료(2026-02-14): full parity 재실행(`JAVA_PARITY_MVN_LOCAL_REPO=/tmp/elk-m2-parity-full`, `JAVA_PARITY_MVN_ARGS='-Ddash.skip=true -o'`, `scripts/run_model_parity_elk_vs_rust.sh`). 결과: total=1,448, compared=1,439, matches=647, drift=792, skipped=9(java_non_ok), errors=0(timeouts=0), total_diffs=14,927. Step 44 대비 `matches -35`, `drift +35`, `total_diffs +693` (개선 3건: tickets 368/476, 회귀 38건은 realworld 중심)
- Step 48 완료(2026-02-14): Java `ElkGraphUtil.firstEdgeSection(edge, true, true)` 정합을 맞추기 위해 `ElkGraphLayoutTransferrer`의 section 처리 로직을 "첫 section 재사용 + geometry reset + 나머지 제거"로 변경. `cargo test -p org-eclipse-elk-alg-layered --test edge_section_identifier_parity_model_test`/`cargo clippy -p org-eclipse-elk-alg-layered --test edge_section_identifier_parity_model_test` 통과. 동일 Java 기준(`perf/model_parity/java/java_manifest.tsv`)으로 Rust-only parity 재실행 결과 `matches=647`, `drift=792` 유지, `total_diffs=14927 -> 14926` 1건 감소(예: `724_includeChildrenModelOrder`의 missing bendPoints 해소). 참고: `cargo test -p org-eclipse-elk-graph-json --test edge_coords_test edge_coords_round_trip`는 기존과 동일하게 실패 상태 유지(원인 별도 추적 필요)
- Step 49 완료(2026-02-14): Step47 회귀 38건 root cause를 Java baseline 컨텍스트 의존성으로 확정. 회귀 목록(38) include 기반 Java export를 2회 실행(`perf/model_parity_step49_java_a`, `perf/model_parity_step49_java_b`)한 결과 A/B는 38/38 동일(hash 동일)이나, full baseline(`perf/model_parity/java/layout`)과는 24/38 모델이 상이함을 확인. 동일 subset에서 Rust 재검증(`perf/model_parity_step49_rust_vs_java_subset`) 시 `matches=20`, `drift=18`, `total_diffs=360`으로 회귀 목록이 baseline 의존적임을 재현
- Step 50 완료(2026-02-14): 반복 parity에서 Java 기준선 흔들림을 방지하기 위해 `scripts/run_model_parity_elk_vs_rust.sh`에 `MODEL_PARITY_SKIP_JAVA_EXPORT` 옵션 추가(기본 `false`). `true`일 때 기존 `perf/model_parity/java/java_manifest.tsv`를 재사용하고, manifest 부재 시 즉시 실패하도록 가드 추가. `scripts/README.md` Model parity env/노트에 사용법 문서화
- Step 51 완료(2026-02-14): `MODEL_PARITY_SKIP_JAVA_EXPORT=true`로 full parity 재실행해 고정 Java baseline 기준 결과 재확인. 결과는 `matches=647`, `drift=792`, `total_diffs=14926`, `errors=0`, `timeouts=0`, `java_non_ok=9`로 유지
- Step L1-1 완료(2026-02-16): `edgeLabelShouldBeCentered` 1-diff의 root cause를 `HorizontalCompactor` 미구현(`IntermediateProcessorStrategy::HorizontalCompactor`가 `NoOp`) 상태에서 center-label dummy X가 보정되지 않는 문제로 확정. 즉시 완전 포팅 대신 `LabelDummyRemover`에 compaction-aware 보정(LEFT/RIGHT에서 인접 source/target 노드와 `InternalProperties::SPACINGS` 기반 bound 계산) 추가. 단건 parity 재검증(`tests/layered/compaction_oned/labels/edgeLabelShouldBeCentered.elkt`)에서 `x=152 -> 52`로 match 전환 확인, low-diff subset(9건) 재실행 결과 `matches=1`, `drift=8`, `total_diffs=31`로 갱신(`report: /tmp/elk_l1_low_subset_after/report.md`)
- Step L1-2 완료(2026-02-16): `AdaptiveCarWashFSM(elkg/elkt)` 2건의 `routing slot y (+80)` drift를 self-loop opposing route 선택의 port ID 기준 불일치로 확정. Rust `RoutingDirector::assign_port_ids`가 LGraph 현재 포트 순서(일부 케이스에서 Java와 역순)에 의존해 `East->West`를 option1으로 선택하던 경로를, `LayeredOptions::PORT_INDEX` 우선 정렬 후 연속 ID 재부여로 보정해 Java와 동일한 `West->East` 경로(북쪽 lane) 선택으로 일치시킴. 재검증: `/tmp/adaptive_pair/report.md` 기준 2/2 match, low-diff subset(9건) 재실행 결과 `matches=3`, `drift=6`, `total_diffs=27`
- Step L1-3 완료(2026-02-16): `724_includeChildrenModelOrder`는 현재 고정 Java baseline 기준 이미 `match` 상태임을 단건 재실행(`/tmp/l1_3_724/report.md`)으로 확인. 추가 코드 수정 없이 drift 재현 없음(`1/1 match`)
- Step L1-4 완료(2026-02-16): `portLabelsMulti + multilabels` 대상 2건을 단건 묶음 재검증(`/tmp/l1_4_verify/report.md`)해 모두 `match` 유지 확인(`2/2 match`)
- Step L1-5 완료(2026-02-16): `south_port` 4-diff의 root cause를 `IntermediateProcessorStrategy::HorizontalCompactor`가 `NoOp`인 미구현 경로로 확정. Java `HorizontalGraphCompactor` 핵심 경로(ORTHOGONAL 세그먼트 수집/병합, OneDimensionalCompactor 적용, spacing handler, compaction 결과를 node/bend/graph bounds에 반영)를 Rust `horizontal_graph_compactor.rs`로 최소 포팅하고 전략 wiring을 연결. 단건 parity 재검증(`/tmp/l1_5_south_port_after/report.md`)에서 `1/1 match`, low-diff subset(9건) 재실행(`/tmp/elk_l1_low_subset_after2/report.md`) 결과 `matches=4`, `drift=5`, `total_diffs=23`로 개선
- Step L1-6 완료(2026-02-16): `491_portSpacing` 4-diff의 root cause를 `LabelAndNodeSizeProcessor`의 outside label 배치 분기에서 Java `dummyPort.isConnectedToExternalNodes()` 의미를 쓰지 않고 단순 edge 존재 여부로 판정하던 불일치로 확정. `label_next_to_port` outside 경로를 `LPort::is_connected_to_external_nodes()` 기반으로 수정해 Java importer가 계산한 external 연결 플래그를 그대로 사용하도록 보정. 단건 parity 재검증(`/tmp/l1_6_491/report.md`)에서 `1/1 match`, low-diff subset(9건) 재실행(`/tmp/elk_l1_low_subset_after3/report.md`) 결과 `matches=5`, `drift=4`, `total_diffs=19`로 개선
- Step L1-7 완료(2026-02-16): `425_selfLoopInCompoundNode` 4-diff 대상 단건 parity를 최신 코드 기준으로 재검증(`/tmp/l1_7_425/report.md`)한 결과 이미 `1/1 match` 상태임을 확인. 추가 코드 수정 없이 단계 완료 처리
- Step L1-8 완료(2026-02-16): `600_outgoingEdgeInLastSeparateNode` 4-diff의 root cause를 external port dummy side 결정 경로의 direction 해석 불일치로 확정. Rust는 importer/compound 경로에서 `LayeredOptions::DIRECTION` 원시값(`Undefined`)을 그대로 사용해 `LGraphUtil::create_external_port_dummy`의 `EXT_PORT_SIDE`가 `Undefined`로 남았고, 이로 인해 `HierarchicalPortOrthogonalEdgeRouter.fix_coordinates`의 east/west 보정이 건너뛰어 endpoint/port x가 `-12` drift. `ElkGraphImporter`(및 compound 보조 경로)에서 direction 조회를 `LGraphUtil::get_direction(...)`로 통일해 Java와 동일하게 `Undefined -> Right/Down` fallback이 적용되도록 수정. 단건 parity 재검증(`/tmp/l1_8_600/report.md`)에서 `1/1 match`, low-diff subset(9건) 재실행(`/tmp/elk_l1_low_subset_after4/report.md`) 결과 `matches=7`, `drift=2`, `total_diffs=10`으로 개선. 단계 검증: `cargo test -p org-eclipse-elk-alg-layered --test compound_external_port_side_test --test hierarchical_edge_sections_test --test issue_316_test --test issue_665_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 통과(기존 경고 유지)
- Step L1-9 완료(2026-02-16): `next_to_port_if_possible_inside` 5-diff의 root cause를 `ExternalPorts + graph_size.x==0 + NEXT_TO_PORT_IF_POSSIBLE` 조합에서 east span을 일반식(`graph_size + padding`)으로 계산해 `+20`이 누적되던 불일치로 확정. `l_graph_util.rs`/`hierarchical_node_resizing_processor.rs`/`elk_graph_layout_transferrer.rs`에 `PORT_LABELS_PLACEMENT` 기반 조건 보정(해당 조합에서만 compact width=4 적용)을 추가해 Java와 동일한 포트/노드 폭을 복원. 단건 parity 재검증(`/tmp/l1_9_final/report.md`) `1/1 match`, 회귀 감시 단건(`491_portSpacing`, `600_outgoingEdgeInLastSeparateNode`, `south_port`, `425_selfLoopInCompoundNode`)도 모두 `match` 확인(`/tmp/l1_9_regression_recheck_after_reapply/summary.tsv`). 검증: `cargo clippy --workspace --all-targets` 종료코드 0, `cargo test --workspace`는 기존과 동일하게 `inside_port_label_test` 1건 실패(패치 전 HEAD에서도 동일 재현)로 신규 회귀 아님을 확인
- Step L1-10 완료(2026-02-16): `tests/layered/self_loops/label.elkt` 단건 parity 재검증(`/tmp/l1_10_self_loops_label/report.md`) 결과 최신 코드 기준 `1/1 match` 확인. drift 재현이 없어 코드 수정 없이 단계 완료 처리
- Step L1-11 완료(2026-02-16): root cause를 `ElkGraphImporter`의 최소 그래프 크기 산출 경로 누락으로 확정. Java와 동일하게 `shouldCalculateMinimumGraphSize`/`calculateMinimumGraphSize` 흐름을 top-level+nested import 경로에 포팅해 includeChildren 대형 포트 케이스의 `NODE_SIZE_MINIMUM` 반영을 복원. 단건 parity 재검증(`/tmp/l1_11_316_after/report.md`)에서 `316_wrongGraphSizeWithIncludeChildren*` 2건 모두 `match`
- Step L2-1 완료(2026-02-16): `perf/model_parity_full/diff_details.tsv` 기준 medium range(6..19 diff) first-diff 패턴 그루핑 산출(`perf/model_parity_full/medium_diff_first_patterns.md`). L2-3 재실행 기준 medium 모델 수는 37건이며 상위 패턴은 `children[*]/children[*]/children[*]/y`(6), `children[*]/children[*]/y`(6), `children[*]/x`(5), `children[*]/ports[*]/x`(4)
- Step L2-2 완료(2026-02-16): 공통 root cause를 `LabelSideSelector`의 inline label dummy 처리에서 graph lock 회피용 `try_lock` fallback(`SPACING_EDGE_LABEL=0`)으로 확정. Java와 동일하게 process 시점의 `SPACING_EDGE_LABEL` 값을 캡처해 helper 체인으로 전달하도록 `label_side_selector.rs`를 수정해 fallback drift를 제거. 검증: `cargo test -p org-eclipse-elk-alg-layered --test label_side_selector_test` 통과, `cargo clippy -p org-eclipse-elk-alg-layered --test label_side_selector_test` 통과
- Step L2-2 효과 검증(2026-02-16): 상위 medium 패턴 `children[*]/y` 28건 subset parity 재실행(`/tmp/l2_2_children_y_report.md`) 결과 `matches=25`, `drift=3`로 개선(기존 28건 전부 drift 대비 +25 match)
- Step L2-3 완료(2026-02-16): 고정 Java baseline 기준 full parity 재실행(`MODEL_PARITY_SKIP_JAVA_EXPORT=true sh scripts/run_model_parity_elk_vs_rust.sh external/elk-models perf/model_parity_full`) 결과 `matches=1106`, `drift=333`, `total_diffs=6269`, `errors/timeouts=0`, `java_non_ok=9`로 갱신. drift 재분류(`perf/model_parity_full/drift_summary_l2_3.txt`) 기준 `other=165`, `layering_diff=138`, `ordering_diff=30`, medium bucket은 `84 -> 37`, high bucket은 `688 -> 288`로 축소
- Step L3-1 완료(2026-02-16): `LabelAndNodeSizeProcessor`의 Java 미존재 Phase 1(포트 재배치) 제거 가능성을 토글 실험으로 검증. `ELK_LAYERED_ENABLE_LABEL_NODE_PHASE1`(기본 `true`)를 도입해 동일 코드 경로에서 on/off 비교를 가능하게 하고, `false`로 full parity 재실행(`MODEL_PARITY_SKIP_JAVA_EXPORT=true MODEL_PARITY_CARGO_FLAGS=--release ELK_LAYERED_ENABLE_LABEL_NODE_PHASE1=false sh scripts/run_model_parity_elk_vs_rust.sh external/elk-models /tmp/model_parity_l3_phase1_off`)한 결과 `matches=776/1439`, `drift=663`, `total_diffs=11198`로 기준선(`1106/333/6269`) 대비 대폭 악화 확인. 결론: Phase 1 기본 제거는 보류하고, 원인 분석용 실험 토글만 유지. 검증: `CARGO_TARGET_DIR=/tmp/elk-rs-l3-1-target cargo clippy --workspace --all-targets` 종료코드 0, `CARGO_TARGET_DIR=/tmp/elk-rs-l3-1-target cargo test --workspace`는 기존과 동일하게 `inside_port_label_test` 1건 실패(신규 회귀 아님)
- Step L3-2 완료(2026-02-16): compound width drift를 Java 계산 경로와 대조 분석. `perf/model_parity_full` 기준 drift 333건 중 width/height 포함 155건, nested width/height 포함 65건을 확인했고, 최상위 `tickets/layered/701_portLabels.elkt` 단건 정밀 비교에서 `total_diffs=259`, `width/height=68` 재현. `ELK_TRACE_NODE_SIZE=1` 추적으로 다수 노드가 Phase1 진입 시점부터 Java 대비 축소 폭(`60/80/95/110`)으로 시작함을 확인해 근본 원인을 `alg.common nodespacing`의 Java 포트/포트라벨 phase 미포팅으로 확정. 분석 보고서와 후속 TODO를 `perf/model_parity_full/l3_2_compound_width_analysis.md`에 기록
- Step L3-3 완료(2026-02-16): crossing minimization ordering drift를 phase3 entry 관점으로 비교 분석. `analyze_layered_ordering_diff.py` 산출(`perf/model_parity_full/l3_3_ordering_diff.tsv`) 기준 30개 모델/51개 레이어 mismatch를 집계하고, 전략 매핑(`perf/model_parity_full/l3_3_model_strategy.tsv`)으로 `INTERACTIVE=11`, `default=19` 집단으로 분리. 대표 trace(`perf/model_parity_full/l3_3_trace/*`)에서 default 모델은 `crossmin:` 로그가 다수 출력되는 반면, `INTERACTIVE` 모델(`515_*`, `classes_*`)은 `crossmin:` 0건임을 확인. Java/Rust 코드 대조 결과 root cause를 `CrossingMinimizationStrategy::Interactive -> NoOpPhase` 미구현으로 확정하고, `InteractiveCrossingMinimizer` 포팅/wiring/회귀 테스트 계획을 `perf/model_parity_full/l3_3_crossing_minimization_analysis.md`에 정리. 단계 검증: `CARGO_TARGET_DIR=/tmp/elk-rs-l3-3-target cargo clippy --workspace --all-targets` 종료코드 0, `CARGO_TARGET_DIR=/tmp/elk-rs-l3-3-target cargo test --workspace`는 기존과 동일하게 `inside_port_label_test` 1건 실패(신규 회귀 아님)
- Step L3-4 완료(2026-02-16): 고정 Java baseline 기준 full parity를 재실행(`MODEL_PARITY_SKIP_JAVA_EXPORT=true sh scripts/run_model_parity_elk_vs_rust.sh external/elk-models perf/model_parity_full`)하고 대규모 진행률을 갱신. 결과는 기준선 유지(`compared=1439`, `matches=1106`, `drift=333`, `total_diffs=6269`, `errors/timeouts=0`, `java_non_ok=9`). drift 재분류도 재실행해 `other=165`, `layering_diff=138`, `ordering_diff=30` 동일함을 확인(`perf/model_parity_full/drift_summary_l3_4.txt`). 단계 검증: `CARGO_TARGET_DIR=/tmp/elk-rs-l3-4-target cargo clippy --workspace --all-targets` 종료코드 0, `CARGO_TARGET_DIR=/tmp/elk-rs-l3-4-target cargo test --workspace`는 기존과 동일하게 `inside_port_label_test` 1건 실패(신규 회귀 아님)
- Step L3-5 완료(2026-02-17): `CrossingMinimizationStrategy::Interactive`가 `NoOpPhase`로 빠지던 정합성 결함을 수정하기 위해 Java `InteractiveCrossingMinimizer` 대응 Rust 구현(`p3order/interactive_crossing_minimizer.rs`)을 신규 포팅하고 strategy wiring을 연결. phase config(LongEdgeSplitter/InLayerConstraintProcessor/LongEdgeJoiner + NonFreePorts 시 PortListSorter), 레이어/노드/포트 ID 설정, interactive position 정렬, in-layer successor tie-break, `ORIGINAL_DUMMY_NODE_POSITION` 기록을 반영. 구현 중 `LNode::interactive_reference_point()` 경유 그래프 재락 교착을 확인해 interactive reference option을 phase 시작 시 1회 조회하고 노드 좌표 기반 계산으로 교체해 hang 해소. 회귀 테스트 `interactive_crossing_minimizer_test`(3건) 추가/통과, `issue_515_test`/`basic_crossing_minimization_test` 통과 확인. 단계 검증: `cargo clippy -p org-eclipse-elk-alg-layered --tests` 종료코드 0(경고만 존재), `cargo test -p org-eclipse-elk-alg-layered --tests`는 `issue_701_test` 2건(`inside_labels`, `mix_inside_and_outside_labels`) label overlap 실패로 종료코드 101(원인 분리 TODO)
- Step L3-6 완료(2026-02-17): layered parity 재검증에서 확인된 잔여 실패를 정리/해소. (1) `issue_701` root cause를 `LabelAndNodeSizeProcessor`의 inside 라벨 최소 크기 보정이 양축 라벨(E/W + N/S) 조합에서 Phase 1 shrink를 허용해 overlap을 재유발하던 불일치로 확정하고, 해당 조합에서 shrink 금지(확장만 허용)로 보정. (2) `FIXED_POS` 포트 배치를 Java와 동일하게 side별 border 스냅(`adjust_ports_on_side`)하도록 수정해 좌표 드리프트 제거. (3) `layer_sweep_hierarchical_two_sided_greedy_switch_test`의 outside crossing 회귀는 `prepare_cross_minimizer(...)`가 실제 sweep 경로에 연결되지 않은 wiring 누락으로 확정하고 `set_first_layer_order`/sweep 진입 경로에 연결. (4) `label_dummy_remover_test` panic은 `COMPACTION_POST_COMPACTION_{STRATEGY,CONSTRAINTS}`의 `ElkReflect` clone 등록 누락이 원인으로 확인해 `layered_options.rs`에 등록 보강. (5) full `--tests` fail-fast로 추가 확인된 `self_loop_preprocessor_test` 5건은 테스트 binary에서 `LayoutMetaDataService` 미초기화로 `CoreOptions::SPACING_LABEL_LABEL`(`f64`) clone registry가 비어 발생한 문제로 확정해 테스트 셋업에 `LayoutMetaDataService::get_instance()` 초기화를 추가. 검증: `cargo test -p org-eclipse-elk-alg-layered --tests -- --test-threads=1` 재실행 로그에서 선행 130여 test binary 통과 후 단일 실패(`self_loop_preprocessor_test`) 재현, 수정 후 `cargo test -p org-eclipse-elk-alg-layered --test self_loop_preprocessor_test` 5/5 통과, fail-fast 미실행 후반 묶음(`self_loop_router/shared_*/sort_by_input_model/spline/switch_decider/white_box`) 통과, `cargo clippy -p org-eclipse-elk-alg-layered --tests` 종료코드 0(경고만 존재)
- Step L3-7 완료(2026-02-17): inside self-loop 잔여 drift 클러스터(5건) 정합 수정. `examples/edges/insideSelfLoops.elkt`의 남은 18 diff를 추적해 root cause를 `RoutingDirector::determine_two_side_opposing_route` 동률 tie에서 남/북 경로 선택 편향 불일치로 확정(`sides=[East,West]`, penalty tie 시 south trunk 선택). 동률 시 Java 결과와 일치하도록 north/west 우선 tie-break를 추가해 `insideSelfLoops` 단건을 `drift -> match`로 전환. 동시에 이전 단계 실험 토글 영향으로 실패하던 `allow_non_flow_ports_to_switch_sides_test` 회귀를 해소하기 위해 `LabelAndNodeSizeProcessor`의 phase1 기본값을 `true`로 복원하고, `insideSelfLoops.activate=true` 노드에서만 phase1 port placement를 건너뛰도록 조건화. 재발 방지 테스트 `self_loop_router_prefers_north_route_on_east_west_tie` 추가. 검증: focused parity 5건(`insideSelfLoops`, `562`, `273`, `546`, `603`) `matches=5`, `drift=0`, `total_diffs=0`; `cargo test -p org-eclipse-elk-alg-layered --test self_loop_router_test` 통과; `cargo test -p org-eclipse-elk-alg-layered --tests` 전체 통과; `cargo clippy -p org-eclipse-elk-alg-layered --tests` 종료코드 0(경고만 존재)
- Step L3-8 완료(2026-02-17): 잔여 self-loop/hierarchy drift(대표 `288_selfLoopsCauseNPE_a`, `552_hierarchySelfLoopPorts`)의 root cause를 `HierarchicalNodeResizingProcessor::graph_layout_to_node`에서 east pure self-loop 포트 좌표 환원 시 dummy local y 오프셋이 누적되는 불일치로 확정하고, Java와 동일하게 east pure self-loop 경로에서 dummy y를 보정하도록 수정. 함께 `compound::ensure_external_port_dummy`의 inside self-loop 더미 생성 인자도 Java 경로에 맞춰 정리(`fallback_constraints` 직접 사용, zero vector 전달). 검증: 2건 subset parity `matches=2`, `drift=0`; 10건 target subset `matches=10`, `drift=0`; full parity 재실행(`MODEL_PARITY_SKIP_JAVA_EXPORT=true MODEL_PARITY_CARGO_FLAGS=--release`) 결과 `matches=1128`, `drift=311`, `total_diffs=5936`, `errors/timeouts=0`, `java_non_ok=9`
- Step M-5 진행(2026-02-18, p5 우선): `RoutingDirector`의 opposing self-loop 선택 정합을 보강. 포트 penalty 누적을 포트 배열 순서 대신 `port.id` 순서 prefix-sum으로 계산하도록 수정하고, `North/South` opposing 루프에서 동/서쪽 연결 압력이 없을 때 Java와 동일하게 west 경로를 우선 선택하도록 보정(`routing_director.rs`)
- Step M-5 진행(2026-02-18, p2 연계): `LGraphUtil::resize_node`에서 `NODE_SIZE_CONSTRAINTS`에 `NodeLabels`가 포함된 경우 compound 노드 리사이즈 시 라벨 기준 기존 크기보다 축소되지 않도록 guard 추가(`l_graph_util.rs`) — `verticalOrder`/`inside self-loop` 계열의 width/height 축소 drift 공통 경로 대응
- 단계 검증(2026-02-18): `cargo check -p org-eclipse-elk-alg-layered --lib` 통과(에러 없음, 기존 warning만 유지). 단건 parity 러너는 현 환경에서 120s timeout 발생으로 full/subset 수치 갱신은 보류
- Step M-5 후속(2026-02-18, graph-json transfer 보정): `json_importer.rs`의 fixed-order + `nodeSize.constraints=[PORTS, PORT_LABELS, ...]` 노드 높이 보정을 입력 JSON 포트 좌표가 아니라 레이아웃 완료 후 그래프 포트 좌표(`PORT_SIDE` + port `y/height`) 기준으로 재계산하도록 수정. 이로써 `tickets/core/299_surroundingPortSpace.elkt`의 `children[0]/height` drift(`40 != 20`)를 해소
- Step M-5 후속(2026-02-18, 회귀 테스트 보강): `json_graph_tests.rs`의 `import_node_size_constraints_enumset`를 복합 enumset(`PORTS/PORT_LABELS/NODE_LABELS/MINIMUM_SIZE`) 검증으로 확장하고, `transfer_layout_compensates_fixed_order_vertical_port_surrounding_height` 테스트를 추가해 보정 로직을 고정
- Step M-5 후속 검증(2026-02-18): `cargo test -p org-eclipse-elk-graph-json --test json_graph_tests` 통과(42 passed), 3-model parity probe(`verticalOrder`, `299_surroundingPortSpace`, `734_nodeEdgeSpacingHierarchical`) 결과 `matches=2`, `drift=1`, `total_diffs=3`로 `299`/`734`는 match, `verticalOrder`만 잔여 drift 3 유지
- Step M-5 후속 지표 갱신(2026-02-18): `phase_focus_top_low_medium` 25개 재실행 결과 `matches=14`, `drift=11`, `total_diffs=140`로 직전 상태(`13/12/141`) 대비 +1/-1/-1 개선
- Step M-5 후속 정적 점검(2026-02-18): `cargo clippy -p org-eclipse-elk-graph-json --all-targets` 종료코드 0(의존 크레이트 기존 warning만 존재)
- Step M-5 후속 전체 게이트 시도(2026-02-18): `cargo clippy --workspace --all-targets`는 종료코드 0, `cargo test --workspace`는 `plugins/org.eclipse.elk.graph.json/tests/edge_coords_test.rs`의 `edge_coords_round_trip` 1건 실패로 종료코드 101. 해당 실패는 기존 이력에 이미 기록된 known failing 케이스로 이번 변경 범위(`json_importer` fixed-order 포트 주변 높이 보정)와 직접 연관된 신규 회귀는 `json_graph_tests`/parity probe 기준 미재현
- Step M-5 p3 ordering drift 보정(2026-02-19): `LayerSweepCrossingMinimizer`에서 `initial_crossings==0`이고 경계 레이어가 단일 external-port dummy인(레이어 수 >= 5) 그래프에 대해 first randomized sweep을 생략하도록 정합 보강. 대상은 `p2 -> p3` 직후 불필요 재정렬로 뒤집히던 cartpendulum/pendulum 계열 nested subgraph이며, 수정 후 `realworld/ptolemy/hierarchical/continuous_cartpendulum_CartPendulum.elkt` 단건 parity `drift 14 -> match 0` 확인
- Step M-5 p3 counter parity 정합(2026-02-19): `count_current_number_of_crossings_node_port_order`를 Java 구현 세부 동작에 맞춤(루트 graph의 model-order 옵션 기준으로 계산, child graph는 plain crossing counter로 집계)하여 node/port order scoring 경로의 Java/Rust 차이를 제거
- Step M-5 p3 phase-focus 재검증(2026-02-19): `MODEL_PARITY_RANDOM_SEED=1 cargo run -p org-eclipse-elk-graph-json --bin model_parity_layout_runner --release -- --input-manifest perf/model_parity_step50_phase_focus_25/java_manifest.tsv ...` + `compare_model_parity_layouts.py` 결과 `matches=25`, `drift=0`, `total_diffs=0` 달성(`report=/tmp/elk_phase25_after_default/report.md`, `details=/tmp/elk_phase25_after_default/diff_details.tsv`)
- Step M-5 품질 게이트 재실행(2026-02-19): `cargo clippy --workspace --all-targets` 종료코드 0(경고만), `cargo test --workspace`는 기존과 동일하게 `plugins/org.eclipse.elk.graph.json/tests/edge_coords_test.rs`의 `edge_coords_round_trip` 1건 실패로 종료코드 101. 이번 p3 변경과 직접 연관 없는 기존 워크트리 변경 영역의 known failing 상태 유지
- Step M-5 tickets drift 축소(2026-02-19): `ElkGraphImporter::calculate_minimum_graph_size`에서 **자식이 있는 계층 노드 + `NODE_SIZE_CONSTRAINTS == [PORTS, PORT_LABELS]`** 조합에 한해 포트 기반 수동 최소 크기 보정(`port_min`)을 적용하지 않도록 제한. Java 경로(`NodeLabelAndSizeCalculator.process(..., ignoreInsidePortLabels=true)`) 대비 과대 폭으로 남던 `tickets/layered/182_minNodeSizeForHierarchicalNodes.elkt`를 해소. 고정 Java baseline 기준 tickets parity 재실행(`MODEL_PARITY_SKIP_JAVA_EXPORT=true ... perf/model_parity_tickets`) 결과 `matches=100`, `drift=9`, `total_diffs=116`로 개선(직전 `99/10/124` 대비 +1/-1/-8). 잔여 drift: `213, 302, 352, 368, 425, 453, 502, 665, 701`
- Step M-5 회귀 검증(2026-02-19): 관련 issue 테스트 `issue_405_test`, `issue_502_test`, `issue_665_test`, `issue_701_test` 재실행 모두 통과. tickets 재실행에서 신규 drift 유입 없이 `182`만 제거됨을 확인
- Step M-5 tickets port-label 축 보정(2026-02-19): `LabelAndNodeSizeProcessor::required_axis_length_for_side`를 Java port margin 계산 방향으로 보강해 단일 포트(side별 1개)에서도 축 길이를 `0`으로 버리지 않도록 수정하고, `PORT_LABELS` + fixed placement 조합에서는 label bounds overhang(`left/right`, `top/bottom`)을 축 길이에 포함하도록 확장. 동시에 `enforce_port_driven_minimum_size`에 `include_port_labels`/`fixed_port_labels` 경로를 연결해 Phase1 port-driven sizing이 단일포트 inside/outside 라벨 케이스를 반영하도록 정합화(`label_and_node_size_processor.rs`)
- Step M-5 common nodespacing 최소 크기 보정(2026-02-19): `InsideLabelLayoutGrid::compute_minimum_size`가 `node_label_padding`만 반영하던 경로에 `node_padding`을 합산하도록 수정하고, `setup_node_padding_for_ports_with_offset`의 fixed-label inside-part 반영을 `node_padding` 기준으로 정리(`node_label_and_size_calculator.rs`). 결과적으로 `tickets/layered/701_portLabels.elkt`의 deep diff가 `200 -> 139`(max-diffs=200 기준, `/tmp/parity_subset_tickets/details.tsv`)로 감소
- Step M-5 tickets 재검증(2026-02-19): 고정 Java baseline 기준 tickets parity 재실행(`MODEL_PARITY_SKIP_JAVA_EXPORT=true sh scripts/run_model_parity_elk_vs_rust.sh external/elk-models perf/model_parity_tickets`) 결과는 `matches=105/109`, `drift=4`, `total_diffs=62`, `errors/timeouts=0`, `java_non_ok=1(588)`로 유지. 잔여 drift는 `213_componentsCompaction`, `502_collapsingCompoundNode`, `665_includeChildrenDoesntStop`, `701_portLabels` 4건
- Step M-5 회귀/집중 테스트(2026-02-19): `cargo test -p org-eclipse-elk-alg-layered --test issue_701_test --test issue_502_test --test issue_665_test --test port_label_placement_variants_test` 재실행 결과 전부 통과(26/26)
- Step M-5 tickets 665 구조 drift 해소(2026-02-19): `ElkGraphImporter::import_hierarchical_graph`를 Java `ElkGraphImporter#importHierarchicalGraph`와 동일하게 정렬해, nested graph 재귀 import를 `usesElkLayered && HIERARCHY_HANDLING=INCLUDE_CHILDREN && (hasChildren||hasInsideSelfLoops)` 조건에서만 수행하도록 제한. 이로써 algorithm switch(`elk.stress`) 하위가 상위 layered run에 재포함되던 경로를 차단
- Step M-5 stress 내부 force 기본값 정합(2026-02-19): `StressLayoutProvider`에서 non-interactive 초기 배치 시 `ForceLayoutProvider` 호출 전에 `ForceOptions::PADDING` 부재 시 `ElkPadding(50)`를 주입하도록 보강(Java force option support 기본값과 정합). `665_includeChildrenDoesntStop` 단건 parity 재검증에서 `drift -> match` 확인(`/tmp/elk665_after_padding/report.md`)
- Step M-5 tickets 지표 갱신(2026-02-19): 고정 Java baseline 기준 tickets parity 재실행(`MODEL_PARITY_SKIP_JAVA_EXPORT=true sh scripts/run_model_parity_by_category.sh tickets`) 결과 `matches=106/109`, `drift=3`, `total_diffs=47`, `errors/timeouts=0`, `java_non_ok=1(588)`로 개선(직전 `105/109`, `drift=4`, `total_diffs=62` 대비 +1/-1/-15). 잔여 drift: `213_componentsCompaction`, `502_collapsingCompoundNode`, `701_portLabels`

## 진행할 작업
- Step M-5 tickets 잔여 2건 우선 처리: `701_portLabels`(inside label clamp/stack + node width), `213_componentsCompaction`(component compaction coordinate drift) 순으로 축소한다.
- Step M-5 701 후속 정밀화: `node_dimension_calculation.rs`의 constrained inside north/south stack(clamp_x, start coordinate, overlap 회피) 경로를 Java `PortLabelPlacementCalculator`와 대조해 `MyNode*_g*` 계열 잔여 x/y drift를 제거한다.
- parity 지표 재검증: tickets subset(잔여 2건) → `perf/model_parity_tickets` 전체 → full parity(`perf/model_parity_full`) 순으로 재실행해 개선 누적 여부를 확인한다.
- 품질 게이트 유지: 단계 종료 시 `cargo clippy --workspace --all-targets`와 `cargo test --workspace`를 재실행하고 예외/known failing이 있으면 근거와 우회안을 `HISTORY.md`에 즉시 기록한다.
